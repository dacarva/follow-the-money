# Eng Review: follow-the-money design (v1 = M1 + M2 + M3)

Generated by /plan-eng-review on 2026-09-25
Target: `~/.gstack/projects/follow-the-money` → plan `tru-master-design-20260925-050150.md` (Status: APPROVED, from /office-hours). Repo copy `docs/designs/follow-the-money.md` is identical except image paths.
Branch: master · Commit: 5f13fb8
Report file: this file (target is a directory, so the design doc itself is left untouched).

## Evidence gathered (bounded probes, 2026-09-25, public Socrata API, read-only)

| Probe | Result |
|---|---|
| SECOP II `jbjy-vk9h` license | **CC BY-SA 4.0** (datos.gov.co metadata `licenseId: CC_40_BY_SA`) |
| RUES `c82u-588k` license | **CC BY-SA 4.0** |
| SECOP `tipodocproveedor` split | CC 4,972,999 (82%) · NIT 907,365 (15%) · No Definido 168,963 · other small |
| `identificaci_n_representante_legal = 'Sin Descripcion'` | 3,927,407 overall; **604,989 of 907,365 NIT-contractor contracts (67%)**. Ratio flat 2018-2026 (e.g. 2025: 110,663 / 152,747) |
| `n_mero_de_documento_ordenador_del_gasto = 'No definido'` | 1,884,341 (31%) |
| `n_mero_de_documento_supervisor` placeholders | 'No definido' 2,193,649; '1' 6,581 |
| Top ordenador del gasto | one cédula on 29,697 contracts; ten ordenadores > 12k each |
| `fecha_de_firma IS NULL` | 426,226 (7%) |
| ID formats seen | `51.740.316`, `860009578-6`, `860.007.336-1` (company NITs inside the legal-rep field) |
| `es_grupo` | Si 40,123 / No 6,030,747 |
| `:updated_at` system field | present and queryable |
| `ceth-n4bn` Grupos de Proveedores SECOP II | members per group: `nit_grupo`, `nit_participante`, `participacion`, `es_lider_del_grupo` (CC BY-SA 4.0) |
| `qmzu-gj57` Proveedores Registrados SECOP II | 1,614,430 suppliers, `n_mero_doc_representante_legal` (current, snapshot), plus rep phone/email (PII) |
| RUES columns | `nit`, `digito_verificacion`, structured `primer_apellido/segundo_apellido/primer_nombre/segundo_nombre`, `num_identificacion_representante_legal`, `organizacion_juridica` |
| Socrata deletions | Consumer API exposes no tombstones; deleted rows simply vanish. A publisher full-replace bumps `:updated_at` on every row ([dev.socrata.com system fields](https://dev.socrata.com/docs/system-fields.html), [replace](https://dev.socrata.com/publishers/soda-producer/replace.html)) |
| nomenklatura | `Resolver` (judgment graph) + `SQLStore` on Postgres/psycopg3 exist and are maintained ([repo](https://github.com/opensanctions/nomenklatura)) |

Additional probe: RUES active non-natural-person entities 1,983,271, of which 1,755,619 (88.5%) carry `num_identificacion_representante_legal`. SECOP `qmzu-gj57` rep-doc field is populated on 1,614,428 rows but full of junk placeholders (`-`, `.`, `0`, `000000`, `0-0-0-` ...).

## Scope Challenge

### A. Assessment

- **Existing solutions.** FtM schema + statements ([docs](https://followthemoney.tech/docs/statements/)); nomenklatura `Resolver` (judgment graph with positive/negative/unsure) and `SQLStore` on Postgres. The design says "nomenklatura-style"; reuse vs reimplement is taken up in Section 2. Socrata `:updated_at` confirmed for incremental pulls. No repo code exists yet (commit 5f13fb8 has only docs/openspec), so nothing in-repo to reuse.
- **Complexity.** Design proposes 2 apps + 2 packages + Postgres, well over 8 files and 2+ new services. Complexity gate tripped.
- **Search check.** [Layer 1] FtM + nomenklatura, Postgres + pg_trgm, Cytoscape.js. [Layer 1] Socrata consumer API has no tombstones; a full replace touches every row. No [EUREKA].
- **TODOS.md.** Not present in repo.
- **Distribution.** Web service only; GitHub Actions to staging, manual prod promotion (design lines 236-238). Self-host compose deferred past M2.

### B. Scope record (post-answer)

feature answers: D3 = A "M1 only (recommended)" (M2/M3 get their own eng review before work starts; M1 architecture still checked for M2/M3 compatibility); structure: D4 = B "Smaller arrangement (recommended)"; accepted scope: M1 (SECOP II + RUES explorer: search, entity page, expand contractor → legal rep → other contractors → contracts/agencies → ordenador del gasto, evidence drawer, shareable URLs, masking, correction flow, PNG download, staging deploy, widen to national, launch gate) built as `apps/web` (React+TS+Vite+Cytoscape) + one Python project `backend/` with modules `<pkg>/ids` (normalize, validate, mask), `<pkg>/ingest`, `<pkg>/resolver`, `<pkg>/api`, two entrypoints (api server, ingest CLI), Postgres via docker-compose. `<pkg>` name waits on design Open Question 3 (must not be `followthemoney`); pending remedies: F1, F2, F4, F5 and all Section 1-4 findings.

### C. Findings

1. **F1 [P1] (confidence 9/10) design line 75, 195** — "The M1 hop 'legal rep → other contractors' uses SECOP's rep-at-signing". Probe: rep-at-signing is `Sin Descripcion` on 604,989 of 907,365 company-contractor contracts (67%), flat across 2018-2026. RUES current rep exists for 88.5% of active companies but the design makes it a display-only edge. The core M1 discovery hop works for about a third of company contracts.
2. **F2 [P1] (confidence 9/10) design line 207, 224 Open Q7** — "Colombian open data; verify the per-dataset license". Probe: SECOP II, RUES, Grupos de Proveedores are all **CC BY-SA 4.0**. Share-alike attaches to the adapted database and to published adaptations (PNG downloads). In M2/M3 it collides with CC BY-NC (OpenSanctions PEPs, line 210) and ODbL (ICIJ, line 211).
3. **F3 [factual correction] (confidence 8/10) design line 218 Open Q2** — "which dataset lists unión temporal / consorcio members". Answer: `ceth-n4bn` Grupos de Proveedores SECOP II lists members with `nit_participante`, `participacion`, `es_lider_del_grupo`. 40,123 group contracts. The design's conditional "if member data is available, group → member edges carry member IDs" (line 161) now applies. Unverified: that `nit_grupo` equals the contract's `documento_proveedor`; verify in the hand trace. No question needed.
4. **F4 [P2] (confidence 9/10) design line 75, 77** — "SECOP signing date → 'rep at signing'" and blocklist "`0`, repeated digits, `999999999`, 'No Definido', 'N/A'". Probe: `fecha_de_firma` NULL on 426,226 rows (7%); dominant placeholders are `Sin Descripcion` (3.9M) and `No definido` (mixed case), supervisor `1`. The blocklist extension is required implementation of the approved blocklist (carried, no question). The missing-date fallback is a new policy (Section 2).
5. **F5 [P2] (confidence 8/10) design line 168** — "incremental refresh via Socrata `:updated_at`". Socrata exposes no deletions and a publisher full-replace re-stamps all 6M rows. Handled in Section 1.

## Decision ledger

### R1: How the M1 shared-legal-rep hop handles contracts with no rep-at-signing
Finding: F1, P1, confidence 9/10, design line 75 + 195, reviewer: Claude (probe)
Plan baseline: original proposal, design line 75: "The M1 hop 'legal rep → other contractors' uses SECOP's rep-at-signing ... RUES's current rep is shown as a separate, labeled edge." (display only, not traversed)
Runtime evidence: rep-at-signing missing on 67% of company-contractor contracts (probe above); RUES current rep present on 88.5% of active companies.
Comparison grid:

| Choice | Current | A | B | C |
|---|---|---|---|---|
| R1 which rep edges the "other contractors" hop traverses | rep-at-signing only | rep-at-signing + RUES current rep (company → rep, "vigente a <fecha de corte>"), each styled/labeled distinctly; current rep never attached to a contract date | A + SECOP Proveedores Registrados (`qmzu-gj57`) current rep as a second current-rep source (rep phone/email never ingested) | unchanged: rep-at-signing only; RUES rep display-only |
| Coverage of company contracts reachable by the hop (est.) | ~33% | ~33% at-signing + current rep for ~88% of active RUES companies | A + SECOP-registered suppliers missing from RUES | ~33% |
| Extra work | none | label/edge style, expand query includes 2 edge kinds, tests (human ~1 day / CC ~30 min) | A + 1 adapter with junk-placeholder cleanup (human ~3 days / CC ~1.5 h) | none |
| Time-semantics rule (line 75, no back-dating) | approved | kept | kept | kept |
| Masking, suppression, all other pending choices | pending/approved as elsewhere | unchanged | unchanged | unchanged |

Question D5:
D5 — Let the shared-legal-rep hop also follow RUES's current legal rep?
Project/branch/task: follow-the-money on master, M1 (design line 75, 195).
ELI10: M1's headline move is contractor → its legal rep → the other companies that person represents. The design only follows the legal rep recorded on the contract at signing. I queried SECOP: that field says "Sin Descripcion" on 67% of company contracts, every year since 2018. RUES has the company's current legal rep for 88.5% of active companies, but the design only displays it and never follows it. As designed, the main discovery path is dead on two of every three company contracts.
Stakes if we pick wrong: Keep it as-is and the Sunday test may land on a contractor with no traversable rep; follow current reps carelessly and a 2026 rep gets implied for a 2018 contract.
Recommendation: A because it restores most coverage with no new source, and the "vigente a <fecha de corte>" label keeps the no-back-dating rule intact.
Completeness: A=8/10, B=9/10, C=4/10
Pros / cons:
A) Follow RUES current rep too, labeled (recommended)
  ✅ Hop works for ~88% of active companies instead of ~33% of company contracts
  ✅ No new source; RUES is already in M1 and reachable-minimized
  ❌ Current rep is a present-day fact; UI must never show it as the rep at a contract's signing
B) A plus SECOP Proveedores Registrados current rep
  ✅ Adds reps for SECOP suppliers not registered in any cámara de comercio
  ✅ Second independent current-rep source can corroborate RUES
  ❌ Extra adapter and heavy junk-placeholder cleanup (human ~3 days / CC ~1.5 h)
C) Keep design: follow rep-at-signing only
  ✅ Strictest time semantics; every traversed rep is tied to a contract date
  ✅ No extra edge kind to label, style or test
  ❌ Core M1 hop missing on ~2 of 3 company contracts; Sunday test fragile
Net: one extra labeled edge kind versus a headline feature that fails most of the time.
Header: Rep hop
Options:
A) Follow current rep, labeled (recommended)
The "other contractors" hop traverses both rep-at-signing (SECOP, dated) and RUES current rep (company → person, labeled "representante legal vigente a <fecha de corte> (RUES)"), with distinct edge styles. Current rep is never attached to a contract date. human ~1 day / CC ~30 min. Low risk; maintenance: one more edge kind in expand queries and tests.
B) A + SECOP supplier registry
Everything in A, plus ingest SECOP Proveedores Registrados (qmzu-gj57) current rep as a second current-rep source, reachable rows only, rep phone/email never ingested. human ~3 days / CC ~1.5 h. Medium risk: junk placeholders; one more adapter to maintain.
C) Keep rep-at-signing only
No change to the design: the hop follows only SECOP rep-at-signing; RUES current rep stays display-only. Zero extra work; the hop is unavailable on ~67% of company contracts.

State: approved
Actual answer: A) Follow current rep, labeled (recommended), D5 answer 2026-09-25
Accepted scope: The "other contractors" hop traverses both rep-at-signing (SECOP, dated) and RUES current rep (company → person, labeled "representante legal vigente a <fecha de corte> (RUES)"), with distinct edge styles. Current rep is never attached to a contract date. No SECOP Proveedores Registrados adapter. Time-semantics rule (design line 75) unchanged.
History: none

### R2: Data licenses for M1 (SECOP II / RUES are CC BY-SA 4.0)
Finding: F2, P1, confidence 9/10, design line 207 + 224 (Open Q7), reviewer: Claude (probe of datos.gov.co metadata)
Plan baseline: original proposal, design line 207: "Colombian open data; verify the per-dataset license | Yes, with attribution"; Open Question 7 left open.
Runtime evidence: `jbjy-vk9h`, `c82u-588k`, `ceth-n4bn`, `qmzu-gj57` all report `licenseId: CC_40_BY_SA`. Compatibility of BY-SA with CC BY-NC (OpenSanctions, M2) and ODbL (ICIJ, M3) for combined derived data: unknown, needs a lawyer.
Comparison grid:

| Choice | Current | A | B | C |
|---|---|---|---|---|
| R2 licensing table + Open Q7 | "verify" | record CC BY-SA 4.0 per dataset, close Open Q7 | same as A | unchanged |
| M1 notices | "with attribution" | attribution + CC BY-SA 4.0 notice in site footer, evidence drawer, API responses (`license` + `attribution` fields per statement/source), and embedded in PNG downloads | attribution + BY-SA notice in footer and PNG only | attribution only |
| M1 legal review scope | masking, wording, correction flow, suppression | + share-alike obligations for the derived DB and BY-SA × BY-NC × ODbL compatibility, answered before M2/M3 source choices | unchanged; compatibility deferred to M2 eng review | unchanged |
| Work | none | human ~0.5 day / CC ~20 min + 1 legal-review agenda item | human ~2 h / CC ~10 min | none |
| M2 source choice (e.g. PEPs from primary source vs OpenSanctions) | M2 design | not decided here (M2 review) | not decided here | not decided here |

Question D6:
D6 — Handle the CC BY-SA 4.0 license on SECOP II and RUES in M1?
Project/branch/task: follow-the-money on master, M1 (design Licensing table line 207, Open Question 7).
ELI10: The design left the datos.gov.co license as "verify". I checked: SECOP II, RUES and the SECOP consortium-members dataset are all CC BY-SA 4.0. Share-alike means anything you publish that is adapted from them (the graph, API output, PNG downloads, any future dump) must carry attribution and be offered under the same license. It also matters later: M2 plans PEPs from OpenSanctions (CC BY-NC) and M3 plans ICIJ (ODbL), and mixing those with BY-SA data in one derived dataset may be incompatible. Only a lawyer can settle that, and the M1 legal review is already booked.
Stakes if we pick wrong: Ship without BY-SA notices and the public site misstates its data terms from day one; discover the BY-NC/ODbL clash after building M2/M3 and a source has to be swapped late.
Recommendation: A because the notices are cheap, and putting the compatibility question on the already-booked M1 legal review settles it before M2 source choices.
Completeness: A=9/10, B=6/10, C=3/10
Pros / cons:
A) Notices everywhere + compatibility on M1 legal review (recommended)
  ✅ Every published surface (site, drawer, API, PNG) states source and CC BY-SA 4.0 terms
  ✅ BY-SA vs BY-NC vs ODbL answered before M2/M3 pick sources, not after
  ❌ Adds an agenda item to the lawyer's review; API responses carry license fields
B) Footer + PNG notice only; compatibility left to M2 review
  ✅ Smallest change that still makes the public site honest about terms
  ✅ Keeps the M1 legal review focused on masking and corrections
  ❌ API consumers get unlabeled BY-SA data; compatibility risk surfaces late
C) Leave as-is (attribution only, Open Q7 open)
  ✅ No work now
  ✅ Nothing changes in the approved design text
  ❌ Public launch without the share-alike notice the license requires
Net: half a day plus one lawyer question now versus relabeling surfaces and swapping sources later.
Header: Licenses
Options:
A) Full notices + legal item (recommended)
Record CC BY-SA 4.0 for SECOP II, RUES and Grupos de Proveedores; close Open Q7. Attribution + CC BY-SA 4.0 notice in the site footer, evidence drawer, API responses (license + attribution fields per source) and embedded in PNG downloads. Add share-alike obligations and BY-SA × BY-NC × ODbL compatibility to the M1 legal review, answered before M2/M3 source choices. M2 source choice itself not decided here. human ~0.5 day / CC ~20 min.
B) Footer + PNG notice only
Record CC BY-SA 4.0 and close Open Q7. Attribution + BY-SA notice in the site footer and PNG downloads only; API and drawer unchanged. Compatibility question deferred to the M2 eng review. human ~2 h / CC ~10 min.
C) Leave as-is
No change: attribution only, Open Question 7 stays open, legal review scope unchanged. Zero work; the site omits the share-alike notice.

State: approved
Actual answer: A) Full notices + legal item (recommended), D6 answer 2026-09-25
Accepted scope: Record CC BY-SA 4.0 for SECOP II, RUES and Grupos de Proveedores; close Open Q7. Attribution + CC BY-SA 4.0 notice in the site footer, evidence drawer, API responses (license + attribution fields per source) and embedded in PNG downloads. Add share-alike obligations and BY-SA × BY-NC × ODbL compatibility to the M1 legal review, answered before M2/M3 source choices. M2 source choice itself not decided here.
History: none

Scope Challenge dispositions: F1 → R1 approved (A). F2 → R2 approved (A). F3 factual correction recorded (group members from `ceth-n4bn`; join key verified in hand trace). F4 blocklist extension carried as required implementation of the approved blocklist (case/space-insensitive; add `Sin Descripcion`, `No definido`, 1-3 digit IDs, all-zero and punctuation-only values; final list from a top-N frequency scan of each ID column); F4 date fallback → Section 2. F5 → Section 1.

## Section 1: Architecture review

Findings:
1. **A1 [P1] (confidence 9/10) design line 80, 195** — "Genuinely prolific individuals ... keep their exact edges" and M1 path "legal rep → other contractors → contracts/agencies → ordenador del gasto". Probe: one ordenador del gasto cédula sits on 29,697 contracts, ten exceed 12k; a supervisor on 16,350; a rep-at-signing on 2,329. No per-expand cap exists (office-hours R3-16 unresolved). One click can ask Cytoscape and the API for ~30k nodes.
2. **A2 [P1] (confidence 8/10) design line 80 + 188-189** — suspect IDs: "until then the edge is shown as a lead", but lead UI ("Mostrar pistas") is M3. M1 has no defined rendering for suspect-ID edges (office-hours R3-1).
3. **A3 [P2] (confidence 7/10) design line 80** — suspect rule "it equals a known agency or contracting-entity NIT" flags legitimate interadministrative contracts where a public entity is the contractor (office-hours R3-2).
4. **A4 [P2] (confidence 8/10) design line 168** — incremental refresh by `:updated_at` misses Socrata deletions and treats a publisher full replace as 6M changes (F5).
5. **A5 [P2] (confidence 7/10) design line 170 + 154** — "materialized entity/edge tables refreshed incrementally for changed source records only" plus "Derived edge (e.g. shared legal rep)". Postgres `REFRESH MATERIALIZED VIEW` is whole-view only, and precomputed pairwise shared-rep edges grow quadratically for hub reps. The storage shape for derived edges is unspecified.

### R3: Expansion limits per click
Finding: A1, P1, confidence 9/10, design line 80 + 195, reviewer: Claude (probe)
Plan baseline: original proposal: no expansion cap; overlay limit "2 hops, p95 ≤ 800 ms" (line 98) applies to M3 hypotheses only.
Runtime evidence: max degrees observed: ordenador 29,697 contracts; supervisor 16,350; rep-at-signing 2,329.
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| R3 nodes returned per expand | unbounded | top 50 neighbors per edge kind; remainder collapsed into aggregate nodes "N contratos más" grouped by agency (or by year when one agency), each aggregate drillable with paging of 50 | top 50, remainder shown as a count only ("y 29.647 más") with a table view link, no graph drill-down |
| Neighbor ordering | unspecified | contract value desc, then signing date desc, ties by id | same as A |
| API contract | unspecified | `/entity/:id/expand?kind=&cursor=` returns nodes + aggregates + `total` | `/entity/:id/expand?kind=&cursor=` returns nodes + `total` |
| Work | none | human ~2 days / CC ~1 h | human ~1 day / CC ~30 min |

Question D7:
D7 — Cap how many nodes one expand click can return?
Project/branch/task: follow-the-money on master, M1 graph expand (design lines 80, 195).
ELI10: Some people sit on an enormous number of contracts. I found one ordenador del gasto cédula on 29,697 SECOP contracts and a supervisor on 16,350. The design keeps all these edges (correctly) but sets no limit on what one click returns. Clicking that person would try to draw ~30k nodes, freeze the browser and blow the API latency. We need a cap and a way to see the rest.
Stakes if we pick wrong: No cap means the most newsworthy nodes (prolific officials) are the ones that crash the page.
Recommendation: A because grouping the overflow by agency keeps the graph useful for exactly the hub people journalists care about.
Completeness: A=9/10, B=7/10
Pros / cons:
A) Top 50 + drillable aggregates by agency/year (recommended)
  ✅ Hub nodes stay explorable: "1.240 contratos con Alcaldía X" is itself a lead
  ✅ Bounded payload and render cost on every click, keyed by one stable ordering
  ❌ Aggregate nodes add a node type, API shape and tests (human ~2 days / CC ~1 h)
B) Top 50 + count + table link
  ✅ Simplest bounded version; table view handles the long tail
  ✅ No new node type in the graph
  ❌ The overflow leaves the graph, so agency concentration patterns are invisible there
Net: one aggregate node type versus losing the pattern hub nodes are meant to reveal.
Header: Expand cap
Options:
A) Top 50 + aggregates (recommended)
Each expand returns at most 50 neighbors per edge kind, ordered by contract value desc, then signing date desc, then id. The rest collapse into aggregate nodes "N contratos más" grouped by agency (by year when there is one agency); each aggregate drills down 50 at a time via cursor. API: /entity/:id/expand?kind=&cursor= returns nodes, aggregates and total. human ~2 days / CC ~1 h.
B) Top 50 + count + table
Each expand returns at most 50 neighbors per edge kind with the same ordering, plus a total count ("y 29.647 más") linking to a paged table view; no aggregate nodes in the graph. API: /entity/:id/expand?kind=&cursor= returns nodes and total. human ~1 day / CC ~30 min.

State: approved
Actual answer: A) Top 50 + aggregates (recommended), D7 answer 2026-09-25
Accepted scope: Each expand returns at most 50 neighbors per edge kind, ordered by contract value desc, then signing date desc, then id. The rest collapse into aggregate nodes "N contratos más" grouped by agency (by year when there is one agency); each aggregate drills down 50 at a time via cursor. API: /entity/:id/expand?kind=&cursor= returns nodes, aggregates and total.
History: none

### R4: How M1 renders suspect-ID edges (no lead UI until M3)
Finding: A2, P1, confidence 8/10, design line 80 + 188-189, reviewer: Claude (office-hours R3-1 carried)
Plan baseline: original proposal, line 80: "until then the edge is shown as a lead"; lead UI deferred to M3 (line 189).
Runtime evidence: unknown volume; design says queue size is measured on the M1 cohort.
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| R4 suspect-ID edge in M1 graph | "shown as a lead" (no lead UI exists in M1) | excluded from graph, search and expand; affected node shows neutral marker "vínculo en revisión" with no counterpart named; moderator resolves via admin CLI → exact edge or rejected | shown as dashed edge labeled "identificador en revisión" linking both nodes; wording added to M1 legal review |
| M1 legal review scope | per R2 | + "vínculo en revisión" marker wording | + dashed-edge wording |
| Work | none | human ~0.5 day / CC ~20 min | human ~1 day / CC ~30 min |

Question D8:
D8 — What does an M1 viewer see for a suspect-ID edge?
Project/branch/task: follow-the-money on master, M1 (design line 80; lead UI is M3 per line 189).
ELI10: When an ID looks like a data-entry error (e.g. names attached to it disagree), the design downgrades the edge to "suspect" and queues it for moderator review, saying "until then the edge is shown as a lead". But M1 has no lead display at all; that ships in M3. So today the design is silent on what an M1 viewer sees. Either the edge disappears with a neutral marker, or M1 gets a small dashed "en revisión" edge.
Stakes if we pick wrong: Show a dashed edge and an unverified person-to-company link is public before any lead wording is legally reviewed; hide it and some real links wait for moderator review.
Recommendation: A because M1 should publish only exact, validated edges, and the marker still tells journalists something is pending.
Completeness: A=8/10, B=8/10
Pros / cons:
A) Hide edge, show neutral "vínculo en revisión" marker (recommended)
  ✅ M1 publishes only validated exact-ID edges; nothing unverified names a person
  ✅ Marker tells a journalist a link exists and is pending, without naming it
  ❌ Real links sit invisible until the maintainer reviews them via the admin CLI
B) Dashed "identificador en revisión" edge
  ✅ Journalists see the pending link immediately and can verify it themselves
  ✅ Early test of lead-style wording before M3
  ❌ Unverified person link is public in M1; needs extra legal-review wording
Net: stricter publication with slower visibility versus faster visibility with public unverified links.
Header: Suspect IDs
Options:
A) Hide + neutral marker (recommended)
Suspect-ID edges are excluded from graph, search and expand in M1. The affected node shows a neutral "vínculo en revisión" marker without naming the counterpart. The maintainer resolves each via the admin CLI into an exact edge or a rejection. Marker wording joins the M1 legal review. human ~0.5 day / CC ~20 min.
B) Dashed "en revisión" edge
Suspect-ID edges render in M1 as dashed edges labeled "identificador en revisión" linking both nodes, with the evidence drawer showing the conflicting records. Dashed-edge wording joins the M1 legal review. human ~1 day / CC ~30 min.

State: approved
Actual answer: A) Hide + neutral marker (recommended), D8 answer 2026-09-25
Accepted scope: Suspect-ID edges are excluded from graph, search and expand in M1. The affected node shows a neutral "vínculo en revisión" marker without naming the counterpart. The maintainer resolves each via the admin CLI into an exact edge or a rejection. Marker wording joins the M1 legal review.
History: none

### R5: When an agency NIT makes an ID "suspect"
Finding: A3, P2, confidence 7/10, design line 80, reviewer: Claude (office-hours R3-2 carried)
Plan baseline: original proposal, line 80: an ID is suspect when "it equals a known agency or contracting-entity NIT".
Runtime evidence: unverified volume. SECOP includes contratos/convenios interadministrativos where public entities are contractors; SoQL cannot join `documento_proveedor` to `nit_entidad`, so count it on the M1 cohort.
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| R5 agency-NIT suspect trigger | any occurrence of an agency NIT as an ID | only (1) contractor NIT equals the contracting entity's own NIT on the same contract, or (2) an agency/legal-entity NIT appears in a natural-person field (rep, ordenador, supervisor); public-entity contractors are normal exact edges | unchanged |
| Name-disagreement trigger (rule 4 second half) | pending (Section 2) | unchanged, pending | unchanged, pending |
| Work | n/a | human ~2 h / CC ~10 min + tests | none |

Question D9:
D9 — Stop flagging every agency NIT as a suspect ID?
Project/branch/task: follow-the-money on master, M1 ID validation rule 4 (design line 80).
ELI10: Rule 4 marks an ID suspect when it equals any known agency NIT. But in SECOP, public entities regularly contract each other: a public university or utility is the contractor on another agency's contract. Under the current rule, every such legitimate contract gets hidden (per D8) and sent to the one-person review queue. The fix flags only the impossible cases: an agency contracting itself, or an agency NIT sitting in a person field like legal rep or ordenador.
Stakes if we pick wrong: Keep the rule and thousands of real public-to-public contracts vanish into a review queue one maintainer cannot clear.
Recommendation: A because it keeps the data-entry-error signal and drops the false positives that would flood the queue.
Completeness: A=9/10, B=4/10
Pros / cons:
A) Flag only impossible placements (recommended)
  ✅ Interadministrative contracts render as normal exact edges, as they should
  ✅ Still catches the real errors: self-contracting and agency NITs in person fields
  ❌ Needs an agency-NIT set built from nit_entidad, plus tests (human ~2 h / CC ~10 min)
B) Keep rule as written
  ✅ No change to the approved text
  ✅ Maximally conservative about anything touching an agency ID
  ❌ Floods the single-moderator queue and hides legitimate public-entity contractors
Net: two precise triggers versus a blanket rule that hides real contracts.
Header: Agency NIT
Options:
A) Flag impossible placements (recommended)
An ID is suspect via agency NIT only when (1) the contractor NIT equals the contracting entity's own NIT on the same contract, or (2) an agency/legal-entity NIT appears in a natural-person field (legal rep, ordenador del gasto, supervisor). Public-entity contractors are normal exact edges. Name-disagreement trigger unchanged (decided separately). human ~2 h / CC ~10 min.
B) Keep rule as written
Any ID equal to a known agency or contracting-entity NIT is suspect, as in design line 80. No work.

State: approved
Actual answer: A) Flag impossible placements (recommended), D9 answer 2026-09-25
Accepted scope: An ID is suspect via agency NIT only when (1) the contractor NIT equals the contracting entity's own NIT on the same contract, or (2) an agency/legal-entity NIT appears in a natural-person field (legal rep, ordenador del gasto, supervisor). Public-entity contractors are normal exact edges. Name-disagreement trigger unchanged (decided separately).
History: none

### R6: Detect deleted / withdrawn source rows
Finding: A4 (F5), P2, confidence 8/10, design line 168, reviewer: Claude (probe + Socrata docs)
Plan baseline: original proposal, line 168: "incremental refresh via Socrata `:updated_at`" (no deletion handling).
Runtime evidence: `:updated_at` present; the consumer API exposes no tombstones, deleted rows just stop appearing ([docs](https://dev.socrata.com/docs/system-fields.html)).
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| R6 deletion detection | none | weekly reconciliation: keyset-page every `:id` per ingested dataset; source records no longer present get `retracted_at` on their statements, leave public views, and stay stored for audit and Ley 1581 access responses | none (as designed) |
| R7 full-replace handling (content hash) | none | unchanged, pending (R7) | unchanged, pending (R7) |
| Work | none | human ~1.5 days / CC ~40 min | none |

Question D10:
D10 — Detect SECOP/RUES rows that the publisher deletes?
Project/branch/task: follow-the-money on master, M1 ingest (design line 168).
ELI10: The design pulls only rows whose Socrata `:updated_at` changed. When datos.gov.co deletes or withdraws a row, the API simply stops returning it; nothing tells us. Our copy would keep showing, and citing, a contract that no longer exists officially. A weekly sweep of all row IDs finds the missing ones and retracts them from public views while keeping them for audit.
Stakes if we pick wrong: Public pages keep citing withdrawn government records, on a site whose promise is "every edge cites its source".
Recommendation: A because a stale citation is exactly what a correction request or a hostile lawyer would find first, and the sweep is cheap.
Completeness: A=9/10, B=4/10
Pros / cons:
A) Weekly ID sweep with retraction (recommended)
  ✅ Withdrawn records leave public views within a week of deletion at the source
  ✅ Retracted statements stay stored, so audits and Ley 1581 access answers remain complete
  ❌ Sweep pages ~6M ids weekly (~1 h of API calls); retraction state needs tests
B) No deletion detection
  ✅ No extra job or state column
  ✅ Simplest ingest loop, as approved
  ❌ Deleted source rows stay on the public site indefinitely
Net: one weekly job versus stale citations that never go away.
Header: Deletions
Options:
A) Weekly ID sweep (recommended)
Weekly reconciliation keyset-pages every :id per ingested dataset; source records no longer present get retracted_at on their statements, leave public views, and remain stored for audit and Ley 1581 access responses. Content-hash handling decided separately (R7). human ~1.5 days / CC ~40 min.
B) No deletion detection
Keep the design's :updated_at-only pull; deleted source rows are never detected. Content-hash handling decided separately (R7). No work.

State: approved
Actual answer: A) Weekly ID sweep (recommended), D10 answer 2026-09-25
Accepted scope: Weekly reconciliation keyset-pages every :id per ingested dataset; source records no longer present get retracted_at on their statements, leave public views, and remain stored for audit and Ley 1581 access responses. Content-hash handling decided separately (R7).
History: Earlier draft of D10 (not sent) bundled deletion sweep with content hashing as options "both / hash only / neither"; split into R6 and R7 because a sweep-only answer was not offered.

### R7: Skip unchanged rows on publisher full re-uploads (content hash)
Finding: A4 (F5), P2, confidence 8/10, design line 168 + 170, reviewer: Claude (Socrata docs)
Plan baseline: original proposal, line 168/170: incremental refresh by `:updated_at`; materialized tables refreshed "for changed source records only".
Runtime evidence: a publisher full replace re-stamps `:updated_at` on every row ([docs](https://dev.socrata.com/publishers/soda-producer/replace.html)); SECOP has 6,070,870 rows.
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| R7 change detection per source record | any `:updated_at` bump = changed | SHA-256 of the canonicalized ingested props per source record; unchanged hash writes no statements and triggers no derived-table update | as designed |
| Mass-change alert | none | not decided here (TODO candidate) | not decided here |
| R6 deletion sweep | approved (D10 A) | unchanged | unchanged |
| Work | none | human ~1 day / CC ~30 min | none |

Question D11:
D11 — Hash each source row so a full re-upload doesn't rewrite everything?
Project/branch/task: follow-the-money on master, M1 ingest (design lines 168, 170).
ELI10: When datos.gov.co re-uploads a whole dataset, every row's `:updated_at` changes even if the content didn't. The design would then rewrite ~120M statements and refresh every derived table, for nothing. Storing a hash of the ~20 fields we actually ingest lets ingest skip rows whose content is the same.
Stakes if we pick wrong: One re-upload turns a minutes-long nightly refresh into many hours of rewrite and index churn on the production database.
Recommendation: A because the hash is one column and one comparison, and it also makes re-runs idempotent.
Completeness: A=9/10, B=5/10
Pros / cons:
A) Content hash (recommended)
  ✅ Re-uploads cost a hash compare per row, not 120M statement rewrites
  ✅ Ingest becomes idempotent: re-running a pull writes nothing new
  ❌ Hash must be canonical (field order, whitespace, number formats) or it churns
B) Keep :updated_at only
  ✅ No extra column or canonicalization code
  ✅ Matches the approved text exactly
  ❌ Every publisher re-upload triggers a full rewrite and derived-table rebuild
Net: one hash column versus multi-hour rewrite storms.
Header: Row hash
Options:
A) Content hash (recommended)
Store a SHA-256 of the canonicalized ingested props per source record; an unchanged hash writes no statements and triggers no derived-table update. Deletion sweep (R6) unchanged; mass-change alert not decided here. human ~1 day / CC ~30 min.
B) Keep :updated_at only
Every :updated_at bump is treated as a change, as in the design. No hash. Deletion sweep (R6) unchanged; mass-change alert not decided here. No work.

State: approved
Actual answer: A) Content hash (recommended), D11 answer 2026-09-25
Accepted scope: Store a SHA-256 of the canonicalized ingested props per source record; an unchanged hash writes no statements and triggers no derived-table update. Deletion sweep (R6) unchanged; mass-change alert not decided here.
History: Draft (not sent) bundled a > 20% mass-change alert; alert moved out as separate instrumentation (TODO candidate).

### R8: Storage shape for the graph serving tables and derived edges
Finding: A5, P2, confidence 7/10, design line 170 + 154, reviewer: Claude
Plan baseline: original proposal, line 170: "materialized entity/edge tables refreshed incrementally for changed source records only"; line 154: derived edge (shared legal rep) lists every contributing record.
Runtime evidence: Postgres `REFRESH MATERIALIZED VIEW [CONCURRENTLY]` recomputes the whole view (no incremental refresh built in). Rep-at-signing max degree 2,329 contracts, so pairwise company↔company edges through one rep grow quadratically.
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| R8 serving tables | "materialized ... refreshed incrementally" (mechanism unspecified) | plain tables `entity` and `edge` (direct edges only: contract→contractor, contract→agency, contract→rep_at_signing, company→current_rep, contract→ordenador, contract→supervisor, group→member), upserted by ingest in the same transaction as the statements of each changed source record | same plain direct-edge tables as A |
| Derived "shared rep / other contractors" edges | stored? unspecified | computed at request time as the 2-hop expand (bounded by R3 caps); evidence drawer = the source records of both direct edges | precomputed pairwise table `derived_edge(company_a, company_b, via_rep)` rebuilt for changed reps |
| R3 expand caps | approved | unchanged | unchanged |
| Work | n/a | baseline | + derived table, rebuild job, fan-out tests (human +2 days / CC +1 h) |

Question D12:
D12 — Compute "shared legal rep" links on the fly, or precompute them?
Project/branch/task: follow-the-money on master, M1 data serving (design lines 154, 170).
ELI10: The design says the graph is served from "materialized tables refreshed incrementally", but Postgres materialized views can only be rebuilt whole, and it doesn't say whether company-to-company "shared legal rep" links are stored. Storing them as pairs explodes for busy reps (2,329 contracts on one rep means millions of pairs). The simpler shape stores only direct facts (contract → contractor, company → rep, ...) in normal tables that ingest updates row by row, and computes "other companies with this rep" when someone clicks, which is a two-join query with the 50-node cap you approved.
Stakes if we pick wrong: Precompute and hub reps create millions of rows that must be rebuilt on every change; compute on the fly and expand latency depends on good indexes.
Recommendation: A because it is the smaller system, stays consistent within one transaction, and the approved caps already bound the query.
Completeness: A=9/10, B=8/10
Pros / cons:
A) Direct edges in plain tables; derived links at query time (recommended)
  ✅ No materialized-view rebuilds; ingest updates edges in the same transaction as statements
  ✅ No quadratic pair table for hub reps; evidence drawer reads the two real source records
  ❌ Expand latency depends on (entity_id, kind) indexes; must be measured at national scale
B) Precompute pairwise derived edges
  ✅ Expand is a single indexed lookup
  ✅ Derived links can be counted and ranked ahead of time
  ❌ Quadratic rows for hub reps, extra rebuild job and consistency lag (human +2 days / CC +1 h)
Net: a join at click time versus a quadratic table with its own rebuild pipeline.
Header: Derived edges
Options:
A) Direct edges + query time (recommended)
Plain tables entity and edge hold direct edges only (contract→contractor, contract→agency, contract→rep_at_signing, company→current_rep, contract→ordenador, contract→supervisor, group→member), upserted by ingest in the same transaction as each changed source record's statements. "Shared rep / other contractors" is computed at request time as the 2-hop expand under the approved caps; the evidence drawer lists the source records of both direct edges. No materialized views.
B) Precompute pairwise edges
Same plain direct-edge tables, plus a precomputed derived_edge(company_a, company_b, via_rep) table rebuilt for reps whose edges changed; expand reads it directly. Approved caps unchanged. human +2 days / CC +1 h.

State: approved
Actual answer: A) Direct edges + query time (recommended), D12 answer 2026-09-25
Accepted scope: Plain tables entity and edge hold direct edges only (contract→contractor, contract→agency, contract→rep_at_signing, company→current_rep, contract→ordenador, contract→supervisor, group→member), upserted by ingest in the same transaction as each changed source record's statements. "Shared rep / other contractors" is computed at request time as the 2-hop expand under the approved caps; the evidence drawer lists the source records of both direct edges. No materialized views.
History: none

Section 1 dispositions: A1 → R3 approved (A). A2 → R4 approved (A). A3 → R5 approved (A). A4 → R6 approved (A), R7 approved (A). A5 → R8 approved (A). Security: masking/ULID/rate-limit design reviewed, no new issue (search-by-cédula exposes nothing SECOP does not already publish). Distribution: GitHub Actions → staging, manual prod (design lines 236-238), no change.

### M1 data flow (as of Section 1; superseded by the final diagram in the review body after R16 and R22)

```
 datos.gov.co (Socrata, CC BY-SA 4.0)
 ┌───────────────┐ ┌──────────────┐ ┌─────────────────┐
 │ SECOP II      │ │ Grupos Prov. │ │ RUES (reachable │
 │ jbjy-vk9h     │ │ ceth-n4bn    │ │ IDs only)       │
 └──────┬────────┘ └──────┬───────┘ └────────┬────────┘
        │ keyset page by :id, $where :updated_at > cursor
        ▼                 ▼                  ▼
 ┌──────────────────────────────────────────────────────┐
 │ ingest (backend/<pkg>/ingest)                        │
 │  1 map row → ~20 props                               │
 │  2 content hash; unchanged → skip            (R7)    │
 │  3 ids.normalize → ids.validate:                     │
 │     placeholder blocklist (F4) → NIT mod-11 →        │
 │     doc-type rule → suspect rule (R5 + name cutoff)  │
 │  4 one txn: statements + entity/edge upsert  (R8)    │
 │     suspect → review queue, edge hidden      (R4)    │
 │  5 RUES reachability backfill by NIT batches         │
 │ weekly: :id sweep → retracted_at             (R6)    │
 └──────────────────────┬───────────────────────────────┘
                        ▼
 ┌──────────────────────────────────────────────────────┐
 │ Postgres: statement (partitioned by dataset),        │
 │ judgment, redirect, entity, edge, review_queue,      │
 │ suppression, correction_ticket; pg_trgm on names     │
 └──────────────────────┬───────────────────────────────┘
                        ▼
 ┌──────────────────────────────────────────────────────┐
 │ api (FastAPI): /search /entity/:id                   │
 │  /entity/:id/expand?kind=&cursor=  top50 + aggregates│
 │  /edge/:id/evidence  (+ license/attribution, R2)     │
 │  every response passes the masking serializer        │
 └──────────────────────┬───────────────────────────────┘
                        ▼
      apps/web: search → entity → Cytoscape graph →
      evidence drawer → PNG (BY-SA notice) → corrección
```

M1 expand for "other contractors" (R1 + R3 + R8):

```
 company C ──rep_at_signing(contract k, fecha_firma)──► person P
    │                                                    ▲
    └──current_rep ("vigente a <corte>", RUES)───────────┘
 P ◄── rep_at_signing / current_rep ── companies {C1..Cn}
      order: contract value desc, fecha desc, id
      n ≤ 50 shown; rest → aggregates by agency (or year)
      suspect edges excluded; node shows "vínculo en revisión"
```

## Section 2: Code quality review

Findings:
1. **Q1 [P2] (confidence 6/10, medium: verify nomenklatura internals) design line 82-84** — "Identity layer (nomenklatura-style)" with fields `origin, reasons[], grade, author, timestamp, evidence_refs[]`, ULID cluster IDs, redirect table, "largest fragment keeps the ID" on splits. nomenklatura's `Resolver` stores judgments with its own table and canonical-ID scheme; it has no origin/reasons/evidence fields and no split rule matching the design. Reuse vs own tables is undecided.
2. **Q2 [P2] (confidence 9/10) design line 75** — "SECOP signing date → 'rep at signing'". `fecha_de_firma` is NULL on 426,226 contracts (7%). No fallback defined.
3. **Q3 [P1] (confidence 8/10) design line 80** — suspect when names attached to an ID disagree, "similarity below the weak threshold". The only weak threshold is 0.95 (line 139), stricter than strong (0.92). With D8 hiding suspect edges, ordinary typos and abbreviations across SECOP records would hide real exact edges (office-hours R3-3).
4. **Q4 [P2] (confidence 8/10) design line 80** — visible "hub" flag for prolific IDs has no criterion or wording (office-hours R3-5). The approved R3 aggregates already show volume ("1.240 contratos con Alcaldía X").
5. **Q5 [factual correction] (confidence 9/10) design line 106, 198** — Roles table says "hub-threshold reviews"; M2 exit says "reviewed hub report". Rule 4 now sends only suspect IDs to review. Rename to "suspect-ID reviews" / "reviewed suspect-ID report" (office-hours R3-4). No behavior change, no question.
6. **Q6 [carried requirement] (confidence 8/10) design line 110-114** — masking must hold in "UI, API, evidence drawer, statement values, image downloads, and URLs". Required implementation of the approved rule: one `MaskedId` value type in `<pkg>/ids`, raw IDs only inside ingest/resolver, API response models accept only `MaskedId` for person IDs, PNG rendering reads API output only. No question.

### R9: Own identity tables vs nomenklatura Resolver
Finding: Q1, P2, confidence 6/10 (medium), design line 82-84, reviewer: Claude
Plan baseline: original proposal: "Identity layer (nomenklatura-style)"; decision log 2026-09-25 "FtM statement model + nomenklatura-style identity judgments".
Runtime evidence: nomenklatura `Resolver` + `SQLStore` exist on Postgres/psycopg3 ([repo](https://github.com/opensanctions/nomenklatura)). Its judgment schema and canonical-ID generation were not inspected in code; treat fit as unverified.
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| R9 judgment storage | "nomenklatura-style" (unspecified) | own `judgment` table with the design's fields (pair, verdict positive/negative/unsure, origin, reasons[], grade, author, timestamp, evidence_refs[]) + own `cluster` (ULID) and `redirect` tables with the design's merge/split rules | nomenklatura `Resolver` table for the judgment graph + side table for origin/reasons/grade/evidence_refs + mapping from nomenklatura canonical IDs to public ULIDs |
| FtM library | `followthemoney` for schema/statements | same | same |
| M1 need | exact-ID + reviewer judgments only | covered | covered |
| Work | n/a | human ~2 days / CC ~1 h (tables + union-find + tests) | human ~2 days / CC ~1 h (integration + mapping layer + tests); upstream schema changes to track |

Question D13:
D13 — Own identity tables, or build on nomenklatura's Resolver?
Project/branch/task: follow-the-money on master, M1 identity layer (design lines 82-84).
ELI10: The design says identity decisions ("these two records are the same person") are "nomenklatura-style". nomenklatura is OpenSanctions' library that already stores such decisions. But the design adds things nomenklatura does not model: who made the decision and how (exact ID vs reviewer), listed reasons, evidence links, public opaque ULIDs, and a specific split rule. Using nomenklatura means a side table plus an ID-mapping layer; writing our own is a few tables that match the design exactly. I have not read nomenklatura's code in this review, so its fit is medium-confidence.
Stakes if we pick wrong: Adopt nomenklatura and the glue layer fights its ID scheme on every merge/split; roll our own and we maintain a small union-find that a library already has.
Recommendation: A because M1 only needs exact-ID and reviewer judgments, and the design's ULID, redirect and split rules are easier to guarantee in tables we control.
Completeness: A=8/10, B=8/10
Pros / cons:
A) Own tables matching the design (recommended)
  ✅ Every design field and the ULID/redirect/split rules are first-class columns, easy to test
  ✅ No mapping layer between library canonical IDs and public URLs
  ❌ We own a small union-find and its tests instead of reusing a maintained one
B) nomenklatura Resolver + side table + ID mapping
  ✅ Reuses a maintained judgment graph already used by OpenSanctions
  ✅ Easier interop if you later run nomenklatura matchers (M3)
  ❌ Glue code maps its canonical IDs to ULIDs and bolts on reasons/evidence; upstream changes must be tracked
Net: a few owned tables versus a library plus glue that must bend to the design's ID rules.
Header: Identity
Options:
A) Own tables (recommended)
Own judgment table (pair, verdict positive/negative/unsure, origin, reasons[], grade, author, timestamp, evidence_refs[]) plus cluster (ULID) and redirect tables implementing the design's merge/split rules. followthemoney library still used for schemas/statements; nomenklatura not an M1 dependency (revisit in M3 eng review). human ~2 days / CC ~1 h.
B) nomenklatura + side table
nomenklatura Resolver table holds the judgment graph; a side table holds origin/reasons/grade/evidence_refs; a mapping table links nomenklatura canonical IDs to public ULIDs and redirects. followthemoney library used for schemas/statements. human ~2 days / CC ~1 h plus upstream tracking.

State: approved
Actual answer: A) Own tables (recommended), D13 answer 2026-09-25
Accepted scope: Own judgment table (pair, verdict positive/negative/unsure, origin, reasons[], grade, author, timestamp, evidence_refs[]) plus cluster (ULID) and redirect tables implementing the design's merge/split rules. followthemoney library still used for schemas/statements; nomenklatura not an M1 dependency (revisit in M3 eng review).
History: none

### R10: Rep-at-signing edges when `fecha_de_firma` is missing
Finding: Q2, P2, confidence 9/10, design line 75, reviewer: Claude (probe)
Plan baseline: original proposal, line 75: "SECOP signing date → 'rep at signing'"; no fallback.
Runtime evidence: `fecha_de_firma IS NULL` on 426,226 of 6,070,870 contracts (7%).
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| R10 edge `valid_from` when signing date missing | undefined | NULL; edge and drawer say "fecha de firma no reportada"; sort/aggregate uses `fecha_de_inicio_del_contrato`, then `ultima_actualizacion`, for ordering only | `fecha_de_inicio_del_contrato` as `valid_from`, edge labeled "fecha aproximada (inicio del contrato)" |
| R3 ordering rule | value desc, signing date desc, id | signing date slot falls back to start date, then last update | same fallback |
| Work | n/a | human ~2 h / CC ~10 min + tests | human ~2 h / CC ~10 min + tests |

Question D14:
D14 — What date does a "rep at signing" edge carry when SECOP has no signing date?
Project/branch/task: follow-the-money on master, M1 time semantics (design line 75).
ELI10: Each "legal rep at signing" edge is dated by the contract's signing date, which is how we avoid implying someone was rep when they weren't. 7% of SECOP contracts (426,226) have no signing date. We can leave the date empty and say so, or substitute the contract start date and label it approximate. Either way we still need some date to sort contracts, so both options use start date for ordering.
Stakes if we pick wrong: Substituted dates can be quoted by a journalist as the signing date; empty dates make timelines patchier.
Recommendation: A because the site's promise is to show exactly what the source says, and an empty date labeled as missing cannot be misquoted.
Completeness: A=9/10, B=8/10
Pros / cons:
A) Leave empty, label "fecha de firma no reportada" (recommended)
  ✅ Never shows a date the source did not publish; nothing to misquote
  ✅ Sorting still works via start date, then last update, internally
  ❌ Timeline-style views later (future scrubber) must handle undated edges
B) Use start date, labeled approximate
  ✅ Every edge has a date for display and future timelines
  ✅ Start date is usually close to the signing date
  ❌ A labeled approximation can still be screenshot and quoted as fact
Net: honest gaps versus complete-looking dates that are sometimes wrong.
Header: Missing date
Options:
A) Empty + labeled (recommended)
Rep-at-signing edges with no fecha_de_firma get valid_from NULL; the edge and evidence drawer say "fecha de firma no reportada". Ordering and aggregation fall back to fecha_de_inicio_del_contrato, then ultima_actualizacion, internally only. human ~2 h / CC ~10 min.
B) Start date, labeled approximate
Rep-at-signing edges with no fecha_de_firma use fecha_de_inicio_del_contrato as valid_from, labeled "fecha aproximada (inicio del contrato)". Ordering uses the same fallback. human ~2 h / CC ~10 min.

State: approved
Actual answer: A) Empty + labeled (recommended), D14 answer 2026-09-25
Accepted scope: Rep-at-signing edges with no fecha_de_firma get valid_from NULL; the edge and evidence drawer say "fecha de firma no reportada". Ordering and aggregation fall back to fecha_de_inicio_del_contrato, then ultima_actualizacion, internally only.
History: none

### R11: Numeric cutoff for "names attached to one ID disagree"
Finding: Q3, P1, confidence 8/10, design line 80 + 139, reviewer: Claude (office-hours R3-3 carried)
Plan baseline: original proposal, line 80: "names attached to it disagree across records (similarity below the weak threshold)"; weak threshold = 0.95 (line 139).
Runtime evidence: unknown distribution; SECOP names are free text per contract. Under approved R4, a suspect ID's edges are hidden in M1, so an over-strict cutoff hides real exact edges.
Comparison grid:

| Choice | Current | A | B | C |
|---|---|---|---|---|
| R11 natural-person disagreement test | any pair of attached names < 0.95 | best-aligned surname-component Jaro-Winkler < 0.80 between any two attached names (given names ignored: SECOP abbreviates them) | full-name similarity (design function) < 0.92 | < 0.95 (as written) |
| Legal-entity disagreement test | same 0.95 | token-set similarity < 0.80 after stripping legal forms (S.A.S., LTDA, S.A., E.S.P., etc.) and punctuation | < 0.92 same normalization | < 0.95 |
| Calibration | "validated on the M1 cohort" (for lead grades) | starting values; before widening, hand-label 200 flagged IDs from the cohort, tune, and record the final numbers in the design | same calibration step | none |
| Work | n/a | human ~1 day / CC ~40 min + labeling time | same | none |

Question D15:
D15 — What counts as "names disagree" for one ID?
Project/branch/task: follow-the-money on master, M1 ID validation rule 4 (design line 80).
ELI10: Rule 4 marks an ID suspect when the names attached to it across records disagree, using "the weak threshold", which is 0.95 similarity. That is stricter than the design's own "strong match" bar (0.92), so a typo, a dropped middle name or "S.A.S" vs "SAS" would flag a real person or company. Since you chose (D8) to hide suspect edges in M1, a strict cutoff silently removes real contracts from the graph. A separate, looser test that only fires on real disagreement (different surnames, different company names) fixes that.
Stakes if we pick wrong: Too strict and thousands of real exact edges disappear into the review queue; too loose and a mistyped cédula links two different people.
Recommendation: A because surnames and legal-form-stripped company names are the stable parts of these records, and the 200-ID labeling pass sets the real number before national launch.
Completeness: A=9/10, B=7/10, C=3/10
Pros / cons:
A) Surname / company-name test at < 0.80, tuned on cohort (recommended)
  ✅ Ignores abbreviations and legal-form noise; fires on genuinely different people or firms
  ✅ Calibrated on 200 hand-labeled cohort IDs before widening to national data
  ❌ Needs a labeling session (~2 h of your time) plus test fixtures
B) Full-name similarity < 0.92
  ✅ Reuses the design's single similarity function, no second test
  ✅ Same calibration step as A
  ❌ Given-name abbreviations and legal-form suffixes still trigger many false flags
C) Keep < 0.95 as written
  ✅ No change to the approved text
  ✅ Most conservative about any name variation
  ❌ Typos alone hide real edges; review queue likely unmanageable for one moderator
Net: a purpose-built test for data-entry errors versus reusing a lead-matching threshold that was never meant for this.
Header: Name cutoff
Options:
A) Surname/company < 0.80 (recommended)
Natural persons: suspect when the best-aligned surname-component Jaro-Winkler between any two attached names is < 0.80 (given names ignored). Legal entities: suspect when token-set similarity is < 0.80 after stripping legal forms (S.A.S., LTDA, S.A., E.S.P.) and punctuation. Starting values; before widening, hand-label 200 flagged cohort IDs, tune, and record final numbers in the design. human ~1 day / CC ~40 min + labeling.
B) Full-name < 0.92
Suspect when the design's full-name similarity between any two attached names is < 0.92, with the same legal-form normalization for companies and the same 200-ID calibration before widening. human ~1 day / CC ~40 min + labeling.
C) Keep < 0.95
Suspect when any two attached names score below 0.95, as written. No calibration step. No work.

State: approved
Actual answer: A) Surname/company < 0.80 (recommended), D15 answer 2026-09-25
Accepted scope: Natural persons: suspect when the best-aligned surname-component Jaro-Winkler between any two attached names is < 0.80 (given names ignored). Legal entities: suspect when token-set similarity is < 0.80 after stripping legal forms (S.A.S., LTDA, S.A., E.S.P.) and punctuation. Starting values; before widening, hand-label 200 flagged cohort IDs, tune, and record final numbers in the design.
History: none

### R12: The visible "hub" flag for prolific IDs
Finding: Q4, P2, confidence 8/10, design line 80, reviewer: Claude (office-hours R3-5 carried)
Plan baseline: original proposal, line 80: prolific individuals "keep their exact edges and get a visible 'hub' flag"; no criterion or wording defined.
Runtime evidence: degrees up to 29,697 contracts (ordenador). Approved R3 already shows overflow counts as aggregate nodes.
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| R12 hub flag | visible flag, undefined | no flag in M1; volume shown as plain counts on the entity card ("N contratos como ordenador del gasto", "representante legal de N empresas") and in R3 aggregates; design line 80 "hub" wording removed | flag "Alta actividad contractual" when rep of ≥ 10 companies or party (any role) to ≥ 500 contracts, computed nightly, shown with the counts; wording joins M1 legal review |
| R3 aggregates | approved | unchanged | unchanged |
| Work | n/a | human ~2 h / CC ~10 min (counts only) | human ~1 day / CC ~30 min + legal wording |

Question D16:
D16 — Keep a visible "hub" label on prolific people, or just show counts?
Project/branch/task: follow-the-money on master, M1 entity card (design line 80).
ELI10: The design says prolific IDs get a visible "hub" flag, but never says who qualifies or what the label says. A label on a named person ("hub", "alta actividad") is an editorial characterization a lawyer will read closely. The expand aggregates you approved (D7) and plain counts on the entity card ("ordenador del gasto en 29.697 contratos") already show the same fact without a characterization.
Stakes if we pick wrong: A label with arbitrary thresholds invites a defamation-flavored complaint; no label means journalists read the number themselves.
Recommendation: A because a sourced count says everything the flag would, with nothing to defend.
Completeness: A=8/10, B=8/10
Pros / cons:
A) No flag; plain counts on card and aggregates (recommended)
  ✅ Counts are pure source facts; nothing for the legal review to reword
  ✅ No thresholds to justify, tune or explain to a data subject
  ❌ Prolific nodes are less visually distinct in the graph at a glance
B) Define flag at ≥ 10 companies or ≥ 500 contracts
  ✅ Hubs stand out instantly on the graph
  ✅ Threshold is explicit and testable
  ❌ Characterizing label on named people; arbitrary cutoffs to defend in legal review
Net: neutral counts versus a label that needs a legal defense.
Header: Hub flag
Options:
A) Counts only (recommended)
No hub flag in M1. The entity card shows plain source counts ("N contratos como ordenador del gasto", "representante legal de N empresas"), and R3 aggregates show overflow volume. Remove the "hub" wording from design line 80. human ~2 h / CC ~10 min.
B) Flag at 10 companies / 500 contracts
Show "Alta actividad contractual" with the counts when an ID is legal rep of ≥ 10 companies or party (any role) to ≥ 500 contracts, computed nightly. Wording joins the M1 legal review. human ~1 day / CC ~30 min.

State: approved
Actual answer: A) Counts only (recommended), D16 answer 2026-09-25
Accepted scope: No hub flag in M1. The entity card shows plain source counts ("N contratos como ordenador del gasto", "representante legal de N empresas"), and R3 aggregates show overflow volume. Remove the "hub" wording from design line 80.
History: none

Section 2 dispositions: Q1 → R9 approved (A). Q2 → R10 approved (A). Q3 → R11 approved (A). Q4 → R12 approved (A). Q5 factual correction recorded (rename to suspect-ID reviews/report). Q6 carried as required implementation of the approved masking rule. Shared-code rubric: no first-party code exists; the one shared unit is `<pkg>/ids` (normalize, validate, mask), justified by the D4 structure answer, not by duplicated code.

## Section 3: Test review

Framework: none detected (repo holds only docs/openspec; no pyproject/package.json; CLAUDE.md has no Testing section). Greenfield: no existing behavior, so the regression rule has nothing to protect yet. No LLM calls in M1, so no evals.

### Planned codepaths and required tests (all GAP until built)

```
CODE PATHS                                                   USER FLOWS
[+] <pkg>/ids                                                [+] Sunday flow [→E2E]
  ├── normalize(raw, field)                                    ├── [GAP] search NIT → entity → expand rep
  │   ├── [GAP] dots/hyphen/space variants → same key            │        → other contractor → drawer → SECOP link
  │   └── [GAP] NIT with/without check digit                  ├── [GAP] search full cédula → masked result
  ├── is_placeholder(value)  (F4 blocklist)                    └── [GAP] shared URL reopens same graph state
  │   └── [GAP] Sin Descripcion / No definido / 1 / 000000 / "-"
  ├── nit_check_digit_valid(nit)  (DIAN mod-11)              [+] Expand UX
  │   └── [GAP] valid, invalid, 9 vs 10 digits                 ├── [GAP] hub node → top 50 + aggregates (R3)
  ├── doc_type_join_allowed(a, b)                              ├── [GAP] drill aggregate, cursor paging
  │   ├── [GAP] legal-entity NIT never joins cédula            ├── [GAP] node with suspect edge → "vínculo en revisión" (R4)
  │   └── [GAP] natural-person NIT joins cédula iff signals    └── [GAP] current-rep vs at-signing edge labels (R1)
  ├── suspect_reason(id, records)                            [+] Evidence drawer
  │   ├── [GAP] self-contracting agency NIT (R5)               ├── [GAP] derived edge lists both source records (R8)
  │   ├── [GAP] agency NIT in person field (R5)                ├── [GAP] "fecha de firma no reportada" (R10)
  │   ├── [GAP] public entity as contractor → NOT suspect      └── [GAP] license + attribution shown (R2)
  │   └── [GAP] name disagreement < 0.80 surname / company (R11)
  └── mask(id, person_type) → MaskedId                       [+] Correction flow
      ├── [GAP] natural person / unknown → ***1234              ├── [GAP] submit → ticket ID → email
      └── [GAP] legal entity → full NIT                         ├── [GAP] suppression → placeholder node, search empty
                                                                └── [GAP] verified vs unverified annotation outcomes
[+] <pkg>/ingest
  ├── pull(dataset, cursor)  keyset by :id, :updated_at      [+] PNG download
  │   ├── [GAP] resume after failure mid-page                   ├── [GAP] attribution + BY-SA notice embedded (R2)
  │   └── [GAP] HTTP 429/5xx retry with backoff                 └── [GAP] no full natural-person ID in pixels/metadata
  ├── map_row → props; content_hash (R7)
  │   ├── [GAP] unchanged hash → zero writes                  [+] Error states
  │   └── [GAP] canonicalization: key order, whitespace         ├── [GAP] unknown/redirected/split entity URL
  ├── write_txn: statements + entity/edge upsert (R8)           ├── [GAP] API 5xx → retry message, no blank graph
  │   └── [GAP] crash mid-txn leaves no partial edges          └── [GAP] search with zero results / 1 char
  ├── rues_backfill(reachable ids) batches
  │   ├── [GAP] newly reachable fetched; unreachable deleted
  │   └── [GAP] suppressed person never re-ingested
  └── sweep(dataset)  (R6)
      ├── [GAP] missing :id → retracted_at, hidden publicly
      └── [GAP] retracted still in Ley 1581 access export
[+] <pkg>/resolver
  ├── exact_id_judgments(); union-find clusters (R9)
  │   ├── [GAP] merge keeps one ULID + redirect
  │   └── [GAP] split: largest keeps ULID, old URL chooser
  └── review CLI: resolve suspect → exact | rejected
[+] <pkg>/api
  ├── /search  pg_trgm + exact ID; rate limit per IP
  │   └── [GAP] limit exceeded → 429
  ├── /entity/:id/expand?kind=&cursor=  (R1, R3, R4, R8)
  │   ├── [GAP] ordering value desc, date desc (fallback R10), id
  │   └── [GAP] suspect edges excluded
  └── /edge/:id/evidence  (R2 license fields)
[+] Privacy suite (design line 114, approved)
  ├── [GAP] regex over every fixture response, URL, entity ID, PNG: no full natural-person ID in any format
  └── [GAP] property test: ULID generator output independent of input IDs

COVERAGE: 0/58 paths tested (greenfield)  |  Code paths: 0/40  |  User flows: 0/18
QUALITY: none yet  |  GAPS: 58 (4 E2E, 0 eval)
Legend: ★★★ behavior + edge + error | ★★ happy path | ★ smoke | [→E2E] integration test
```

Required tests carried without a new question (proof of approved behavior): every [GAP] row above tied to R1-R12 or to the design's approved rules (masking, privacy suite, golden-case fixture, correction flow). Framework and E2E depth are new choices, asked below.

### R13: Test framework for backend and web
Finding: Test framework detection: none; proposing one. P2, confidence 9/10, reviewer: Claude
Plan baseline: original proposal: design names the stack (FastAPI, React+TS+Vite) but no test tooling.
Runtime evidence: no test infrastructure in repo (probe above).
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| R13 backend unit + integration | none | pytest; integration tests run against real Postgres 16 with pg_trgm (docker-compose service locally, service container in GitHub Actions); Hypothesis for the ID/ULID property tests | pytest with SQLite for integration; Hypothesis for property tests |
| Web unit tests | none | Vitest + Testing Library | Vitest + Testing Library |
| E2E | none | not decided here (R14) | not decided here (R14) |
| Work | n/a | human ~1 day / CC ~30 min setup | human ~0.5 day / CC ~20 min setup |

Question D17:
D17 — Which test stack, and do integration tests hit real Postgres?
Project/branch/task: follow-the-money on master, M1 (greenfield: no tests or test tooling exist yet).
ELI10: The backend leans on Postgres-specific features: pg_trgm name search, partitioned statement tables, transactional upserts. Tests against SQLite would pass while production breaks on exactly those features. The standard picks are pytest for Python and Vitest for a Vite app; the real question is whether backend integration tests run against a real Postgres container.
Stakes if we pick wrong: SQLite tests give false confidence on search, partitioning and the ingest transaction, the parts most likely to break at national scale.
Recommendation: A because the privacy and ingest guarantees live in Postgres behavior, so they must be tested on Postgres.
Completeness: A=9/10, B=5/10
Pros / cons:
A) pytest + real Postgres + Hypothesis; Vitest (recommended)
  ✅ Tests exercise pg_trgm, partitions and upsert transactions exactly as production does
  ✅ Same docker-compose service runs locally and as a GitHub Actions service container
  ❌ Slower test runs (container start ~5 s) and Docker needed to run the suite
B) pytest + SQLite + Hypothesis; Vitest
  ✅ Fast, dependency-free test runs
  ✅ Simplest CI setup
  ❌ Cannot test pg_trgm, partitioning or Postgres upsert semantics; false passes likely
Net: a few seconds of container start versus tests that skip the database features you depend on.
Header: Test stack
Options:
A) pytest + real Postgres (recommended)
Backend: pytest, integration tests against real Postgres 16 with pg_trgm (docker-compose locally, service container in GitHub Actions), Hypothesis for ID and ULID property tests. Web: Vitest + Testing Library. E2E decided separately (R14). human ~1 day / CC ~30 min.
B) pytest + SQLite
Backend: pytest with SQLite for integration tests, Hypothesis for property tests. Web: Vitest + Testing Library. E2E decided separately (R14). human ~0.5 day / CC ~20 min.

State: approved
Actual answer: A) pytest + real Postgres (recommended), D17 answer 2026-09-25
Accepted scope: Backend: pytest, integration tests against real Postgres 17 with pg_trgm (docker-compose locally, service container in GitHub Actions), Hypothesis for ID and ULID property tests. Web: Vitest + Testing Library. E2E decided separately (R14).
History: D17 answer approved "Postgres 16"; version changed to 17 on 2026-09-25 as necessary implementation of R23 (Supabase runs Postgres 17; test/prod parity). No other change.

### R14: Automated end-to-end tests
Finding: 4 flows marked [→E2E] in the Section 3 diagram (Sunday flow, masked cédula search, shared URL, PNG privacy). P2, confidence 8/10, reviewer: Claude
Plan baseline: original proposal: Sunday test is a manual launch gate (design line 196, 227); no automated E2E.
Runtime evidence: none (greenfield).
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| R14 automated E2E | none | Playwright in GitHub Actions against the docker-compose stack seeded with the golden-case fixture: (1) NIT search → entity → expand rep → other contractor → drawer → SECOP link, (2) full-cédula search shows only masked ID, (3) shared URL restores graph state, (4) downloaded PNG carries attribution/BY-SA notice and no full natural-person ID | none; manual Sunday test only |
| Manual Sunday test + legal gate | approved launch gate | unchanged | unchanged |
| R13 stack | approved | unchanged | unchanged |
| Work | n/a | human ~2 days / CC ~1 h | none |

Question D18:
D18 — Automate the golden-case journey with Playwright in CI?
Project/branch/task: follow-the-money on master, M1 (design lines 196, 227: Sunday test is a manual launch gate).
ELI10: The M1 promise is a two-minute path: search a company, open its legal rep, see the other companies, open the SECOP record. It crosses the web app, the API, masking and Postgres. Unit tests cover each piece, but only an end-to-end test proves the whole path still works after each change. The design relies on one manual Sunday test right before launch. Four Playwright tests seeded with your hand-traced golden case would run on every push.
Stakes if we pick wrong: Without E2E, a masking or expand regression can ship to staging (or production) and only surface when a journalist hits it.
Recommendation: A because the golden fixture already exists by design, and the privacy promise deserves a check on every push, not once before launch.
Completeness: A=9/10, B=5/10
Pros / cons:
A) Playwright E2E on 4 flows in CI (recommended)
  ✅ The Sunday path, masked search, shared URLs and PNG privacy are checked on every push
  ✅ Reuses the golden-case fixture the design already requires
  ❌ Slower CI (~3-5 min for the compose stack) and some browser-test flakiness to manage
B) Manual Sunday test only
  ✅ No E2E infrastructure to build or maintain
  ✅ Fastest CI
  ❌ Cross-layer regressions (masking in PNG, broken expand) are caught late or by users
Net: a few minutes of CI per push versus trusting one manual run before launch.
Header: E2E
Options:
A) Playwright E2E (recommended)
Playwright in GitHub Actions against the docker-compose stack seeded with the golden-case fixture, covering: NIT search → entity → expand rep → other contractor → drawer → SECOP link; full-cédula search shows only the masked ID; shared URL restores graph state; downloaded PNG carries attribution/BY-SA notice and no full natural-person ID. Manual Sunday test and legal gate unchanged. human ~2 days / CC ~1 h.
B) Manual only
No automated E2E. The manual Sunday test and legal review remain the only end-to-end checks. No work.

State: approved
Actual answer: A) Playwright E2E (recommended), D18 answer 2026-09-25
Accepted scope: Playwright in GitHub Actions against the docker-compose stack seeded with the golden-case fixture, covering: NIT search → entity → expand rep → other contractor → drawer → SECOP link; full-cédula search shows only the masked ID; shared URL restores graph state; downloaded PNG carries attribution/BY-SA notice and no full natural-person ID. Manual Sunday test and legal gate unchanged.
History: none

Section 3 dispositions: 58 planned paths, 58 GAPs (greenfield), 4 E2E. Required tests for R1-R12 and the approved privacy/golden-case rules carried. R13 approved (A). R14 approved (A). Regression rule: nothing existing to protect. Evals: none (no LLM). Test Plan Artifact saved: `~/.gstack/projects/follow-the-money/tru-master-eng-review-test-plan-20260925-054309.md`.

## Section 4: Performance review

Findings:
1. **P1 [P2] (confidence 8/10) design line 98, 227** — the only latency target is the M3 overlay ("p95 ≤ 800 ms"). M1 search and expand have no budget, and the Sunday test's "within 2 minutes" depends on them. R8 moved derived edges to query time, so expand latency at national scale is now the main performance risk.
2. **P2 [carried implementation] (confidence 8/10)** — R3 ordering needs a covering index `edge(src_entity, kind, value DESC, sort_date DESC, id)`; exact-ID search needs a btree on normalized ID; name search a GIN `gin_trgm_ops` index. Required to implement approved R3/R8 and the design's pg_trgm search. No question.
3. **P3 [P3] (confidence 7/10) design line 170** — "statements (partitioned by dataset)" leaves SECOP as one ~120M-row partition, so partitioning buys little for the biggest table. With R8, statements serve evidence/audit reads by `source_record_id` only, so a btree on `(dataset, source_record_id)` is what matters. Informational, no change proposed.
4. **P4 [P3] (confidence 7/10) design line 168 + learning colombia-open-data-id-keys** — bulk national pulls run unauthenticated ("queryable via Socrata API without auth"). Socrata throttles anonymous clients; an app token raises limits. Raised as a TODO candidate, not a design change.

### R15: M1 latency budget and pre-launch load check
Finding: P1, P2, confidence 8/10, design line 98 + 227, reviewer: Claude
Plan baseline: original proposal: no M1 latency target; M3 overlay p95 ≤ 800 ms.
Runtime evidence: none yet (no code). Max observed degree 29,697 contracts.
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| R15 M1 latency budget | none | server-side p95 ≤ 300 ms for `/search` and ≤ 500 ms for `/entity/:id/expand` (incl. aggregates) on national data | none (as designed) |
| Pre-launch check | Sunday test only | load script (k6 or Locust) replays 1,000 expands sampled across degree buckets incl. top-100 hubs against the widened national DB on staging; launch gate requires the budget | none |
| Work | n/a | human ~1 day / CC ~30 min + one staging run | none |

Question D19:
D19 — Set an M1 latency budget and check it on national data before launch?
Project/branch/task: follow-the-money on master, M1 performance (design line 98 has a budget only for M3).
ELI10: M1 has no speed target. You chose (D12) to compute "other companies with this rep" at click time, and some people have tens of thousands of contracts, so expand speed on the full national database is the main performance risk. A budget (search under 300 ms, expand under 500 ms, 95% of the time) plus one load test on staging with the full data turns "it felt fast on the cohort" into a measured launch gate.
Stakes if we pick wrong: Without it, slow expands on hub nodes surface on launch day, when journalists and press are first clicking around.
Recommendation: A because the risk is concentrated in a few hub nodes that a sampled load test finds cheaply.
Completeness: A=9/10, B=5/10
Pros / cons:
A) p95 300/500 ms + staging load test as launch gate (recommended)
  ✅ Measured, not guessed: hub-node expands are tested on the real national data
  ✅ Gives a clear number to tune indexes against before widening goes public
  ❌ One more launch-gate item and a load script to maintain (human ~1 day / CC ~30 min)
B) No budget
  ✅ No extra gate before launch
  ✅ No load-test tooling
  ❌ Hub-node slowness is discovered by users, on the busiest day
Net: one staging load run versus finding slow hubs in public.
Header: Latency
Options:
A) Budget + load test (recommended)
Server-side p95 ≤ 300 ms for /search and ≤ 500 ms for /entity/:id/expand (including aggregates) on national data. Before launch, a load script (k6 or Locust) replays 1,000 expands sampled across degree buckets including the top-100 hubs against the widened national DB on staging; meeting the budget joins the M1 launch gate. human ~1 day / CC ~30 min.
B) No budget
No M1 latency target or load test; the manual Sunday test remains the only speed check. No work.

State: approved
Actual answer: A) Budget + load test (recommended), D19 answer 2026-09-25
Accepted scope: Server-side p95 ≤ 300 ms for /search and ≤ 500 ms for /entity/:id/expand (including aggregates) on national data. Before launch, a load script (k6 or Locust) replays 1,000 expands sampled across degree buckets including the top-100 hubs against the widened national DB on staging; meeting the budget joins the M1 launch gate.
History: none

Section 4 dispositions: P1 → R15 approved (A). P2 carried as required implementation of R3/R8. P3 informational. P4 → TODO candidate.

## Outside Voice (Codex, completed)

Provider: codex CLI, read-only, model `gpt-6-astra` (reasoning high). Plan body truncated to 30KB (design doc Reviewer Concerns appendix cut); approved decisions R1-R15 and probe evidence passed in full.

```
- **The usefulness test is selected to succeed.** A contractor already known to share a representative tests navigation, not discovery or typical coverage. With legal-rep IDs missing on 67% of NIT-contractor contracts, that distinction matters. RUES adds current relationships, but cannot establish historical ones. Test representative investigation tasks—including dead ends—before making national ingestion a prerequisite for launch.

- **“Representative at signing” remains an unsupported temporal assumption.** A signing date on a contract row does not establish that its representative fields preserve the person recorded at signing. The supplied evidence does not demonstrate that guarantee. Verify the source’s field semantics and amendment behavior before attaching the signing date to that relationship.

- **Refresh rules do not yet define a reliable lifecycle.** A weekly ID sweep needs a completeness condition before it can declare records missing. Interrupted scans, full replacements, and disappearing/reappearing rows need explicit handling. In particular, an unchanged record returning after retraction must become visible again despite R7’s “unchanged = no writes.” Hash comparison alone cannot govern publication state.

- **Identity corrections have no defined propagation mechanism.** R8 updates direct edges during ingestion, while the admin CLI can change identity judgments independently. Rejecting a mistaken identity, splitting a cluster, or suppressing a person must immediately affect existing edges, search results, counts, and redirects—even when source hashes remain unchanged. The plan needs either query-time identity resolution or explicit invalidation and rebuilding.

- **Suppression preserves an identification route.** A nameless representative node connecting two named companies can still identify the person through those companies’ public records. Retaining shared-representative edges therefore preserves the association the suppression ostensibly limits. Define whether suppression merely removes labels or actually removes public person-level linkability; the current rendering promises more protection than it provides.

- **A hash and a live source link are insufficient evidence history.** Keeping missing rows for audit does not address overwritten rows. If a source changes its representative, name, or identifier, an earlier graph claim or editorial decision needs the source version that supported it. Specify versioned retention of the used fields, observation dates, and evidence references; a SHA-256 digest cannot reconstruct them.

- **The storage architecture is solving an unapproved future problem.** M1 proposes roughly 130 million statements plus separate entity/edge tables before validating ordinary investigative usefulness. Separating source facts from identity judgments does not require a generic property-level statement engine. Typed source records, provenance, and separate identity tables could satisfy M1 with FtM conversion at the boundary. Justify the statement engine through a concrete M1 requirement before accepting its storage and maintenance cost.

- **National operating feasibility has no gate.** Bytes per statement and API latency do not establish whether the system can finish initial ingestion, survive full replacements, complete weekly sweeps, or recover within an affordable budget. Measure refresh duration, peak storage including WAL and backups, recovery time, and monthly cost before widening. Hosting budget cannot remain an open question while national coverage is mandatory.

Recommendation: Revise M1 before implementation because its success test does not justify national scope, and temporal claims, refresh lifecycle, and correction propagation remain underspecified.
```

Follow-up probe for Codex finding 2 (2026-09-25): for top NIT contractors with rep IDs present (860025674, 900062917, 860063869, 900204272), the rep ID is identical on every contract signed 2016-2026. Of 37,002 NIT contractors with rep IDs, the largest multi-rep ones (860002184, 800214426, 900019737) show a second rep ID only in scattered rows (1-4 contracts in one year or undated), never as a dated handover. Interpretation (confidence 7/10): the SECOP contract row carries the supplier-profile rep (current or last known at extraction), not a snapshot at signing.

Outside findings mapped:
- X1 ← Codex 2 (rep-at-signing semantics). Reopens design line 75 and R10 on new evidence.
- X2 ← Codex 4 (identity corrections propagation).
- X3 ← Codex 3 (sweep completeness, reappearing rows). Necessary implementation of approved R6/R7: a sweep may retract only after a complete scan whose row count is within 1% of the dataset's reported count; a returning row clears `retracted_at` even when its hash is unchanged. Carried, no question.
- X4 ← Codex 6 (versioned evidence history).
- X5 ← Codex 5 (suppression linkability).
- X6 ← Codex 8 (national operating feasibility gate).
- X7 ← Codex 1 (usefulness test selected to succeed).
- X8 ← Codex 7 (statement engine vs typed source records).

### R16: What the SECOP legal-rep edge claims about time
Finding: X1, P1, confidence 7/10, design line 75 + R1 + R10, reviewer: Codex (finding 2) + Claude probe
Plan baseline: design line 75 "rep at signing" dated by `fecha_de_firma`; R1 approved (D5 A) "rep-at-signing (SECOP, dated)"; R10 approved (D14 A) missing-date label "fecha de firma no reportada".
Runtime evidence: rep ID constant across 10 years of signings for top companies; no dated handovers seen (probe above). Not conclusive: field documentation not checked.
Reason for reopening: contradictory evidence to the "at signing" assumption behind line 75, R1 and R10.
Comparison grid:

| Choice | Current | A | B | C | D |
|---|---|---|---|---|---|
| R16 SECOP rep edge meaning/label | "representante legal a la firma", dated by signing date | "representante legal reportado en SECOP para este proveedor" with source and `observed_at`; no signing-date claim; contract node still shows its own signing date | unchanged | unchanged until a bounded check: compare rep in 10 golden-case contract PDFs (pre-2021) against the dataset field, plus Colombia Compra Eficiente field docs; then return to this row | proposal deferred; unchanged |
| R1 traversal (both rep kinds, distinct labels) | approved | kept; labels become "reportado en SECOP" vs "vigente a <corte> (RUES)" | unchanged | unchanged | unchanged |
| R10 missing signing date | approved | still applies to the contract node's date; no longer on the rep edge | unchanged | unchanged | unchanged |
| Work | n/a | human ~2 h / CC ~10 min (labels, drawer copy, tests) | none | ~2 h of your time in the hand trace | none |

Question D20:
D20 — Stop calling the SECOP legal rep the rep "at signing"?
Project/branch/task: follow-the-money on master, M1 time semantics (design line 75; R1, R10).
ELI10: The design treats the legal-rep ID on a SECOP contract as the person who was rep when the contract was signed, and dates the edge that way. Codex questioned it, and I checked: for the busiest companies the rep ID is identical on every contract from 2016 to 2026, and where a second ID shows up it is scattered noise, never a dated change. That looks like SECOP stamping the supplier's profile rep onto every row, not a signing-time snapshot. If so, "rep at signing (2018)" is a claim the source never made.
Stakes if we pick wrong: Keep the label and the site may state that a person represented a company on a date they did not, which is the kind of error a defamation complaint targets.
Recommendation: A because "reported in SECOP" is true under either reading, costs two hours, and can be upgraded later if the hand trace proves the field is a true snapshot.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Apply: relabel as "reportado en SECOP", no signing-date claim (recommended)
  ✅ True whichever way SECOP fills the field; nothing to retract later
  ✅ Two hours of label, drawer and test changes; traversal (R1) unchanged
  ❌ Loses the historical "who was rep then" claim that made the design's time rule attractive
B) Keep "rep at signing" with signing date
  ✅ Matches the approved design and R1/R10 wording
  ✅ Gives journalists a dated claim if the field really is a snapshot
  ❌ Probe evidence says the date likely misstates when the person was rep
C) Investigate first (golden-case PDFs + field docs)
  ✅ Settles the semantics with primary documents before choosing a label
  ✅ Folds into the hand trace you already need to do
  ❌ Label stays undecided; M1 rep edges cannot be finished until you return to this
D) Defer this change
  ✅ No work now; revisit at M1 legal review
  ✅ Keeps current approvals intact
  ❌ Leaves a likely false temporal claim in the build plan
Net: an always-true label now versus a dated claim that the data probably does not support.
Header: Rep timing
Options:
A) Relabel "reportado en SECOP" (recommended)
SECOP rep edges read "representante legal reportado en SECOP para este proveedor" with source and observed_at, and make no signing-date claim; the contract node keeps its own signing date (R10 applies there). R1 traversal of both rep kinds unchanged, labels "reportado en SECOP" vs "vigente a <corte> (RUES)". human ~2 h / CC ~10 min.
B) Keep "rep at signing"
No change: SECOP rep edges stay "representante legal a la firma" dated by fecha_de_firma, per design line 75, R1 and R10.
C) Investigate first
Before choosing, compare the rep in 10 pre-2021 golden-case contract PDFs with the dataset field and read Colombia Compra Eficiente's field documentation, then return to this row. Label stays undecided meanwhile; no implementation approved.
D) Defer this change
Leave this proposal unresolved and revisit at the M1 legal review; current approvals (R1, R10) unchanged.

State: approved
Actual answer: A) Relabel "reportado en SECOP" (recommended), D20 answer 2026-09-25
Accepted scope: SECOP rep edges read "representante legal reportado en SECOP para este proveedor" with source and observed_at, and make no signing-date claim; the contract node keeps its own signing date (R10 applies there). R1 traversal of both rep kinds unchanged, labels "reportado en SECOP" vs "vigente a <corte> (RUES)".
History: none

### R17: Propagating identity changes made outside ingest
Finding: X2, P1, confidence 8/10, R8 + R4 + R9 + design line 84, 125, reviewer: Codex (finding 4)
Plan baseline: R8 approved (D12 A): entity/edge rows upserted by ingest; R4 (D8 A): maintainer resolves suspects via admin CLI; design line 84 merge/split rules, line 125 suppression. No propagation mechanism stated.
Runtime evidence: none (no code). Under R7, unchanged source hashes trigger no writes, so ingest alone never re-derives rows after a judgment change.
Comparison grid:

| Choice | Current | A | B | C | D |
|---|---|---|---|---|---|
| R17 propagation of resolver/moderator actions | unspecified | each action (suspect → exact/rejected, merge, split, suppression, correction outcome b/c) runs in one transaction that recomputes entity/edge rows, search-index rows and entity-card counts for the affected clusters and writes redirects; integration tests assert search, expand and counts reflect the change immediately | unchanged | bounded spike (≤ 1 day) comparing in-transaction rebuild with query-time cluster resolution on the cohort; return to this row | proposal deferred |
| R8 storage shape | approved | unchanged | unchanged | unchanged | unchanged |
| Work | n/a | human ~2 days / CC ~1 h | none | ~1 day spike | none |

Question D21:
D21 — Make moderator identity changes update the public graph immediately?
Project/branch/task: follow-the-money on master, M1 resolver and admin CLI (R8, R4, design lines 84, 125).
ELI10: You approved (D12) that ingest writes the graph tables, and (D11) that ingest skips rows whose source content didn't change. But some changes don't come from the source: the maintainer confirms or rejects a suspect ID, merges or splits an entity, or suppresses a person after a Ley 1581 request. Nothing in the plan says those decisions rewrite the graph, search index and counts. So a suppressed person could stay searchable until some unrelated source change touches the row. The fix: each such action rebuilds exactly the affected rows in the same database transaction.
Stakes if we pick wrong: A suppression or correction you promised a data subject silently fails to take effect, which is a legal-deadline problem, not a bug report.
Recommendation: A because suppression and corrections carry legal deadlines, and an in-transaction rebuild of a few affected clusters is small and testable.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Apply: rebuild affected rows in the action's transaction (recommended)
  ✅ Suppressions, rejections, merges and splits show up on the site the moment they commit
  ✅ Integration tests can assert search/expand/counts before and after each action
  ❌ Every moderator action needs a rebuild path and tests (human ~2 days / CC ~1 h)
B) Keep as is
  ✅ No extra code now
  ✅ Ingest stays the single writer of graph tables
  ❌ Moderator decisions and suppressions may never reach public views
C) Investigate: 1-day spike vs query-time resolution
  ✅ Measures whether resolving clusters at query time is simpler at national scale
  ✅ Bounded to one day on the cohort
  ❌ Propagation stays undefined until you return; admin CLI cannot be finished
D) Defer this change
  ✅ No work now
  ✅ Revisit when the admin CLI is built
  ❌ Leaves the Ley 1581 suppression path without a working mechanism in the plan
Net: a small rebuild path per action versus legal commitments that may not take effect.
Header: Propagation
Options:
A) Rebuild in same txn (recommended)
Each resolver/moderator action (suspect → exact or rejected, merge, split, suppression, correction outcomes b and c) runs in one transaction that recomputes entity/edge rows, search-index rows and entity-card counts for the affected clusters and writes redirects. Integration tests assert search, expand and counts reflect the change immediately. R8 unchanged. human ~2 days / CC ~1 h.
B) Keep as is
No propagation mechanism; graph tables change only when ingest processes a changed source record.
C) Investigate first
Run a spike of at most 1 day on the cohort comparing in-transaction rebuild with query-time cluster resolution, then return to this row. No implementation approved meanwhile.
D) Defer this change
Leave this proposal unresolved until the admin CLI is built; current approvals unchanged.

State: approved
Actual answer: A) Rebuild in same txn (recommended), D21 answer 2026-09-25
Accepted scope: Each resolver/moderator action (suspect → exact or rejected, merge, split, suppression, correction outcomes b and c) runs in one transaction that recomputes entity/edge rows, search-index rows and entity-card counts for the affected clusters and writes redirects. Integration tests assert search, expand and counts reflect the change immediately. R8 unchanged.
History: none

### R18: Keep prior versions of changed source records
Finding: X4, P2, confidence 8/10, design line 74 + R7 + R6, reviewer: Codex (finding 6)
Plan baseline: design line 74 statements carry `observed_at`; R7 (D11 A) content hash skips unchanged rows; R6 (D10 A) keeps deleted rows for audit. Overwrite behavior for changed rows unspecified.
Runtime evidence: SECOP rows are edited in place (`ultima_actualizacion`, `:updated_at`); a changed rep or name would replace the value that supported an earlier view, judgment or correction response.
Comparison grid:

| Choice | Current | A | B | C | D |
|---|---|---|---|---|---|
| R18 changed source record | overwrite (implied) | versioned: a changed record closes the old version (`observed_to` = pull time) and inserts a new one; public views read current versions; judgments, suspect reviews, correction tickets and suppression records store the version id they relied on; Ley 1581 access export includes prior versions | unchanged | bounded check: measure change rate per week on the cohort for 2 weekly pulls, then return | proposal deferred |
| X8 storage format (statements vs typed records) | pending | not decided here | not decided here | not decided here | not decided here |
| R7 hash, R6 sweep | approved | unchanged (hash decides whether a new version is written) | unchanged | unchanged | unchanged |
| Work | n/a | human ~1.5 days / CC ~45 min | none | ~2 weeks of elapsed cohort pulls | none |

Question D22:
D22 — Keep old versions when SECOP or RUES edits a row?
Project/branch/task: follow-the-money on master, M1 ingest and audit trail (design line 74; R6, R7).
ELI10: Datos.gov.co edits rows in place: a rep ID gets fixed, a name changes. The plan keeps deleted rows (D10) but says nothing about edited ones, so an edit would silently overwrite the value that a moderator's decision, a correction answer or a journalist's screenshot relied on. Versioning means an edit closes the old version with a date and adds a new one; the public site shows the current version, and every decision records which version it was based on.
Stakes if we pick wrong: When a data subject or a lawyer asks "what did you show on March 3 and why?", you can't answer.
Recommendation: A because the site's credibility and Ley 1581 access answers depend on reconstructing what was shown and why, and the hash (D11) already tells us exactly when a version changes.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Apply: version changed records; decisions cite versions (recommended)
  ✅ Any past view, judgment or correction response can be reconstructed from stored versions
  ✅ Reuses the D11 hash as the "new version" trigger; no extra diffing
  ❌ Storage grows with the edit rate; version ids thread through judgments and tickets
B) Keep overwrite
  ✅ Simplest storage; one row per source record
  ✅ No version plumbing in the resolver or correction flow
  ❌ Earlier evidence for decisions and corrections is lost on every source edit
C) Investigate: measure weekly edit rate on the cohort first
  ✅ Sizes the storage cost before committing
  ✅ Two weekly pulls on the cohort are cheap
  ❌ Leaves overwrite behavior undecided for ~2 weeks; ingest write path waits
D) Defer this change
  ✅ No work now
  ✅ Revisit before widening to national data
  ❌ Anything ingested before the decision has no history
Net: some extra storage versus being unable to explain past pages and decisions.
Header: Versions
Options:
A) Version records (recommended)
A changed source record closes its old version (observed_to = pull time) and inserts a new one; public views read current versions. Judgments, suspect reviews, correction tickets and suppression records store the version id they relied on, and the Ley 1581 access export includes prior versions. The D11 hash decides when a new version is written; storage format (X8) not decided here. human ~1.5 days / CC ~45 min.
B) Keep overwrite
Changed source records overwrite the previous values; no version history. R6/R7 unchanged.
C) Investigate first
Measure the weekly edit rate on the cohort across 2 weekly pulls, then return to this row. No implementation approved meanwhile.
D) Defer this change
Leave this proposal unresolved until before widening; current approvals unchanged.

State: approved
Actual answer: A) Version records (recommended), D22 answer 2026-09-25
Accepted scope: A changed source record closes its old version (observed_to = pull time) and inserts a new one; public views read current versions. Judgments, suspect reviews, correction tickets and suppression records store the version id they relied on, and the Ley 1581 access export includes prior versions. The D11 hash decides when a new version is written; storage format (X8) not decided here.
History: none

### R19: Does suppression remove a person's linkability or only their label?
Finding: X5, P2, confidence 7/10, design line 124-125, reviewer: Codex (finding 5)
Plan baseline: design line 125 (approved in office-hours): suppressed person → "persona suprimida" placeholder, "derived edges through it stay (so 'shared legal rep' between two companies remains visible)"; suppression applies only to incidental persons (line 124).
Runtime evidence: none. Under R1 (D5 A), RUES current rep is traversable, so an incidental RUES-only rep can be the link between two named companies; the companies' own RUES records name their rep.
Comparison grid:

| Choice | Current | A | B | C | D |
|---|---|---|---|---|---|
| R19 suppressed person's edges | placeholder node; edges through it stay | placeholder removed; all edges through the suppressed person hidden publicly; companies no longer linked via that person; suppression record kept internally | unchanged | unchanged until the M1 legal review answers "does a nameless linking node defeat suppression under Ley 1581?"; then return to this row | proposal deferred |
| M1 legal review agenda | per R2, R4 | unchanged | unchanged | + this question | unchanged |
| Work | n/a | human ~0.5 day / CC ~20 min (uses R17 rebuild path) | none | one agenda item | none |

Question D23:
D23 — Should suppressing a person also hide the company links that run through them?
Project/branch/task: follow-the-money on master, M1 correction flow (design lines 124-125).
ELI10: When an incidental person (someone with no public role, only in RUES) asks to be suppressed, the design replaces them with a nameless "persona suprimida" node but keeps the lines through it, so two companies still show as sharing a rep. Codex points out that anyone can open those companies' RUES records and read the rep's name, so the nameless node still points straight at the person. Removing the links protects the person better but hides a real company-to-company connection. Whether a nameless node satisfies Ley 1581 is a legal call, and you already have a lawyer reviewing the correction flow.
Stakes if we pick wrong: Keep the links and a granted suppression may not actually protect the person; drop them and journalists lose some genuine company connections.
Recommendation: C because this is a legal judgment about what suppression must achieve, the lawyer is already booked for the correction flow, and the approved rendering stays in place until they answer.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Apply: hide all edges through a suppressed person
  ✅ Suppression actually removes the person's linkability, not just the name
  ✅ Small change on top of the D21 rebuild path
  ❌ Genuine company-to-company connections disappear from the public graph
B) Keep design: nameless node, links stay
  ✅ Company connections remain visible for investigations
  ✅ Matches the approved office-hours design
  ❌ The person may remain identifiable through the linked companies' public records
C) Investigate: put it on the M1 legal review (recommended)
  ✅ The person qualified to decide (the lawyer) decides, with the concrete example
  ✅ No rework now; approved rendering stays until the answer
  ❌ The final rendering waits on the legal review date
D) Defer this change
  ✅ No work, no agenda item
  ✅ Revisit if a suppression request actually arrives
  ❌ The first real request would land with the question unanswered
Net: let the lawyer decide the privacy line versus deciding it ourselves now.
Header: Suppression
Options:
A) Hide edges too
Suppressing a person hides the placeholder and every edge through them publicly, so companies are no longer linked via that person; the suppression record stays internal. Uses the D21 rebuild path. human ~0.5 day / CC ~20 min.
B) Keep design
No change: "persona suprimida" placeholder node, derived edges through it stay, as in design line 125.
C) Ask the M1 legal review (recommended)
Add to the M1 legal review: "does a nameless linking node defeat suppression under Ley 1581?" Approved rendering (design line 125) stays until the answer; then return to this row. No implementation change approved now.
D) Defer this change
Leave this proposal unresolved; revisit when a suppression request arrives. Current rendering unchanged.

State: approved
Actual answer: A) Hide edges too (recommendation was C), D23 answer 2026-09-25
Accepted scope: Suppressing a person hides the placeholder and every edge through them publicly, so companies are no longer linked via that person; the suppression record stays internal. Uses the D21 rebuild path. Supersedes design line 125 "derived edges through it stay".
History: none

### R20: Operating-feasibility gate before widening to national data
Finding: X6, P2, confidence 8/10, design line 172-178 + 195 + Open Q4, reviewer: Codex (finding 8)
Plan baseline: design line 178 go/no-go = "extrapolate from the M1 cohort's measured bytes per statement"; R15 (D19 A) latency budget + load test; hosting budget is Open Question 4.
Runtime evidence: none (no infra). Approved R6 weekly sweep (~6M ids), R18 versioning and R7 hashing all add ops load not in the sizing estimate.
Comparison grid:

| Choice | Current | A | B | C | D |
|---|---|---|---|---|---|
| R20 pre-widening ops gate | bytes/statement extrapolation only | on staging with national data, measure and record: initial national ingest time, nightly incremental time, weekly sweep time, full-replace recovery time, peak storage incl. WAL and backups, restore-from-backup time, monthly cost; you set a monthly hosting cap before widening; public launch blocked if cost exceeds the cap or a nightly refresh exceeds 6 h | unchanged | cohort-only extrapolation of those metrics, then return | proposal deferred |
| R15 latency gate | approved | unchanged | unchanged | unchanged | unchanged |
| Open Q4 hosting budget | open | must be answered (cap number) before widening | open | open | open |
| Work | n/a | human ~1 day / CC ~30 min (runbook + metrics script) + staging time | none | ~0.5 day | none |

Question D24:
D24 — Add an operations gate (time, storage, restore, cost) before going national?
Project/branch/task: follow-the-money on master, M1 widening and launch gate (design lines 172-178, 195; Open Question 4).
ELI10: Before launch, the plan widens from the test cohort to all 6M SECOP contracts. The only go/no-go check is "bytes per statement". It doesn't ask whether the first full load finishes, whether nightly refreshes fit in a night, whether the weekly sweep and versioning fit, whether you can restore from backup, or what it costs per month, and the hosting budget is still an open question. The gate: measure those on staging with national data and set a monthly cost cap; launch waits if cost or refresh time blow past it.
Stakes if we pick wrong: You discover after launch that refreshes overrun, backups don't restore, or the bill is beyond what you will keep paying, and the site goes stale or down.
Recommendation: A because a solo-run public site lives or dies on cost and recoverability, and these are cheap to measure once the national data is on staging anyway.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Apply: measured ops gate + your monthly cap (recommended)
  ✅ Launch happens only if refresh, restore and cost fit what one maintainer can sustain
  ✅ Forces the hosting-budget question (Open Q4) to a number before money is spent
  ❌ Adds a staging measurement pass and a runbook before launch (human ~1 day / CC ~30 min)
B) Keep: bytes/statement extrapolation only
  ✅ Fastest path to launch
  ✅ Matches the approved design
  ❌ Refresh overruns, failed restores or cost surprises appear in production
C) Investigate: extrapolate the same metrics from the cohort only
  ✅ Cheap early signal without national data on staging
  ✅ Bounded to ~0.5 day
  ❌ Extrapolation misses nonlinear costs (indexes, WAL, sweep); gate stays undecided
D) Defer this change
  ✅ No work now
  ✅ Revisit when widening starts
  ❌ Hosting budget stays open while national coverage is mandatory
Net: one measured staging pass versus finding the bill and the refresh time in production.
Header: Ops gate
Options:
A) Measured ops gate (recommended)
On staging with national data, measure and record initial national ingest time, nightly incremental time, weekly sweep time, full-replace recovery time, peak storage including WAL and backups, restore-from-backup time and monthly cost. You set a monthly hosting cap (answers Open Q4) before widening; public launch is blocked if cost exceeds the cap or a nightly refresh exceeds 6 h. R15 unchanged. human ~1 day / CC ~30 min plus staging time.
B) Keep extrapolation only
No change: go/no-go stays the bytes-per-statement extrapolation from the cohort; Open Q4 stays open.
C) Investigate first
Extrapolate the same metrics from the cohort only (~0.5 day), then return to this row. No gate approved meanwhile.
D) Defer this change
Leave this proposal unresolved until widening starts; current approvals unchanged.

State: approved
Actual answer: A) Measured ops gate (recommended), D24 answer 2026-09-25
Accepted scope: On staging with national data, measure and record initial national ingest time, nightly incremental time, weekly sweep time, full-replace recovery time, peak storage including WAL and backups, restore-from-backup time and monthly cost. You set a monthly hosting cap (answers Open Q4) before widening; public launch is blocked if cost exceeds the cap or a nightly refresh exceeds 6 h. R15 unchanged.
History: none

### R21: A usefulness test that is not pre-selected to succeed
Finding: X7, P2, confidence 7/10, design line 196 + 227, reviewer: Codex (finding 1)
Plan baseline: design line 196: Sunday test on a contractor "known to have a documented shared legal representative"; line 227: found within 2 minutes.
Runtime evidence: rep coverage probe (67% of company contracts lack a SECOP rep; RUES fills most current reps per R1) means typical contractors differ from the hand-picked case.
Comparison grid:

| Choice | Current | A | B | C | D |
|---|---|---|---|---|---|
| R21 launch usefulness evidence | Sunday test on a known shared-rep case | Sunday test unchanged + typical-task test: 3 journalists × 5 contractors drawn at random from national data (3 companies, 2 natural persons); task "who is behind this contractor and what else are they connected to, or conclude the data shows nothing"; record time, outcome, and correct dead-end conclusions; launch requires a correct connection or a correct "nothing in the data" within 5 minutes on ≥ 70% of the 15 tasks | unchanged | run the typical-task test informally with 1 journalist first, then return | proposal deferred |
| R20 ops gate, R15 latency gate | approved | unchanged | unchanged | unchanged | unchanged |
| Work | n/a | ~1 day of sessions + fixture sampling script (CC ~15 min) | none | ~2 h | none |

Question D25:
D25 — Add a random-contractor usefulness test to the launch gate?
Project/branch/task: follow-the-money on master, M1 launch gate (design lines 196, 227).
ELI10: The Sunday test hands a journalist a contractor already known to share a legal rep and times how fast they find it. Codex's point: that tests navigation on a case built to succeed, not whether the tool helps on an ordinary contractor, where the rep may be missing or there's simply nothing to find. A second test draws contractors at random and checks that journalists reach either a real connection or a correct "nothing here" within 5 minutes. That tells you whether the national launch is worth it, not just whether the golden path works.
Stakes if we pick wrong: You launch on a demo that works for one case while typical searches dead-end confusingly, and early users decide the tool doesn't work.
Recommendation: A because the probes already showed typical contractors look different from the golden case, and 15 short tasks is a cheap way to find out before going public.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Apply: 15 random-contractor tasks, ≥ 70% gate (recommended)
  ✅ Measures usefulness on typical contractors, including honest dead ends
  ✅ Surfaces confusing empty states and missing-rep cases before the public does
  ❌ Needs 3 journalist sessions (~1 day total) and may delay launch if it fails
B) Keep Sunday test only
  ✅ One short session, already planned
  ✅ Matches the approved launch gate
  ❌ Proves the golden path, says nothing about ordinary searches
C) Investigate: 1 informal journalist session first
  ✅ Cheap early read (~2 h) on whether typical searches work
  ✅ Can shape the formal test's threshold
  ❌ Gate stays undecided; one person is a weak signal
D) Defer this change
  ✅ No extra sessions before launch
  ✅ Revisit after launch with real usage data
  ❌ Public launch without evidence the tool helps on typical contractors
Net: a day of journalist sessions versus launching on a single best-case demo.
Header: Usefulness
Options:
A) Random-task gate (recommended)
Keep the Sunday test and add a typical-task test: 3 journalists × 5 contractors drawn at random from national data (3 companies, 2 natural persons). Task: who is behind this contractor and what else are they connected to, or conclude the data shows nothing. Record time, outcome and correct dead-end conclusions. Launch requires a correct connection or a correct "nothing in the data" within 5 minutes on ≥ 70% of the 15 tasks. R15/R20 unchanged.
B) Sunday test only
No change: the launch gate keeps only the Sunday test on a known shared-rep contractor.
C) Investigate first
Run the typical-task test informally with 1 journalist (~2 h), then return to this row. No gate change approved meanwhile.
D) Defer this change
Leave this proposal unresolved until after launch; current gate unchanged.

State: approved
Actual answer: A) Random-task gate (recommended), D25 answer 2026-09-25
Accepted scope: Keep the Sunday test and add a typical-task test: 3 journalists × 5 contractors drawn at random from national data (3 companies, 2 natural persons). Task: who is behind this contractor and what else are they connected to, or conclude the data shows nothing. Record time, outcome and correct dead-end conclusions. Launch requires a correct connection or a correct "nothing in the data" within 5 minutes on ≥ 70% of the 15 tasks. R15/R20 unchanged.
History: none

### R22: Store M1 source data as FtM statements or as typed source records
Finding: X8, P2, confidence 7/10, design line 74 + 170 + 174-177, reviewer: Codex (finding 7)
Plan baseline: design line 74 "Every adapter emits FtM statements"; decision log 2026-09-25 (user): "built on FtM statement model + nomenklatura-style identity judgments ... statement/provenance model avoids a rewrite". This row would reverse part of that logged decision.
Reason for reopening: changed assumptions. Approved R6 (sweep), R7 (hash), R8 (serving tables), R18 (versioning) all operate per source record; statements are no longer the serving layer. Sizing is ~120M statements vs ~6M SECOP records.
Runtime evidence: none (no code). Identity layer (R9) is separate either way.
Comparison grid:

| Choice | Current | A | B | C | D |
|---|---|---|---|---|---|
| R22 M1 source storage | FtM statements (~130M rows), versioned per R18 | typed, versioned source-record tables per dataset (e.g. `secop_contract`, `rues_record`, `group_member`, each with version, hash, observed_from/to, retracted_at, source URL); FtM entities/statements generated at the boundary with the `followthemoney` library for the evidence drawer and future exports; M2 adds one typed table per source | unchanged | ≤ 2-day spike on the cohort storing both shapes; compare bytes, ingest time and versioning code; then return | proposal deferred |
| Identity layer R9, serving tables R8 | approved | unchanged | unchanged | unchanged | unchanged |
| Sizing (design 174-177) | ~130-140M statements, 60-120 GB | re-estimated from ~6M SECOP + reachable RUES records | unchanged | measured | unchanged |
| Work | n/a | less than statements for M1 (no pivot); + FtM mapper at boundary (human ~1 day / CC ~30 min) | baseline | ~2 days | none |

Question D26:
D26 — Store M1 data as typed source records instead of 120M FtM statements?
Project/branch/task: follow-the-money on master, M1 storage (design lines 74, 170, 174-177; logged decision "FtM statement model", 2026-09-25).
ELI10: The design stores everything as FtM "statements": one row per property per record, about 120M rows for SECOP alone. You chose that so hypothesis mode wouldn't force a rewrite. Since then, every ingest decision you approved today works per source record: the weekly sweep, the row hash, versioning, the direct-edge graph tables. Statements are no longer what the site reads. Codex's suggestion: keep one typed, versioned row per source record (~6M for SECOP), and generate FtM statements at the edge when the evidence drawer or an export needs them. Identity judgments and hypothesis mode live in the separate identity tables either way. This would reverse part of your logged decision, so it's your call.
Stakes if we pick wrong: Keep statements and every approved ingest rule pays a 20x row pivot and bigger storage; switch and each new M2 source needs its own typed table instead of one generic store.
Recommendation: A because all approved ingest rules are per record, identity stays separate, and FtM compatibility is kept at the boundary; the statement store no longer earns its size in M1.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Apply: typed versioned source records, FtM at the boundary (recommended)
  ✅ ~20x fewer rows; sweep, hash and versioning map one-to-one onto a record row
  ✅ FtM interop kept via the followthemoney library when rendering evidence or exporting
  ❌ Reverses part of a logged decision; each M2 source adds a typed table and mapper
B) Keep FtM statement store
  ✅ One generic store for every current and future source
  ✅ Matches your logged decision and the approved design
  ❌ ~130M rows plus pivots for every per-record rule you approved today
C) Investigate: 2-day cohort spike of both shapes
  ✅ Real numbers on bytes, ingest time and code size before choosing
  ✅ Fits inside the cohort ingest you do first anyway
  ❌ The ingest write path, the first thing you build, waits on the spike
D) Defer this change
  ✅ No change to the approved design now
  ✅ Revisit at the M2 eng review
  ❌ Data ingested before then is in the shape you may later migrate
Net: a smaller store that fits today's approved rules versus one generic store sized for future sources.
Header: Storage
Options:
A) Typed records (recommended)
Store M1 data in typed, versioned source-record tables per dataset (secop_contract, rues_record, group_member; each with version, hash, observed_from/to, retracted_at, source URL). Generate FtM entities/statements at the boundary with the followthemoney library for the evidence drawer and future exports. Identity (R9) and serving tables (R8) unchanged; sizing re-estimated from ~6M SECOP + reachable RUES records. Supersedes design line 74 storage for M1. human ~1 day / CC ~30 min for the mapper.
B) Keep statements
No change: every adapter writes FtM statements (versioned per R18) into the partitioned statement table, as designed and logged.
C) Investigate first
Spike both storage shapes on the cohort for at most 2 days; compare bytes, ingest time and versioning code; then return to this row. No storage change approved meanwhile.
D) Defer this change
Leave this proposal unresolved until the M2 eng review; the design's statement store stays the plan.

State: approved
Actual answer: A) Typed records (recommended), D26 answer 2026-09-25
Accepted scope: Store M1 data in typed, versioned source-record tables per dataset (secop_contract, rues_record, group_member; each with version, hash, observed_from/to, retracted_at, source URL). Generate FtM entities/statements at the boundary with the followthemoney library for the evidence drawer and future exports. Identity (R9) and serving tables (R8) unchanged; sizing re-estimated from ~6M SECOP + reachable RUES records. Supersedes design line 74 storage for M1. Partially reverses the logged decision "FtM statement model" (identity judgments and hypothesis-mode direction unchanged).
History: none

Outside Voice dispositions: X1 → R16 approved (A). X2 → R17 approved (A). X3 carried (sweep completeness guard + un-retract on reappearance). X4 → R18 approved (A). X5 → R19 approved (A, against recommendation C). X6 → R20 approved (A). X7 → R21 approved (A). X8 → R22 approved (A). Cross-model: Codex and Claude agreed on refresh lifecycle and scale risks; Codex alone surfaced the rep-at-signing semantics (confirmed by Claude probe), identity propagation, suppression linkability, versioning and the storage shape. No remaining disagreement.

## Final planning decisions: TODO proposals

### T1: Revise the design doc for the remaining office-hours legal/process concerns before the M1 legal review
Finding: office-hours Reviewer Concerns R3-10, R3-11, R3-12, R3-13 (unresolved, "Disposition: CONCERNS_RECORDED"), reviewer: office-hours adversarial loop, carried by Claude
Plan baseline: design doc lines 262-428 list them as recorded concerns; none were in this eng review's scope decisions.
Runtime evidence: n/a (document gaps).
TODO body:
- **What:** Update the design doc for R3-10 (unverified requesters must not get "a solicitud del titular" annotations), R3-11 (per-type deadlines 10/15 business days, drop the blanket 15), R3-12 (where requester ID documents live, who can read them, retention and deletion), R3-13 (backup moderator or escalation path; re-measure the suspect-ID queue on national data before launch; Ley 1581 deadlines take priority).
- **Why:** All four are M1 correction-flow obligations the lawyer will review; three are legal-exposure items.
- **Pros:** Legal review sees a consistent doc; closes 4 of 16 recorded concerns. **Cons:** ~2 h of writing; R3-13 needs you to name a backup person.
- **Context:** The other 12 concerns are resolved here (R3-1 → R4, R3-2 → R5, R3-3 → R11, R3-4 → Q5, R3-5 → R12, R3-16 → R3) or belong to M3 (R3-6, R3-7, R3-8, R3-9, R3-14, R3-15).
- **Depends on / blocked by:** Before booking the M1 legal review.
- Effort S, Priority P1, section `## Legal & privacy`.
Comparison grid:

| Choice | Current | A | B | C |
|---|---|---|---|---|
| T1 disposition | unrecorded | add to repo `TODOS.md` (created) under `## Legal & privacy`, P1 | skip | do it now in this review: edit the design doc (both copies) for R3-10..13 |

Question D27:
D27 — Track the four open office-hours legal concerns as a TODO?
Project/branch/task: follow-the-money on master; design doc Reviewer Concerns R3-10 to R3-13.
ELI10: The office-hours review left 16 concerns recorded but unresolved. This review settled or re-homed 12 of them. Four remain, all about the correction flow the lawyer will check: unverified people shouldn't be able to post statements labeled "a solicitud del titular"; the deadline text contradicts itself; there's no rule for where requesters' ID copies are stored and when they're deleted; and one maintainer has no backup for legal deadlines. They need a doc update before the legal review.
Stakes if we pick wrong: Skip it and the lawyer reviews a doc with a known impersonation hole and no data-retention rule for ID copies.
Recommendation: A because it's a 2-hour doc fix that must happen before the legal review, and R3-13 needs a name only you can supply.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Add to TODOS.md, P1 (recommended)
  ✅ Tracked in the repo next to the code, with the exact four fixes listed
  ✅ Lets you pick the backup moderator on your own time
  ❌ Creates TODOS.md in the repo (new file)
B) Skip
  ✅ No new file or tracking overhead
  ✅ You may already plan to rewrite the correction section
  ❌ Easy to forget before booking the lawyer; the impersonation hole stays
C) Do it now: edit the design doc in this session
  ✅ Closed today, both doc copies consistent
  ✅ No TODO to track
  ❌ I'd have to guess the backup moderator and retention period; you'd need to review my text
Net: a tracked 2-hour fix versus a known legal hole going to the lawyer.
Header: TODO legal
Options:
A) Add to TODOS.md (recommended)
Create TODOS.md in the repo with this item under "## Legal & privacy" at P1: update the design doc for R3-10 (verified-only "a solicitud del titular"), R3-11 (10/15-day per-type deadlines), R3-12 (ID-document storage, access, retention, deletion), R3-13 (backup moderator, national queue re-measure, legal deadlines first), before the M1 legal review.
B) Skip
Do not record this TODO; the four concerns stay only in the design doc's Reviewer Concerns appendix.
C) Build it now
Edit both design doc copies now to resolve R3-10..13 with proposed text (placeholders where only you can decide: backup moderator name, retention period), for your review.

State: approved
Actual answer: A) Add to TODOS.md (recommended), D27 answer 2026-09-25
Accepted scope: Create TODOS.md in the repo with this item under "## Legal & privacy" at P1: update the design doc for R3-10 (verified-only "a solicitud del titular"), R3-11 (10/15-day per-type deadlines), R3-12 (ID-document storage, access, retention, deletion), R3-13 (backup moderator, national queue re-measure, legal deadlines first), before the M1 legal review.
History: none

### T2: Socrata app token for bulk national pulls and weekly sweeps
Finding: P4, P3, confidence 7/10, design line 168 + learning colombia-open-data-id-keys, reviewer: Claude
Plan baseline: ingest runs unauthenticated ("queryable via Socrata API without auth").
Runtime evidence: all probes this session ran unauthenticated and completed; Socrata documents lower throttling limits for anonymous clients. Throughput at national scale (6M-row initial pull, weekly `:id` sweep per R6) not measured.
TODO body:
- **What:** Register a datos.gov.co (Socrata) app token; send it as `X-App-Token` from ingest; keep it in the deploy secret store, never in the repo.
- **Why:** The initial national pull and the weekly 6M-id sweep are the heaviest API users; anonymous throttling could stretch them past the R20 6-hour refresh limit.
- **Pros:** 10-minute setup, higher limits, identifiable client if datos.gov.co needs to contact you. **Cons:** one more secret to manage.
- **Context:** R20 ops gate measures ingest and sweep time on staging; do this before that measurement.
- **Depends on / blocked by:** Before the R20 staging measurement.
- Effort S, Priority P2, section `## Ingest`.
Comparison grid:

| Choice | Current | A | B | C |
|---|---|---|---|---|
| T2 disposition | unrecorded | add to TODOS.md under `## Ingest`, P2 | skip | build now: make the ingest client send `X-App-Token` from an env var from the first commit (accepted implementation scope); token registration stays your task, listed in TODOS.md |

Question D28:
D28 — Track getting a Socrata app token for bulk ingest?
Project/branch/task: follow-the-money on master, M1 ingest (design line 168).
ELI10: All of today's API probes ran without a key. That's fine for small queries, but Socrata throttles anonymous clients harder, and M1 will pull 6M SECOP rows once and sweep all 6M row IDs every week (D10). A free app token raises the limits and identifies you to datos.gov.co. It's a 10-minute setup, and it should happen before you measure refresh times for the ops gate (D24).
Stakes if we pick wrong: Throttled pulls could push nightly or weekly jobs past the 6-hour limit you approved and fail the ops gate for a non-reason.
Recommendation: C because reading the token from an env var from the first commit costs nothing and avoids retrofitting the client later.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Add to TODOS.md, P2
  ✅ Tracked for before the staging measurement
  ✅ No code decision now
  ❌ Client may get written without the header and need a retrofit
B) Skip
  ✅ Nothing to track or store
  ✅ Anonymous access already works for probes
  ❌ Risk of throttled national pulls during the ops gate
C) Build it in: ingest client sends X-App-Token from env (recommended)
  ✅ Zero retrofit; works anonymously until you set the variable
  ✅ Token stays in the secret store, never the repo
  ❌ You still need to register the token yourself on datos.gov.co
Net: a one-line header from day one versus a possible throttling surprise during the ops gate.
Header: TODO token
Options:
A) Add to TODOS.md
Add under "## Ingest" at P2: register a Socrata app token, send it as X-App-Token from ingest, store it in the deploy secret store, before the R20 staging measurement.
B) Skip
Do not record this TODO; ingest stays anonymous.
C) Build it now (recommended)
Accepted implementation scope: the ingest HTTP client sends X-App-Token from an environment variable when set (anonymous otherwise), from the first commit; token registration on datos.gov.co stays your task and is listed in TODOS.md.

State: approved
Actual answer: C) Build it now (recommended), D28 answer 2026-09-25
Accepted scope: The ingest HTTP client sends X-App-Token from an environment variable when set (anonymous otherwise), from the first commit; token registration on datos.gov.co stays your task and is listed in TODOS.md.
History: none

### T3: Alert when a source dataset changes en masse in one pull
Finding: split out of R7 (D11) as separate instrumentation, P3, confidence 7/10, reviewer: Claude
Plan baseline: R7 approved (D11 A) content hash; no alert.
Runtime evidence: a Socrata full replace re-stamps `:updated_at` on every row; with R7, a real mass content change (schema/format change by the publisher) would silently produce millions of new versions (R18).
TODO body:
- **What:** After each pull, if > 20% of a dataset's records got a new content hash, log and email the maintainer; ingest continues.
- **Why:** A publisher schema or formatting change (e.g. new date format, renamed field) would otherwise create millions of spurious versions and edge rewrites without anyone noticing.
- **Pros:** Catches publisher-side breakage the day it happens; ~20 lines. **Cons:** needs an email/notification channel on the ingest host.
- **Context:** Hash from R7; versions from R18; the threshold is a starting value.
- **Depends on / blocked by:** R7 hash implemented.
- Effort S, Priority P2, section `## Ingest`.
Comparison grid:

| Choice | Current | A | B | C |
|---|---|---|---|---|
| T3 disposition | unrecorded | add to TODOS.md under `## Ingest`, P2 | skip | build now: alert (log + email) when > 20% of a dataset's records get a new hash in one pull, shipped with the R7 hash (accepted implementation scope) |

Question D29:
D29 — Alert when a pull changes more than 20% of a dataset?
Project/branch/task: follow-the-money on master, M1 ingest (split out of D11).
ELI10: The row hash you approved (D11) skips re-uploads where content didn't change. But if datos.gov.co changes a format for real (a new date style, a renamed field), millions of rows get new hashes, and with versioning (D22) that means millions of new versions and graph rewrites overnight. A simple alert when more than 20% of a dataset changes in one pull tells you the next morning, before it snowballs.
Stakes if we pick wrong: A silent publisher change doubles storage and rewrites the graph before anyone looks.
Recommendation: C because it's ~20 lines next to the hash code and the failure it catches is expensive and silent.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Add to TODOS.md, P2
  ✅ Tracked without adding scope to the first ingest milestone
  ✅ Can be combined with broader monitoring later
  ❌ First national pulls run without the safety net
B) Skip
  ✅ Nothing extra to build or wire up
  ✅ The ops gate (D24) may reveal storage jumps anyway
  ❌ Publisher format changes go unnoticed until storage or pages look wrong
C) Build it with the hash (recommended)
  ✅ Safety net exists from the first national pull
  ✅ ~20 lines alongside code you're already writing
  ❌ Needs an email or notification channel on the ingest host now
Net: twenty lines now versus a silent, expensive failure mode.
Header: TODO alert
Options:
A) Add to TODOS.md
Add under "## Ingest" at P2: after each pull, if > 20% of a dataset's records got a new content hash, log and email the maintainer; ingest continues.
B) Skip
Do not record this TODO; no mass-change alert.
C) Build it now (recommended)
Accepted implementation scope: ship with the R7 hash an alert (log + email to the maintainer) when > 20% of a dataset's records get a new hash in one pull; ingest continues.

State: approved
Actual answer: C) Build it now (recommended), D29 answer 2026-09-25
Accepted scope: Ship with the R7 hash an alert (log + email to the maintainer) when > 20% of a dataset's records get a new hash in one pull; ingest continues.
History: none

### T4: SECOP Proveedores Registrados as a second current-rep source
Finding: R1 option B not chosen in D5; candidate follow-up, P3, confidence 7/10, reviewer: Claude
Plan baseline: R1 approved (D5 A): RUES current rep only; no `qmzu-gj57` adapter.
Runtime evidence: `qmzu-gj57` has 1,614,430 suppliers with a rep-doc field full of junk placeholders; RUES covers 88.5% of active companies. Coverage gain for SECOP suppliers absent from RUES is unmeasured.
TODO body:
- **What:** After M1 launch, measure how many company contractors in the graph have no RUES current rep but a valid rep in SECOP Proveedores Registrados; if material (> 5% of company contractors), add a reachable-only adapter (rep phone/email never ingested).
- **Why:** Foreign suppliers and entities outside cámaras de comercio have no RUES rep, so the D5 hop is dead for them.
- **Pros:** Closes a measured coverage gap. **Cons:** another adapter and a heavy placeholder blocklist.
- **Context:** Reuses the typed-record (R22), hash (R7), version (R18) and sweep (R6) patterns; same CC BY-SA 4.0 license.
- **Depends on / blocked by:** M1 launch; coverage measurement.
- Effort M, Priority P3, section `## Ingest`.
Comparison grid:

| Choice | Current | A | B | C |
|---|---|---|---|---|
| T4 disposition | unrecorded | add to TODOS.md under `## Ingest`, P3 (measure after launch, build if > 5% gap) | skip | build now: add the qmzu-gj57 adapter to M1 (reverses the D5 choice of A over B) |

Question D30:
D30 — Track a possible second current-rep source (SECOP supplier registry)?
Project/branch/task: follow-the-money on master, post-M1 follow-up (from D5).
ELI10: In D5 you chose to follow RUES current reps and skip SECOP's own supplier registry, which also lists reps but is full of junk values. RUES misses some suppliers (foreign companies, entities not registered at a chamber of commerce), so the rep hop is dead for them. After launch you could measure how big that gap is and add the registry only if it matters.
Stakes if we pick wrong: Skipping it means a coverage gap you never measure; building now adds an adapter M1 doesn't need yet.
Recommendation: A because the gap is unmeasured and the D5 decision stands; a measure-then-build TODO keeps the door open cheaply.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Add to TODOS.md, P3 (recommended)
  ✅ Measured decision after launch instead of a guess now
  ✅ Clear trigger: build only if > 5% of company contractors lack a RUES rep
  ❌ One more open TODO to carry
B) Skip
  ✅ Keeps the TODO list short
  ✅ RUES already covers 88.5% of active companies
  ❌ The gap for foreign/unregistered suppliers is never measured
C) Build it now in M1
  ✅ Maximum rep coverage at launch
  ✅ Second independent source to corroborate RUES
  ❌ Reverses your D5 choice and adds an adapter plus junk-value cleanup to M1
Net: measure first versus build a source M1 may not need.
Header: TODO reps
Options:
A) Add to TODOS.md (recommended)
Add under "## Ingest" at P3: after M1 launch, measure company contractors lacking a RUES current rep but having a valid rep in SECOP Proveedores Registrados (qmzu-gj57); if > 5%, add a reachable-only adapter that never ingests rep phone/email.
B) Skip
Do not record this TODO; RUES remains the only current-rep source.
C) Build it now
Add the qmzu-gj57 adapter to M1 scope now (reachable rows only, rep phone/email never ingested), reversing the D5 choice.

State: approved
Actual answer: A) Add to TODOS.md (recommended), D30 answer 2026-09-25
Accepted scope: Add under "## Ingest" at P3: after M1 launch, measure company contractors lacking a RUES current rep but having a valid rep in SECOP Proveedores Registrados (qmzu-gj57); if > 5%, add a reachable-only adapter that never ingests rep phone/email.
History: none

### T5: Eng reviews for M2 and M3 before they start, with carried items
Finding: consequence of D3 (M1-only scope), P2, confidence 9/10, reviewer: Claude
Plan baseline: D3 A: "M2/M3 get their own eng review before work starts".
Runtime evidence: n/a.
TODO body:
- **What:** Run /plan-eng-review for M2 before starting it, carrying: the M1 legal review's BY-SA × BY-NC × ODbL answer (R2), PEP source choice (OpenSanctions CC BY-NC vs primary Función Pública source), typed-record adapters per source (R22), and the renamed suspect-ID report exit criterion (Q5). Run it for M3 before starting it, carrying office-hours concerns R3-6 (ICIJ name parsing), R3-7 (multi-key blocking), R3-8 (country in blocking), R3-9 (co-linked entity definition), R3-14 (confirmed/refuted/unresolved metric), R3-15 (ODbL share-alike for derived leads).
- **Why:** D3 locked only M1; these items are known now and would otherwise be rediscovered.
- **Pros:** Nothing lost between milestones. **Cons:** none beyond carrying the item.
- **Context:** This review's report file lists all R1-R22 decisions M2/M3 must stay compatible with (identity tables R9, propagation R17, versioning R18).
- **Depends on / blocked by:** M1 launch (M2); M2 exit (M3).
- Effort S, Priority P2, section `## Planning`.
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| T5 disposition | unrecorded | add to TODOS.md under `## Planning`, P2 | skip |

Question D31:
D31 — Track the M2/M3 eng reviews and what they must pick up?
Project/branch/task: follow-the-money on master; D3 locked this review to M1.
ELI10: You scoped this review to M1 (D3). Several M2/M3 items are already known: the lawyer's answer on mixing CC BY-SA with CC BY-NC and ODbL data, where to get PEP data, and six office-hours concerns about the M3 name matcher and its success metric. A TODO makes sure each milestone's review starts with that list instead of rediscovering it. (No "build now" option: these are future reviews.)
Stakes if we pick wrong: M2 starts with OpenSanctions PEPs before the license answer is in, or M3's matcher repeats known blocking and parsing mistakes.
Recommendation: A because it costs nothing and preserves findings that took real probing to surface.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Add to TODOS.md, P2 (recommended)
  ✅ Each milestone's review starts with the known carry-over list
  ✅ License and matcher issues can't slip between milestones
  ❌ One long-lived TODO in the file
B) Skip
  ✅ Shorter TODO list
  ✅ The items still exist in this report and the design doc appendix
  ❌ Relies on remembering to read this report before M2/M3
Net: a free reminder versus relying on memory months from now.
Header: TODO M2/M3
Options:
A) Add to TODOS.md (recommended)
Add under "## Planning" at P2: run /plan-eng-review before M2 (carry R2 legal answer, PEP source choice, typed-record adapters, suspect-ID report rename) and before M3 (carry office-hours R3-6, R3-7, R3-8, R3-9, R3-14, R3-15).
B) Skip
Do not record this TODO; the items remain in this report and the design doc appendix only.

State: approved
Actual answer: A) Add to TODOS.md (recommended), D31 answer 2026-09-25
Accepted scope: Add under "## Planning" at P2: run /plan-eng-review before M2 (carry R2 legal answer, PEP source choice, typed-record adapters, suspect-ID report rename) and before M3 (carry office-hours R3-6, R3-7, R3-8, R3-9, R3-14, R3-15).
History: none

Approval readiness: PASS. Checked R1 (D5), R2 (D6), R3 (D7), R4 (D8), R5 (D9), R6 (D10), R7 (D11), R8 (D12), R9 (D13), R10 (D14), R11 (D15), R12 (D16), R13 (D17), R14 (D18), R15 (D19), R16 (D20), R17 (D21), R18 (D22), R19 (D23), R20 (D24), R21 (D25), R22 (D26), T1 (D27), T2 (D28), T3 (D29), T4 (D30), T5 (D31); scope D3, D4. Carried without new questions: F3 factual correction, F4 blocklist extension, Q5 rename, Q6 masking type, P2 indexes, X3 sweep guard + un-retract. Regression contracts: none needed (greenfield). No pending or deferred rows.

## Late change (2026-09-25): authentication and database hosting

User message after D32 (instead of choosing a next step): "Did we think about authentication? If not let's use supabase and for database hosting let's use Postgres, initially hosted at supabase and configured via env". Per Recovery routing, the affected stages (Architecture, Tests, Performance, launch gates) are reopened for these items only; D32 navigation will be asked again after outputs are refreshed.

Evidence (web, 2026-09-25): Supabase exposes the `public` schema through PostgREST and, until recently, granted `anon`/`authenticated` full table privileges there by default; opt-out became default for new projects on 2026-05-30 and is enforced on existing projects on 2026-10-30 ([changelog](https://supabase.com/changelog/45329-breaking-change-tables-not-exposed-to-data-and-graphql-api-automatically), [securing your API](https://supabase.com/docs/guides/api/securing-your-api)). Hosted projects run Postgres 17 (e.g. 17.6.x in [supabase#50626](https://github.com/supabase/supabase/issues/50626)). Connections: direct/session on 5432, transaction pooler on 6543 ([pooling docs](https://supabase.com/docs/guides/database/connecting-to-postgres/pooling-and-limits)); open reports of intermittent transaction-pooler hangs ([supabase#49991](https://github.com/supabase/supabase/issues/49991)).

Current auth state in the approved design: M1 has no login (Roles table, design line 102-106: anonymous viewer; moderator = maintainer via admin CLI); registered researchers and the moderator web UI arrive in M3; staging is "a private staging URL" (line 195) with no access mechanism named; no auth provider chosen anywhere.

### R23: Database hosting on Supabase Postgres, configured via env
Finding: late change, user-directed. Plan baseline: design line 170 "PostgreSQL" + line 177 "one managed Postgres instance" (provider unspecified); R13 (D17 A) tests on Postgres 16.
State: approved
Actual answer: user message 2026-09-25 after D32: "for database hosting let's use Postgres, initially hosted at supabase and configured via env"
Accepted scope: Production and staging Postgres hosted on Supabase; all connection settings from environment variables. Necessary implementation carried with it (no separate question; each preserves an approved contract):
- `DATABASE_URL` = direct/session connection (port 5432) for ingest, migrations, sweep and admin CLI (long transactions, R8/R17); `DATABASE_POOL_URL` = pooler for the API; R15 load test runs against whichever the API uses, and the API may fall back to direct if the pooler misses the p95 budget.
- Postgres major version in docker-compose and CI moves from 16 to 17 to match Supabase (updates R13's version only; reason: test/prod parity). `pg_trgm` enabled by migration.
- Masking contract (design line 110-114, Q6): all app tables live in a private schema (e.g. `ftm`) that is not in the Data API's exposed schemas; the Data API is disabled for the project or exposes no app schema; RLS is enabled with no policies on any table that ever lands in `public`; `anon`/`authenticated` hold no grants on app tables. A CI check asserts a Data API request with the publishable key cannot read any app table.
- Secrets (DB URLs, service/secret keys, `X-App-Token` from T2) only in env / the deploy secret store; `.env.example` committed with placeholders.
- R20 ops gate measures restore on Supabase's backup mechanism (daily backups vs PITR add-on) and includes the Supabase plan + disk + compute in the monthly cost vs cap.
History: none

### R24: What Supabase Auth protects in M1
Finding: late change, P2, confidence 8/10, design lines 102-106, 195, reviewer: user + Claude
Plan baseline: M1 has no end-user auth; moderator uses admin CLI; staging access mechanism unnamed; accounts in M3; provider unspecified. User message: "If not let's use supabase" (provider chosen: Supabase Auth).
Runtime evidence: none (no code).
Comparison grid:

| Choice | Current | A | B | C |
|---|---|---|---|---|
| R24 Supabase Auth use in M1 | no auth; staging "private URL" | Supabase Auth gates the staging deployment: email allowlist, magic link; web gets a login screen on staging only; FastAPI verifies Supabase JWTs (JWKS) on every staging request; production public site stays anonymous; moderator stays CLI; the same provider and JWT middleware serve M3 researcher/moderator accounts | A + moderator web UI moved from M3 into M1 (review queue, suppression, corrections) behind Supabase Auth with a `moderator` role claim; admin CLI kept as fallback | Supabase Auth recorded as the provider for M3 only; no auth code in M1; staging gated by host-level basic auth |
| Public site (production) | anonymous | anonymous | anonymous | anonymous |
| R23 hosting | approved | unchanged | unchanged | unchanged |
| Work | none | human ~2 days / CC ~1 h (login screen, JWT middleware, allowlist, tests) | human ~6 days / CC ~3 h (A + moderator UI, role checks, audit log UI, tests) | human ~1 h / CC ~5 min |

Question D33:
D33 — What should Supabase Auth protect in M1?
Project/branch/task: follow-the-money on master, M1 after the eng review; your late change (auth + Supabase hosting).
ELI10: We did think about who logs in, but not about how. The design keeps the M1 public site fully anonymous (anyone can search and browse), the maintainer moderates through a command-line tool, and real accounts (invited researchers, a moderator web screen) only arrive in M3. No auth provider was ever chosen, and the "private staging URL" had no lock on it. You've now chosen Supabase Auth as the provider. The question is how much of it to build in M1: just lock staging (and get the plumbing M3 will reuse), also pull the moderator web UI forward from M3, or only record the choice and build nothing yet.
Stakes if we pick wrong: Too little and staging with national data sits behind a guessable URL; too much and M1 grows by a week of moderator UI before launch.
Recommendation: A because staging will hold national data before the legal review and needs a real lock, and building the JWT path now de-risks M3 without adding moderator UI scope.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Lock staging with Supabase Auth; public stays anonymous (recommended)
  ✅ Staging with national data gets a real per-person lock (allowlisted emails, magic link)
  ✅ JWT verification in FastAPI is built once and reused by M3 accounts
  ❌ ~2 days of login screen, middleware and tests that the public site never uses in M1
B) A + moderator web UI now (moved from M3)
  ✅ Corrections, suppressions and suspect reviews get a UI instead of a CLI
  ✅ Easier to hand moderation to the backup moderator (office-hours R3-13)
  ❌ Adds ~a week of UI and role checks to M1 before launch
C) Record Supabase Auth for M3; basic auth on staging
  ✅ Near-zero M1 work
  ✅ Keeps M1 focused on data and graph
  ❌ Staging lock is a shared password; auth plumbing waits until M3
Net: a real staging lock plus reusable plumbing versus either more M1 scope or a shared password.
Header: Auth scope
Options:
A) Staging lock (recommended)
Supabase Auth gates the staging deployment with an email allowlist and magic link; the web app shows a login screen on staging only; FastAPI verifies Supabase JWTs (JWKS) on every staging request. Production public site stays anonymous; moderator stays on the admin CLI; the same provider and middleware serve M3 accounts. human ~2 days / CC ~1 h.
B) Staging lock + moderator UI
Everything in A, plus the moderator web UI (review queue, suppression, correction outcomes) moves from M3 into M1 behind Supabase Auth with a moderator role claim; the admin CLI stays as a fallback. human ~6 days / CC ~3 h.
C) Provider for M3 only
Record Supabase Auth as the provider for M3 accounts; no auth code in M1; staging protected by host-level basic auth. human ~1 h / CC ~5 min.

State: approved
Actual answer: A) Staging lock (recommended), D33 answer 2026-09-25
Accepted scope: Supabase Auth gates the staging deployment with an email allowlist and magic link; the web app shows a login screen on staging only; FastAPI verifies Supabase JWTs (JWKS) on every staging request. Production public site stays anonymous; moderator stays on the admin CLI; the same provider and middleware serve M3 accounts.
History: none

Late-change dispositions: R23 approved (user message). R24 approved (D33 A). R13 accepted scope updated for version parity (16 → 17) as necessary implementation of R23. New findings from the late change: L1 [P1] Supabase Data API could expose unmasked IDs (handled in R23 carried safeguards + CI check); L2 [P2] transaction-pooler latency risk (handled by R23 direct-connection fallback + R15 load test).

Approval readiness (after late change): PASS. Prior checks unchanged; added R23 (user message 2026-09-25) and R24 (D33). No pending rows.

## Review body

### Working plan: accepted amendments to the design (M1)

Apply these to `docs/designs/follow-the-money.md` (and the gstack copy) when implementation starts:

| Design text | Amendment | Source |
|---|---|---|
| line 72 "v1 = M1 + M2 + M3" | this eng review locks M1 only; M2/M3 need their own eng review | D3, T5 |
| lines 166-170 stack layout | `apps/web` + one Python project `backend/` (modules ids, ingest, resolver, api; two entrypoints) | D4 |
| line 74 "Every adapter emits FtM statements" | typed, versioned source-record tables per dataset; FtM generated at the boundary | R22 |
| line 75 "rep at signing" | SECOP rep = "representante legal reportado en SECOP para este proveedor" (no signing-date claim); RUES current rep "vigente a <corte>"; both traversable | R1, R16 |
| line 77 blocklist | + `Sin Descripcion`, `No definido` (case-insensitive), 1-3 digit, all-zero, punctuation-only; final list from a frequency scan | F4 |
| line 80 rule 4 | agency-NIT only for self-contracting or in a person field; name disagreement < 0.80 (surname / legal-form-stripped company), calibrated on 200 labeled IDs; no hub flag, plain counts | R5, R11, R12 |
| line 80 "shown as a lead" | M1 hides suspect edges; node shows "vínculo en revisión" | R4 |
| line 84 identity | own judgment / cluster (ULID) / redirect tables; every moderator action rebuilds affected rows in its transaction | R9, R17 |
| line 106, 198 "hub-threshold reviews", "hub report" | "suspect-ID reviews", "reviewed suspect-ID report" | Q5 |
| line 125 suppressed rendering | no placeholder, no edges through a suppressed person | R19 |
| line 161 group members | from `ceth-n4bn` Grupos de Proveedores (Open Q2 answered) | F3 |
| line 168 refresh | keyset paging, content hash, versioning, weekly `:id` sweep with completeness guard and un-retract, `X-App-Token`, > 20% change alert | R6, R7, R18, X3, T2, T3 |
| line 170 materialized tables | plain direct-edge `entity`/`edge` tables in the ingest transaction; shared-rep computed at query time | R8 |
| expand behaviour (new) | ≤ 50 neighbors per kind, drillable aggregates by agency/year | R3 |
| missing `fecha_de_firma` | NULL, "fecha de firma no reportada" on the contract; ordering falls back to start date, then last update | R10 |
| lines 195-196, 227 launch gate | + p95 ≤ 300/500 ms load test; + ops gate (times, storage, restore, monthly cap); + typical-task test ≥ 70% of 15 tasks | R15, R20, R21 |
| line 207, Open Q7 licensing | CC BY-SA 4.0; notices on footer, drawer, API, PNG; compatibility question on the M1 legal review | R2 |
| M1 legal review agenda | + share-alike and BY-SA × BY-NC × ODbL; + "vínculo en revisión" wording | R2, R4 |
| tests | pytest + real Postgres 17 + Hypothesis; Vitest; Playwright E2E (4 flows) | R13, R14, R23 |
| line 170, 177 Postgres host (new) | Supabase-hosted Postgres 17, env-configured (`DATABASE_URL` direct, `DATABASE_POOL_URL` pooled); app tables in a private schema, Data API closed to them, RLS deny-all in `public`; CI check that the publishable key reads nothing | R23 |
| lines 102-106, 195 roles / staging (new) | Supabase Auth: staging gated by email allowlist + magic link, FastAPI verifies JWTs on staging; production anonymous; moderator stays CLI; same provider for M3 accounts | R24 |

### Final M1 data flow

```
 datos.gov.co (Socrata, CC BY-SA 4.0; X-App-Token when set)
 ┌─────────────┐ ┌──────────────┐ ┌──────────────────────┐
 │ SECOP II    │ │ Grupos Prov. │ │ RUES (reachable IDs) │
 └─────┬───────┘ └──────┬───────┘ └──────────┬───────────┘
       │ nightly: keyset by :id where :updated_at > cursor
       ▼                ▼                    ▼
 ┌────────────────────────────────────────────────────────┐
 │ ingest                                                 │
 │  map row → typed record ─► hash same? ─yes─► skip      │
 │                              │no                       │
 │                              ▼                         │
 │  ids: normalize → blocklist → NIT mod-11 → doc-type    │
 │       → suspect? (self-contracting / agency NIT in     │
 │         person field / names < 0.80)                   │
 │  one txn: close old version, insert new version,       │
 │           upsert direct edges; suspect → review queue  │
 │  > 20% changed in pull ─► alert maintainer             │
 │ weekly: full :id sweep ─ complete & count within 1%? ─►│
 │         missing → retracted_at; reappeared → restore   │
 └──────────────────────────┬─────────────────────────────┘
                            ▼
 ┌────────────────────────────────────────────────────────┐
 │ Supabase Postgres 17, private schema `ftm` (Data API   │
 │ closed; RLS deny-all in public): secop_contract /      │
 │ rues_record /                                          │
 │ group_member (versioned), judgment, cluster (ULID),    │
 │ redirect, entity, edge, review_queue, suppression,     │
 │ correction_ticket; pg_trgm GIN on names                │
 └───────┬───────────────────────────────┬────────────────┘
         │                               ▲
         ▼                               │ one txn per action:
 ┌─────────────────────────┐   ┌─────────┴──────────────┐
 │ api (FastAPI)           │   │ admin CLI (maintainer) │
 │ /search                 │   │ resolve suspect, merge,│
 │ /entity/:id             │   │ split, suppress,       │
 │ /entity/:id/expand      │   │ correction outcomes    │
 │   top 50 + aggregates   │   │ → rebuild entity/edge, │
 │ /edge/:id/evidence      │   │   search rows, counts, │
 │   + license/attribution │   │   redirects            │
 │ MaskedId serializer     │   └────────────────────────┘
 │ staging only: Supabase  │     (DATABASE_URL, direct)
 │ JWT check (allowlist)   │
 │ (DATABASE_POOL_URL)     │
 └──────────┬──────────────┘
            ▼
 apps/web: search → entity card (plain counts) → Cytoscape →
 drawer (source records, BY-SA) → PNG (notice) → corrección
```

Rep hop after R1 + R16:

```
 company C ──"rep reportado en SECOP" (source, observed_at)──► person P
     └────"rep vigente a <corte> (RUES)"────────────────────────┘
 P ──both kinds──► companies C1..Cn   (≤ 50, value desc, date desc, id;
                                        rest → aggregates by agency/year;
                                        suspect edges hidden, marker shown;
                                        suppressed P → no node, no edges)
```

Files that need an inline diagram comment when built: `<pkg>/ingest/pipeline.py` (hash → version → edge upsert → sweep states), `<pkg>/resolver/actions.py` (moderator action → rebuild set), `<pkg>/ids/suspect.py` (suspect decision tree).

### Test additions from Outside Voice (required proof of R16-R22, T2, T3, X3)

```
[+] R16 labels       [GAP] SECOP rep edge text has no signing date; RUES edge distinct
[+] R17 propagation  [GAP] resolve/merge/split/suppress → search, expand, counts, redirects change in same txn
[+] R18 versions     [GAP] edited row → old version closed, new current; access export lists both
[+] R19 suppression  [GAP] suppressed person → no node, no edges, search empty; companies unlinked
[+] R22 records      [GAP] FtM boundary mapper round-trips a contract to Contract/Company/Person entities
[+] X3 sweep guard   [GAP] incomplete scan or count off > 1% → no retractions; reappeared row restored with unchanged hash
[+] T2 token         [GAP] header sent when env var set, absent otherwise
[+] T3 alert         [GAP] > 20% new hashes → alert fired, ingest continues
[+] R20 / R21        runbook + sampling script (manual gates, not CI)
[+] R23 Data API     [GAP] publishable-key request to every app table → denied (CI, against staging project)
[+] R23 env config   [GAP] app refuses to start without DATABASE_URL; pooled URL used by API only
[+] R24 staging auth [GAP] staging: no/invalid/expired JWT → 401, non-allowlisted email → 403; production: no auth required
COVERAGE (planned, greenfield): 0/71 paths tested | GAPS: 71 (4 E2E, 0 eval)
```

### NOT in scope

- M2 (Cuentas Claras, PEPs, OFAC, GLEIF) and M3 (ICIJ leads, hypothesis mode, researcher accounts): separate eng reviews (D3, T5).
- SECOP Proveedores Registrados as a second current-rep source: measure after launch (D5, T4).
- Visible hub label on prolific people: replaced by plain counts (D16).
- Precomputed pairwise shared-rep edges and materialized views (D12).
- nomenklatura as an M1 dependency (D13); revisit in the M3 review.
- Moderator web UI and researcher accounts: stay in M3, on the Supabase Auth provider chosen in D33.
- CSV / FtM-JSON data exports, self-host bundle, timeline scrubber: future directions in the design.

### What already exists

- `followthemoney` library: schemas and entity model, used at the boundary to emit FtM from typed records (R22). Reused, not rebuilt.
- nomenklatura `Resolver` / `SQLStore`: evaluated; not adopted for M1 because the design's ULID, redirect, split and reasons/evidence fields fit owned tables better (D13).
- Socrata SODA consumer API: `:updated_at`, `:id`, `$where`, `$group`, keyset paging. Used as-is.
- Postgres `pg_trgm`: name search. Cytoscape.js: graph rendering.
- Supabase: hosted Postgres 17, Supavisor pooler, backups, Auth (magic link, JWT/JWKS). Used as-is; the auto-generated Data API is deliberately closed to app tables.
- Repo: no code yet (docs/openspec only), so nothing in-repo to reuse or extract.

### Failure modes

| New path | Realistic production failure | Test / handling | User sees |
|---|---|---|---|
| nightly pull | Socrata 429/5xx mid-pagination | retry with backoff, resume from cursor (test) | nothing; data a day older |
| content hash | publisher changes a date format → millions of new hashes | > 20% alert (T3, test) | nothing wrong; maintainer alerted |
| weekly sweep | scan dies halfway, half the rows look "missing" | completeness + 1% count guard (X3, test) | nothing retracted |
| versioning | edited row loses the evidence behind a decision | versions kept, decisions cite version id (R18, test) | current value; history on request |
| ids / suspect | typo-level name variation flags real IDs | 0.80 cutoff + 200-ID calibration (R11) | correct edges; few markers |
| expand | hub with 29,697 contracts | top 50 + aggregates (R3), load test (R15) | bounded graph, drillable groups |
| moderator action | suppression doesn't reach search | same-txn rebuild (R17, integration test) | suppression immediate |
| masking | full cédula in a PNG or URL | MaskedId type + privacy suite + E2E (Q6, R14) | only ***1234 |
| rep labels | 2026 rep shown as rep "at signing" in 2018 | "reportado en SECOP" wording (R16, test) | no false dated claim |
| licensing | BY-SA notice missing on exports | notice on footer/drawer/API/PNG (R2, E2E) | attribution everywhere |
| Supabase Data API | app table reachable with the publishable key → unmasked cédulas | private schema, no grants, RLS deny-all, CI probe (R23) | nothing; request denied |
| Supabase pooler | transaction pooler hangs → expand p95 blown | R15 load test; API falls back to direct connection (R23) | normal latency |
| staging auth | JWT check accidentally enabled on production, or disabled on staging | env-driven flag with a test per environment (R24) | production anonymous; staging login |

Critical gaps (no test, no handling, silent): **0**.

### Worktree parallelization strategy

| Step | Modules touched | Depends on |
|---|---|---|
| S1 scaffold + CI + compose | repo root, `.github/`, `backend/`, `apps/web/` | — |
| S2 ids (normalize, blocklist, NIT, doc-type, suspect, MaskedId) | `backend/<pkg>/ids` | S1 |
| S3 schema + typed versioned records + identity tables | `backend/<pkg>/db` | S1 |
| S4 ingest adapters, hash, versioning, sweep, alert, token | `backend/<pkg>/ingest` | S2, S3 |
| S5 resolver + admin CLI + propagation | `backend/<pkg>/resolver` | S2, S3 |
| S6 API endpoints | `backend/<pkg>/api` | S3 (reads), S2 (masking) |
| S7 web UI (variant B) against API contract fixtures | `apps/web` | S1 (+ S6 contract) |
| S8 privacy suite + Playwright E2E + load test | `backend/tests`, `e2e/`, `load/` | S4, S5, S6, S7 |
| S9 Supabase project config + staging auth (JWT middleware, login screen, allowlist) | `infra/supabase`, `backend/<pkg>/api/auth`, `apps/web/src/auth` | S1 (+ S6 for middleware wiring) |

Lanes: A: S2 → S4. B: S3 → S5. C: S6 (after S3 lands). D: S7 (from S1, against contract fixtures). E: S9 (Supabase config from S1; auth middleware joins C once S6 exists).
Execution order: S1 first. Launch A + B + D + E. Merge S3, then start C. Merge all, then S8.
Conflict flags: S4 and S5 both write `entity`/`edge` rows; agree on one rebuild function in `backend/<pkg>/db` before both lanes start.

## Implementation Tasks
Synthesized from this review's findings. Each task derives from a specific
finding above. Run with Claude Code or Codex; checkbox as you ship.
Effort assumption: scaffolding ~100x, features ~30x, tests ~50x human/CC.

- [ ] **T1 (P1, human: ~1 day / CC: ~15 min)** — repo — Scaffold `apps/web` + `backend/` + docker-compose Postgres 16 (pg_trgm) + GitHub Actions with a Postgres service
  - Surfaced by: Scope Challenge — D4 structure; Section 3 — R13 test stack
  - Files: `backend/pyproject.toml`, `apps/web/package.json`, `docker-compose.yml`, `.github/workflows/ci.yml`
  - Verify: `pytest` and `vitest` run green in CI against the Postgres service
- [ ] **T2 (P1, human: ~2 days / CC: ~45 min)** — ids — Normalization, frequency-scanned placeholder blocklist, NIT mod-11, doc-type join rule, `MaskedId` type, Hypothesis property tests
  - Surfaced by: Scope Challenge F4; Section 2 Q6
  - Files: `backend/<pkg>/ids/`
  - Verify: `pytest backend/tests/ids` incl. property tests; top-N scan output committed as a fixture
- [ ] **T3 (P1, human: ~2 days / CC: ~1 h)** — ids — Suspect rules: self-contracting / agency NIT in person field; surname / company-name < 0.80; 200-ID labeling calibration
  - Surfaced by: Section 1 A3 (R5); Section 2 Q3 (R11)
  - Files: `backend/<pkg>/ids/suspect.py`, `backend/tests/ids/test_suspect.py`
  - Verify: unit tests for each trigger and non-trigger; calibration numbers recorded in the design doc
- [ ] **T4 (P1, human: ~3 days / CC: ~1.5 h)** — db — Typed versioned source-record tables, identity tables (judgment, cluster ULID, redirect), entity/edge tables with covering indexes
  - Surfaced by: Outside Voice X8 (R22), X4 (R18); Section 2 Q1 (R9); Section 1 A5 (R8); Section 4 P2
  - Files: `backend/<pkg>/db/`
  - Verify: migration tests on real Postgres; `EXPLAIN` shows index use for expand ordering
- [ ] **T5 (P1, human: ~4 days / CC: ~2 h)** — ingest — SECOP, RUES (reachable backfill), Grupos de Proveedores adapters; keyset paging; content hash; versioning; X-App-Token; > 20% alert
  - Surfaced by: Scope Challenge F3, F5; Section 1 A4 (R7); Outside Voice X4 (R18); T2, T3 TODO decisions
  - Files: `backend/<pkg>/ingest/`
  - Verify: integration tests: unchanged hash writes nothing; edit creates version; alert fires above 20%; header present when env set
- [ ] **T6 (P1, human: ~2 days / CC: ~1 h)** — ingest — Weekly `:id` sweep with completeness + 1% count guard and un-retract on reappearance
  - Surfaced by: Section 1 A4 (R6); Outside Voice X3
  - Files: `backend/<pkg>/ingest/sweep.py`
  - Verify: tests for complete scan, interrupted scan, count mismatch, reappearing unchanged row
- [ ] **T7 (P1, human: ~3 days / CC: ~1.5 h)** — resolver — Exact-ID judgments, union-find clusters, merge/split/redirect, admin CLI, same-transaction rebuild for every moderator action, suppression hides all edges
  - Surfaced by: Section 2 Q1 (R9); Outside Voice X2 (R17), X5 (R19); Section 1 A2 (R4)
  - Files: `backend/<pkg>/resolver/`
  - Verify: integration tests assert search/expand/counts/redirects before and after each action
- [ ] **T8 (P1, human: ~4 days / CC: ~2 h)** — api — `/search`, `/entity/:id`, `/entity/:id/expand` (top 50 + aggregates, suspect exclusion + marker), `/edge/:id/evidence` (license + attribution), rep labels, missing-date labels, plain counts
  - Surfaced by: Section 1 A1 (R3), A2 (R4); Scope F1 (R1); Outside Voice X1 (R16); Section 2 Q2 (R10), Q4 (R12); Scope F2 (R2)
  - Files: `backend/<pkg>/api/`
  - Verify: API tests on the golden fixture; privacy regex over every response
- [ ] **T9 (P1, human: ~5 days / CC: ~2 h)** — web — Variant B UI: search, entity card, Cytoscape graph with aggregate nodes, evidence drawer, BY-SA footer, PNG with notice, correction form
  - Surfaced by: Scope F2 (R2); Section 1 A1 (R3); Outside Voice X1 (R16)
  - Files: `apps/web/src/`
  - Verify: Vitest component tests; Playwright flows in T10
- [ ] **T10 (P1, human: ~2 days / CC: ~1 h)** — tests — Privacy suite (responses, URLs, IDs, PNG) + Playwright E2E for the 4 flows on the golden-case fixture
  - Surfaced by: Section 3 — R14; design privacy rule (line 114)
  - Files: `backend/tests/privacy/`, `e2e/`
  - Verify: CI job green; a planted full cédula in a fixture makes it fail
- [ ] **T11 (P1, human: ~2 h / CC: ~15 min)** — docs — Apply the accepted design amendments table above to both design doc copies; add R2/R4 items to the legal review agenda
  - Surfaced by: all approved R1-R22; Section 2 Q5
  - Files: `docs/designs/follow-the-money.md`, `~/.gstack/projects/follow-the-money/tru-master-design-20260925-050150.md`
  - Verify: re-read both copies; no "rep at signing", "hub", or materialized-view wording left
- [ ] **T12 (P2, human: ~1 day / CC: ~30 min)** — perf — Load script (k6 or Locust): 1,000 expands across degree buckets incl. top-100 hubs; p95 gate
  - Surfaced by: Section 4 P1 (R15)
  - Files: `load/`
  - Verify: staging run report shows p95 ≤ 300 ms search, ≤ 500 ms expand
- [ ] **T13 (P2, human: ~1 day / CC: ~30 min)** — ops — Pre-widening ops runbook + metrics script; set the monthly hosting cap
  - Surfaced by: Outside Voice X6 (R20)
  - Files: `docs/ops/widening-gate.md`, `backend/<pkg>/ops/metrics.py`
  - Verify: recorded ingest/sweep/restore times and monthly cost vs cap
- [ ] **T14 (P2, human: ~1 day / CC: ~15 min)** — launch — Typical-task test protocol + random contractor sampling script
  - Surfaced by: Outside Voice X7 (R21)
  - Files: `docs/launch/typical-task-test.md`, `backend/<pkg>/ops/sample_contractors.py`
  - Verify: 15 tasks run; ≥ 70% pass recorded
- [ ] **T15 (P1, human: ~1 day / CC: ~30 min)** — infra — Supabase projects (staging, prod): Postgres 17, env config (`DATABASE_URL`, `DATABASE_POOL_URL`), private `ftm` schema, Data API closed, RLS deny-all in `public`, `.env.example`, CI Data API probe
  - Surfaced by: Late change — R23; finding L1
  - Files: `infra/supabase/`, `.env.example`, `backend/<pkg>/settings.py`, `.github/workflows/ci.yml`
  - Verify: CI probe with the publishable key returns no rows/denied for every app table
- [ ] **T16 (P1, human: ~2 days / CC: ~1 h)** — auth — Staging lock: Supabase Auth magic link + email allowlist, FastAPI JWT (JWKS) middleware enabled by env on staging only, web login screen on staging
  - Surfaced by: Late change — R24 (D33)
  - Files: `backend/<pkg>/api/auth.py`, `apps/web/src/auth/`
  - Verify: tests for 401/403/200 on staging config; production config serves anonymously

### Unresolved decisions

None. Every choice in this review has an actual answer.

### Completion summary
- Step 0: Scope Challenge — scope reduced per recommendation (M1 only; one Python backend)
- Architecture Review: 5 issues found (+2 from the late auth/hosting change: L1 Data API exposure, L2 pooler latency)
- Code Quality Review: 4 issues found (+1 factual correction, +1 carried requirement)
- Test Review: diagram produced, 71 planned gaps identified (greenfield); 2 tooling issues resolved (R13, R14)
- Performance Review: 2 issues found (+1 carried index requirement, +1 informational)
- NOT in scope: written
- What already exists: written
- TODOS.md updates: 5 items proposed to user (3 added to TODOS.md, 2 built into M1 scope)
- Failure modes: 0 critical gaps flagged
- Unresolved decisions: 0 in this review
- Outside voice: codex, completed (8 findings, all resolved: 8 applied)
- Parallelization: 5 lanes, 4 parallel / 1 sequential (C waits on B's schema)
- Late change: Supabase Postgres hosting (R23, user-directed) and Supabase Auth staging lock (R24, D33 A) folded in
- Lake Score: 14/15 (picked the most complete option on 14 of 15 coverage choices; D5 took A=8 over B=9)

### Suppressed findings (appendix)

- (confidence 4/10) Search by full cédula plus per-IP rate limiting allows cédula→name enumeration. Suppressed: SECOP already publishes the same pairs openly, so the site adds no new exposure.
- (confidence 4/10) `statement` partitioning by dataset (design line 170) gives SECOP one huge partition. Moot after R22 (typed records).

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 0 | — | — |
| Outside Review | codex CLI via `/plan-eng-review` outside voice | Independent 2nd opinion | 1 | completed (issues_found) | 8 findings, all resolved (8 applied) |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 2 | ISSUES OPEN (PLAN, this run, refreshed after late change) | 18 issues, 0 critical gaps |
| Design Review | `/plan-design-review` | UI/UX gaps | 0 | — | — |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | — |

- **OUTSIDE COVERAGE:** codex, phase plan-review, completed 2026-09-25, 8 findings; all resolved through D20-D26 or carried (X3). The late auth/hosting change (R23, R24) had no outside-voice pass. Prior: codex, phase office-hours, completed.
- **CROSS-MODEL:** Claude (host) and codex (outside, model gpt-6-astra) overlapped on refresh lifecycle and national-scale risk; codex alone raised the rep-at-signing semantics, which a Claude probe then supported. No unresolved disagreement.
- **VERDICT:** No review CLEAR yet. Eng Review found 18 issues, all mapped to approved decisions and Implementation Tasks T1-T16 (issues_open = mapped work, not open questions); eng review required.

NO UNRESOLVED DECISIONS
