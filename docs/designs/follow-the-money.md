# Design: Cabos Sueltos — investigation graph for Colombian public money

Generated by /office-hours on 2026-09-25
Branch: master
Repo: follow-the-money (local, open-source monorepo planned)
Status: APPROVED
Mode: Builder
Amended: 2026-09-25 by /plan-eng-review (R1-R24), /plan-design-review (23 decisions), /design-consultation (name, `DESIGN.md`) and the /plan-eng-review re-review (R25-R45, ledger at the end of this file). This file is the current M1 plan. The review reports in `docs/reviews/` are history, and the gstack copy (`~/.gstack/projects/follow-the-money/tru-master-design-20260925-050150.md`) is kept in sync with this file (image paths differ). Product name: **Cabos Sueltos**; slug `cabosueltos` (package, org, domains); the repo is still named `follow-the-money` until renamed (TODOS.md).

## Problem Statement

Corruption in Colombia is documented in public records, but the records are scattered across SECOP II, RUES, Cuentas Claras, officials' declarations, and international datasets that nobody joins. Journalists and activists who want to know "who is who, who runs what, who is behind what and how are they connected" have to spend days stitching CSVs and portals together by hand.

Cabos Sueltos is a public, open-source website: search a name, NIT, or cédula → land on an entity page → navigate a relationship graph of contracts, companies, legal representatives, approving officials, campaign donations, and international links. It borrows wallet-tracing UX from crypto research (Arkham) and goes further: cross-dataset leads that are probabilistic, explained, and verifiable.

Audience: investigative journalists and anti-corruption activists, Colombia first.

## What Makes This Cool

- **Colombian public data already carries the "wallet addresses."** SECOP II contract rows include the contractor's document number, the legal representative's ID, the *ordenador del gasto*'s cédula, and the supervisor's cédula. RUES carries the legal rep ID. Joins that are fuzzy guesswork in most countries are validated key joins here.
- **Hypothesis mode.** Name-only matches (e.g. ICIJ Offshore Leaks) appear as graded leads with reasons. A viewer toggles "asumir misma persona," the graph lights up with everything that follows, and a persistent banner lists every assumption in play. Toggle it off and it's gone. A guess never silently becomes a published fact.
- **Every edge opens its evidence.** Click any line → the source record(s), date, and a link labeled for what the source actually is (official registry, leak database, document page).

## Constraints

- Public hosted site; self-host (public-data stack only) later from the same code.
- Open-source monorepo. Colombia first; other countries later as adapters.
- Legal exposure is real: Colombian defamation claims, and habeas data under Ley 1581/2012 (data-subject rights to access, rectify, and delete; reclamos answered within 15 business days).
- Data licenses differ per source (see Licensing).
- DIAN's beneficial-owner registry (RUB) is not public. RUES open data has no shareholders and is a **current snapshot**.

## Premises (agreed)

1. **Don't rebuild Aleph.** A public, pre-loaded site where the mental map and the UX are the product. FollowTheMoney (FtM) is the data model (interop with OpenAleph, OpenSanctions, Gephi, Neo4j).
2. **Source classes are pluggable adapters:** (a) ID-keyed registries: SECOP II, RUES, Cuentas Claras, Función Pública PEPs, OFAC SDN (Colombian entries), GLEIF; (b) name-only structured data: ICIJ Offshore Leaks in v1, with UK PSC, debarment lists, and Wikidata later; (c) unstructured document dumps: future direction.
3. **Identity is a separate layer from facts** (revised after the Codex challenge). Sourced assertions are stored apart from identity judgments. Leads are graded **strong / moderate / weak with listed reasons**, never shown as a percentage until calibrated.
4. **Every edge cites its source record(s).**
5. **Leads, not verdicts.** Every identity link is one of three visibly distinct classes: **exact-ID** (validated ID join), **confirmado por revisión editorial** (a moderator-promoted judgment backed by an identity-establishing document), or **pista, requiere verificación** (everything else). Only the second class may use the word "confirmado". A correction flow exists from the first public day. No "corrupt" labels.
6. **Cédulas of natural persons, and natural-person NITs, are masked everywhere** (tightened during spec review from "private citizens only"; see Privacy rules).
7. **Licensing:** primary sources first; each source's redistribution terms are honored per output channel.

## Landscape (verified 2026-09-25)

| Source | Size / facts | Join key |
|---|---|---|
| SECOP II Contratos (datos.gov.co `jbjy-vk9h`, Socrata) | 6,070,870 contracts; contractor doc, legal rep ID *as reported in SECOP* (not dated to signing; `Sin Descripcion` on 67% of company-contractor contracts), ordenador del gasto cédula, supervisor cédula, `URLProceso`, signing date (NULL on 7%), contract value (contains typo outliers up to 8.8e20 COP) | NIT / cédula |
| SECOP II Grupos de Proveedores (`ceth-n4bn`) | Unión temporal / consorcio members: `nit_participante`, `participacion`, `es_lider_del_grupo` | NIT |
| RUES (datos.gov.co `c82u-588k`) | 9,407,309 merchants/companies; NIT, dates, status, *current* legal rep ID | NIT / cédula |
| Cuentas Claras (CNE) | Campaign donors 2015–2026, open formats | ID coverage to verify |
| Función Pública PEPs (OpenSanctions `co_funcion_publica`) | 18,682 persons with national ID, 6,795 positions | cédula |
| OFAC SDN | 437 Colombia-linked entities, 420 with cédula/NIT | cédula / NIT |
| GLEIF | 1,133 Colombian LEIs; `registeredAs` = NIT; foreign parents | NIT |
| ICIJ Offshore Leaks | Offshore entities, officers, intermediaries, addresses | name only → candidates |

Prior art: OCCRP ended open-source Aleph maintenance in Dec 2025 (Aleph Pro is closed; OpenAleph is the DARC fork). Transparencia por Colombia's Monitor Ciudadano cross-matched donors to contracts for 2018–2022 as CSV (24k rows) with no graph, search, or API. PACO publishes contractor red flags. Nobody offers a public graph investigation experience over this data.

## Cross-Model Perspective (Codex cold read)

- **Coolest version:** a living investigation you can branch, challenge, and replay: hypothesis branching, time travel, watched investigations, exportable evidence trails that include the unresolved assumptions.
- **Key insight:** Codex picked out the user's own line, "that's where things can get interesting. that's where patterns emerge", as the tell. Its own take was that the excitement lies in crossing between disconnected datasets, and that the breakthrough is helping researchers choose their next verification step.
- **Reuse:** FtM + nomenklatura (same/different/undecided judgments) as the foundation; yente later; Cytoscape.js first, Sigma.js benchmark later; Postgres before any graph DB; Splink only later.
- **Challenged premise #3** (a single confidence number per edge) → accepted and revised.
- **Weekend suggestion** (a SECOP-only shared-legal-representative explorer): adopted as M1's *scope*, built on Approach B's statement/identity model. We keep one name-only source (ICIJ) inside v1 at M3, because cross-dataset discovery is the core motivation.

## Approaches Considered

- **A: SECOP Rep Explorer on plain relationship tables.** Rejected *for its data model only*: hypothesis mode would force a rewrite. Its user-facing scope lives on as M1.
- **C: OpenAleph backend + custom front-end.** Rejected: heavy Elasticsearch/worker ops for a public site, cross-referencing that isn't hypothesis-aware, and coupling to a small-org fork. Revisit only as a document-extraction backend if document dumps are ever taken on.

## Recommended Approach: B, FtM statement engine + hypothesis UX (staged)

**v1 = M1 + M2 + M3.** M4+ are future directions outside this approval. The eng reviews lock **M1 only**; M2 and M3 each get their own eng review before work starts (eng D3, TODOS.md).

### Data model

- Every adapter writes **typed, versioned source-record tables** per dataset (`secop_contract`, `rues_record`, `group_member`) with a content hash, `valid_from/valid_to`, `observed_at` and `retracted_at`. An unchanged hash writes nothing; an edited row closes the old version and inserts a new one (R7, R18). FtM entities are generated at the boundary for interop, not stored as the primary model (R22). Only the properties the product uses are ingested (~20 per SECOP contract, not all ~90 columns).
- **Contract values.** At ingest `valor_atipico` is set when a value is above COP 20 billones, or a natural-person (`tipodocproveedor` = CC on the same row) "Prestación de servicios" contract is above COP 5.000 millones; thresholds are checked on the 200 highest-value contracts per type before widening. Flagged values are excluded from sums and sorted after valued rows; the drawer shows the raw value with "Valor reportado en SECOP fuera del rango habitual; verifíquelo en el registro oficial". A reported 0 shows as "COP 0"; an empty value as "sin valor reportado" (R29, R43).
- **Time semantics.** `valid_from/valid_to` come from the source only when it has real dates. `observed_at` is the ingestion cutoff. SECOP's legal-rep field is labeled **"representante legal reportado en SECOP"** and is never presented as the rep *at signing* (R1, R16). RUES's current rep is labeled **"vigente a <fecha de corte>"** and never back-dated. Both are traversable in the "legal rep → other contractors" hop as two distinct edge kinds. A missing `fecha_de_firma` stays NULL and shows "fecha de firma no reportada"; ordering falls back to start date, then last update (R10).
- **ID validation before any exact-ID join:**
  1. Placeholder blocklist (`0`, repeated digits, `999999999`, "No Definido", "N/A", `Sin Descripcion`, `No definido` in any case, 1-3 digit values, all-zero and punctuation-only values); the final list comes from a frequency scan.
  2. NIT check-digit validation (DIAN modulo-11), with NITs normalized with and without the check digit.
  3. Document-type consistency: a **legal-entity NIT never joins a cédula**. A **natural-person NIT** whose base equals a cédula and whose check digit validates *may* join that cédula when the person-type signals agree (RUES `organizacion_juridica` = persona natural, and/or SECOP `TipoDocProveedor`), because SECOP records the same natural-person contractor as either CC or NIT.
  4. Suspect-ID detection. An ID is marked *suspect* and queued for review only when it looks like a data-entry error: an agency NIT appears as the contractor on its own contract or in a natural-person field (legal rep, ordenador, supervisor) (R5); or the names attached to one ID disagree, similarity < 0.80 on the surname (persons) or the legal-form-stripped name (companies), calibrated on 200 labeled IDs (R11). Public-entity contractors are normal exact edges. Prolific IDs with consistent names keep their exact edges and are shown with plain counts, never a "hub" label (R12). **In M1 suspect edges are hidden:** no line, no table row, no drawer evidence, not in the PNG; the focus card shows a neutral chip "N vínculo(s) en revisión" with the counterpart unnamed (R4). The review queue is best-effort; Ley 1581 deadlines take priority.
  5. Exact-ID matches to **OFAC or PEP** entries also require name agreement; without it they stay a lead (see the ID-based grade rows).
- **Identity layer** (nomenklatura-style). Judgments `positive | negative | unsure` with `origin: exact_id | matcher | reviewer`, `reasons[]`, `grade`, `author`, `timestamp`, `evidence_refs[]`.
- **Canonical graph = sourced statements + validated exact-ID judgments + editorially promoted judgments** (the third class is stored and rendered with its own label, see Premise 5). Matcher candidates and unpromoted reviewer judgments never enter it.
- **Stable IDs.** Each cluster gets an opaque random ID (ULID), **never derived from a cédula/NIT** (a hash of a 10-digit cédula is brute-forceable). Merges keep one ID and redirect the others (redirect table; the web answers a merged URL with a 302, never a cached 301, R41). Splits: the largest fragment keeps the ID, the others get new IDs, and the old URL shows "esta entidad fue dividida" with a chooser.
- **Owned identity tables in M1** (judgment, cluster ULID, redirect), not nomenklatura (R9). Every moderator action rebuilds the affected entity/edge/search rows, counts and redirects in its own transaction (R17).

### Hypotheses vs judgments (two tiers)

| Tier | Created by | Stored | Visible to |
|---|---|---|---|
| **Viewer hypothesis** | Anyone, via the "asumir misma persona" toggle on a lead | URL only (opaque candidate IDs; no server state) | Whoever holds the link |
| **Reviewer judgment** | Registered researcher: "coincide / no coincide" + evidence note | Postgres, attributable, audit-logged | Private to the researcher until they publish it. Once published, shown on the lead as "2 revisores la consideran coincidente; sigue siendo pista" (never "confirmada"), and never as a solid canonical edge |

- **Reviewer identity in public.** Published judgments show the reviewer's **organization or a pseudonym**. The real identity is visible only to moderators. This protects journalists from retaliation and avoids tipping off subjects. Covered by the M3 legal review.

- **Promotion to canonical** happens only when a moderator approves a reviewer-confirmed lead **and** the confirmation cites a document that establishes identity (e.g. a notarized document or a court record). It is shown with a distinct edge style, "confirmado por revisión editorial (<fecha>)", which is different from exact-ID. It is revertible with an audit trail.
- **Hypothesis banner.** Any view with active hypotheses shows a persistent, non-dismissable banner listing each assumption and its grade. Image downloads from that view embed the same list.
- **Exports in v1:** only the **PNG image download** of the current graph view (M1), with source attribution and, when applicable, the hypothesis list. CSV/FtM-JSON data exports are a future direction.
- **Overlay mechanism.** A query-time union-find over canonical cluster IDs plus the active hypothesis pairs. Limits: at most 5 active hypotheses and 2 hops of expansion from the focus entity. Target p95 ≤ 800 ms.

### Roles

| Role | Milestone | Can |
|---|---|---|
| Anonymous viewer | M1 | Search, browse, view evidence, use viewer hypotheses (M3), submit corrections |
| Registered researcher | M3 | Everything above + persisted confirm/reject judgments. Accounts are invite-based and vetted by a moderator (journalists and NGOs first) |
| Moderator | M1 (maintainer via admin CLI); web UI at M3 | Handle corrections, suppressions, suspect-ID reviews, promotions |

Staging is gated by Supabase Auth (magic link + email allowlist; FastAPI verifies JWTs on staging only). Production is anonymous (R24).

### Privacy rules

- **Masking.** Every natural person's cédula **and natural-person NIT** is shown as `***` + the last 4 digits in the UI, API, evidence drawer, statement values, image downloads, and URLs. Officials are identified by name + position + dates, which doesn't need the full number. Only **legal-entity** NITs are shown in full.
- **Person-type classification** (in order): RUES `organizacion_juridica`; SECOP `TipoDocProveedor` / `Tipo de Identificación Representante Legal`; FtM schema (Person vs Company) from the source. When unknown, treat as a natural person and mask.
- **Search by full cédula** is allowed (journalists often hold IDs from documents). It returns the entity with its ID masked. This only confirms what SECOP already publishes about that person, and it's rate-limited per IP.
- **Minimization.** From RUES, only entities reachable from the other sources are ingested (contractors, legal reps, donors, PEP-linked companies, and their reps). Not the full 9.4M registry. Each refresh runs a **reachability backfill**: IDs newly reachable from new SECOP/CNE records are fetched from RUES by NIT/ID (Socrata `$where` in batches), and RUES rows that are no longer reachable are deleted.
- **Automated tests.** No response body, URL, entity ID, or image download contains a natural person's full ID number, whether bare digits or NIT-formatted with dots, a hyphen, and a check digit (regex check over the fixture corpus plus a property test on the ID generator).

### Correction / habeas-data flow (live from the first public day)

- **Intake:** a "Solicitar corrección" link on every entity page → a form plus a dedicated email address. The form posts to `POST /api/correcciones` (type, entity ULID, name, email, message; no uploads) and returns a ticket code `CS-<año>-<secuencia>`. Per-IP limit 5/hour plus a honeypot, no third-party captcha. The maintainer gets a transactional email (retried on failure; the ticket is kept) and lists open tickets by deadline with `tickets --vencen` (R33).
- **Deadlines:** computed in Colombian business days (holiday calendar). The confirmation shows the legal term ("10 / 15 días hábiles desde la recepción de la solicitud completa") and a "fecha estimada"; the maintainer sets `completa_desde` when the request is complete and the deadline is recomputed; an incomplete reclamo is `pendiente_de_completar` with its clock paused. The clock-start rule per type is on the M1 legal-review agenda (R39).
- **Requester data:** the form shows an "aviso de privacidad" with a required authorization checkbox and asks people not to include document numbers. Tickets live in the private `ftm` schema, readable only through the admin CLI, and are deleted a fixed period after closure; the period and notice text come from the M1 legal review (R38).
- **Owner:** the maintainer (moderator role). **Response target:** within 15 business days, per Ley 1581.
- **Request types:** *consulta* (access: "what do you hold about me?") with a **10-business-day** target, and *reclamo* (rectify/suppress) with a **15-business-day** target, per Ley 1581.
- **Requester verification.** Access, rectification, and suppression require a signed request plus a copy of the requester's ID (or a power of attorney for a representative), handled out of band and never stored in the main database. An **unverified** requester can still get (a) an annotation or (d) a referral to the source agency.
- **Access response:** every statement, identity judgment, and lead linked to the person, including hidden and weak leads and reviewer judgments (with reviewers pseudonymized).
- **Outcomes:** (a) annotate the entity with the requester's statement; (b) hide or reject an identity judgment or lead; (c) suppress public display of a natural person; (d) refer the requester to the source agency when the underlying official record is wrong; (e) **deny, with a stated reason**.
- **Suppression criteria.** Records of a natural person's **public role** are not suppressed but can be annotated: contractor, legal rep of a contractor (as reported in SECOP or RUES), ordenador del gasto, supervisor, campaign donor, PEP, or sanctioned party. **Incidental persons** can be suppressed: reachable only through a RUES snapshot, with no public-role record. Leads about any person can always be hidden (b).
- **Suppressed rendering:** no node, no placeholder and no edges through a suppressed person, so companies that shared that person are no longer linked through them (R19). Search by that person's name or cédula returns nothing, and their old URL is identical to a nonexistent one: same body, same 404 (design 8A, R27). Inside a shared link's trail they are dropped exactly like a vanished node (R34). The suppression record is kept internally so re-ingestion doesn't bring the person back. Golden-case fixtures must use non-suppressed records.
- Each outcome is logged with its date. Affected pages show a neutral notice: "Parte de esta información fue anotada o limitada a solicitud del titular (Ley 1581 de 2012)."
- The flow, including the suppression criteria, is part of the M1 legal-review gate.

### Leads: visibility, grading, candidates

- **Default visibility.** Leads are **hidden by default**. The viewer turns on "Mostrar pistas" on an entity, which lists strong and moderate leads. A second toggle, **"Incluir pistas débiles"**, only available once "Mostrar pistas" is on, adds weak leads and shows a warning ("coincidencia solo por nombre; alta probabilidad de homónimo"). Any listed lead can then be toggled with "asumir misma persona." Leads are never included in search results or server-rendered, indexable HTML (lead views are `noindex`).
- **Name similarity function.** Normalization: ASCII-fold, lowercase, drop particles (de, del, la, las, los, y), split into given-name tokens and surname tokens (using the source's field structure when present, otherwise the Colombian convention: the last two tokens are surnames). Score: the mean Jaro-Winkler of the best one-to-one token alignment, computed separately for surnames and given names and weighted 0.6 / 0.4. A missing second surname on one side is not penalized, but the first surname must align at ≥ 0.9. The thresholds below are validated and tuned on the pre-M3 ICIJ sample run.
- **Grade rules (initial).** Corroborating attributes are birth year/month, address, a co-linked entity, and nationality *only when it comes from a field independent of the blocking key* (e.g. birth country). **Any attribute used for blocking never counts as corroboration.**

  | Basis | Condition | Grade | Reason shown |
  |---|---|---|---|
  | Name | similarity ≥ 0.92 + ≥ 2 corroborating attributes | strong | the listed attributes |
  | Name | similarity ≥ 0.92 + 1 corroborating attribute | moderate | the attribute |
  | Name | similarity ≥ 0.95, no corroboration | weak | "solo nombre" |
  | ID | OFAC/PEP exact ID, name disagrees | strong | "mismo número de identificación, nombre distinto" |
  | ID | suspect ID (agency NIT / inconsistent names), pending review | moderate | "identificador sospechoso, en revisión" |

  Anything below these rows is not stored.
- **Candidate generation.** Blocking on normalized first-surname + second-surname-or-first-given-name tokens, plus country. Every candidate in a block is scored and graded first, then ordered by grade, then corroboration count, then name similarity, and only then **truncated to 10 per source record**. Records that hit the cap are logged. Before M3, a sample run on ICIJ's Colombia-linked records measures volume, grade distribution, and the cap-hit rate.
- **Path ranking (reserved for the future path-search feature):** weakest-link grade, then the number of non-exact edges, then path length. There are no multiplied "probabilities."

### Evidence drawer by source class

| Source class | Link label |
|---|---|
| Colombian government registry (SECOP, RUES, CNE, Función Pública) | "Ver registro oficial ↗" |
| Foreign official list (OFAC, GLEIF) | "Ver en <fuente> ↗" |
| Leak database (ICIJ) | "Ver en ICIJ Offshore Leaks ↗ (filtración, no registro oficial)" |
| Derived edge (e.g. shared legal rep) | Lists **every** contributing record, each with its own link |
| Published reviewer judgment | Reviewer organization/pseudonym, date, and the cited evidence document |
| Editorial promotion | "Confirmado por revisión editorial", date, and the identity-establishing document |

### Group contractors (unión temporal / consorcio)

- Node type `ContractorGroup` (FtM Organization with a group flag).
- Members come from SECOP II Grupos de Proveedores (`ceth-n4bn`: `nit_participante`, `participacion`, `es_lider_del_grupo`); group → member edges carry member IDs (Open Question 2, answered). The hand trace verifies that `nit_grupo` equals the contract's `documento_proveedor`.
- Groups with no member rows show **"miembros no disponibles en datos abiertos"** and still expand through their legal rep, ordenador del gasto, and contracts.

### Stack

- `apps/web`: React + TypeScript + Vite, Cytoscape.js with fcose (pinned nodes on expand). The graph module is a dynamic import: loaded at once on ≥ 768px, and under 768px only when the "Grafo" tab opens or a link opens on it (R35). `docs/designs/assets/m1-entity-graph/entity-page.html` is the visual and interaction spec, not code to port; no Pretext dependency (R31).
- `backend/`: one Python project, package `cabosueltos` (R26), modules `ids` (normalize, validate, mask), `ingest`, `resolver`, `api`, `db`; two entrypoints: the FastAPI server and the ingest/admin CLI (eng D4).
- **Serving:** FastAPI serves the API and Vite's build output from one origin. `/entidad/:ulid` does one indexed lookup: found 200, merged 302 (`Cache-Control: no-store`, query string copied), split 200 with the chooser, not found or suppressed 404 with the identical body. CSP (`font-src 'self'`, no third-party origins) and `X-Robots-Tag` are set there. Local dev uses the Vite dev server with an `/api` proxy (R27, R41).
- **Postgres:** Supabase-hosted Postgres 17, configured by env (`DATABASE_URL` direct for ingest/migrations/CLI, `DATABASE_POOL_URL` for the API). App tables live in a private `ftm` schema closed to the Data API; RLS deny-all on anything in `public`; a CI probe asserts the publishable key reads nothing (R23). Tables: typed versioned source records, judgment / cluster / redirect, plain direct-edge `entity` / `edge` tables written in the ingest transaction (shared-rep computed at query time), review queue, suppression, correction tickets; `pg_trgm` for search; covering index for expand ordering (R8).
- **Refresh:** nightly keyset paging on `:updated_at`, `X-App-Token` from env, content hash, versioning, > 20% changed-rows alert, weekly `:id` sweep with a completeness guard and un-retract (R6, R7, R18, T2, T3).
- **Fonts:** a build script fetches pinned Cabinet Grotesk and General Sans WOFF2 files into gitignored `apps/web/public/fonts/`, from the CI cache, then a private project store, then Fontshare, each checked against a committed SHA-256 manifest. A production build fails if no source verifies. The app awaits `document.fonts.load()` before the first graph draw and PNG export. Images containing the fonts go only to a private registry (R30, R44, `DESIGN.md`).

### Sizing (estimate, to be re-measured on the M1 cohort before widening)

The figures below were estimated as FtM statements. M1 stores typed records (R22), so the cohort measures bytes per record, and the pre-widening ops gate (R20) decides go/no-go.


- SECOP II: 6.07M contracts × ~20 ingested props ≈ **120M statements**.
- Minimized RUES: ~0.5–1M entities × ~12 props ≈ **6–12M statements**.
- M2 sources: < 5M statements.
- Total ≈ 130–140M statements, roughly 60–120 GB including indexes. That's one managed Postgres instance; refreshes are incremental.
- Go/no-go: extrapolate from the M1 cohort's measured bytes per statement.

### Visual direction

- **Design system:** `DESIGN.md` (Azul carbón palette, Cabinet Grotesk + General Sans + JetBrains Mono, graph grammar, light print theme for PNGs). Read it before any UI work.
- **References:** `assets/m1-entity-graph/m1-desktop-v2.png` and `m1-mobile-v2.png` (layout; colors superseded by `DESIGN.md`), and `assets/m1-entity-graph/entity-page.html` (visual and interaction spec, fictional data). The office-hours variant B image below is history.

![Office-hours mockup: variant B, Arkham terminal (superseded)](assets/follow-the-money-mockup-variant-B.png)

- **Screens (design review 1A):** `/` search-first home, `/buscar` results with disambiguation (full-ID match jumps to the entity), `/entidad/:ulid` for company, person and agency, split chooser, `/correccion`, `/metodologia`, staging `/login`.
- **Entity page:** header strip with Copiar enlace, Descargar PNG and "Solicitar corrección" (3A); card with plain counts and the finding line "Comparte representante legal con N empresas" (4A); breadcrumb trail, Deshacer and Volver al foco; graph; Conexiones table under the graph; evidence drawer (evidence only). Graph draws 8 per kind with a "+N · ver en tabla" pill; the table lists every result, 50 per page (9A).
- **Graph grammar (15A):** contract edges grouped per agency with count and value; rep reportado en SECOP solid 3px + arrow; rep vigente (RUES) long dash + arrow; ordenador/supervisor dotted, attached on the agency side (6A); group member double line; shapes square / circle / rounded diamond / hexagon; distinguishable in grayscale; legend always visible. Selection is the only emphasis (12A).
- **Interaction (18A, 19A):** click selects, "+" or "Expandir" expands with a kind filter; graph, table and drawer selection stay in sync. The URL holds the full view: focus, trail with each step's kinds, selection, table sort/filter, mobile tab; never an ID number (R45). A shared link reopens through one `/api/vista` request (R34, R40).
- **States (7A, 8A), drawer (10A), citations (11A), motion (13A), responsive (16A: ≥ 1280 / 768-1279 / < 768 with tabs and bottom sheet), accessibility (17A: WCAG 2.2 AA, the table is the accessible equivalent, axe in CI), PNG on the print theme (20A), Spanish-only with i18n from the first commit (22):** as specified in `docs/reviews/design-review-2026-09-25.md`.
- **Guardrails:** a lead never says "verificada"; neutral icons, not official crests; no verdict words.

### Milestones and gates

- **M1: SECOP II + RUES explorer.** The first ingest cohort is **derived from the golden-case trace** (every record in the trace, plus the agency's contracts for the same two years). Search (name/NIT/cédula with disambiguation) → entity page → expand contractor → legal rep (reportado en SECOP / vigente en RUES) → other contractors → contracts/agencies → ordenador del gasto. Evidence drawer, shareable URLs, masking, correction flow. Deployed to a **private staging URL** during development. Once the cohort works, ingest is **widened to all of SECOP II plus reachable RUES** before launch.
  - **Public launch gate (end of M1):** national SECOP II + reachable RUES coverage is live; a Colombian press-freedom lawyer (e.g. FLIP) reviews masking, wording, the correction flow, and the suppression criteria; the Sunday test passes; search p95 ≤ 300 ms and expand p95 ≤ 500 ms on the load test (R15); the pre-widening ops gate is met (ingest, sweep and restore times, monthly cost under the cap; R20); and the typical-task test passes on ≥ 70% of 15 randomly sampled tasks (R21). The legal review also covers CC BY-SA share-alike and BY-SA × BY-NC × ODbL (R2), the "en revisión" and atypical-value wording (R4, R29), person-page indexing (R36), the correction-form notice and retention (R38), and the deadline clock (R39). For the Sunday test, someone other than the tester picks a contractor from the ingested data that is known to have a documented shared legal representative.
- **M2: remaining ID-keyed sources.** Cuentas Claras, Función Pública PEPs, OFAC SDN, GLEIF.
  - *Exit:* every source passes ID validation with a reviewed suspect-ID report, and the golden case gains at least one cross-source hop (e.g. rep → donation → candidate) with evidence.
- **M3: ICIJ leads + hypothesis mode + researcher accounts.**
  - *Gate:* a second legal review, covering lead display, reviewer-annotation wording, and public reviewer identity (organization/pseudonym), before M3 features go public.
- **Future directions (not approved here):** path search ("¿cómo se conectan A y B?", using the path-ranking rule above); CSV/FtM-JSON data exports; a timeline scrubber (limited to relationships with real historical dates, with the limit stated in the UI); watched investigations and change reports; UK PSC, debarment lists, and Wikidata; a private-data self-host mode; document dumps (only where they connect to Colombian public money, e.g. Colombian court records).

## Licensing

| Source | License / terms | API + public site | Image downloads (v1's only export) | Self-host bundle |
|---|---|---|---|---|
| SECOP II, RUES, Grupos de Proveedores (datos.gov.co) | **CC BY-SA 4.0** (verified 2026-09-25) | Yes; notice on the footer, drawer, API and citations (R2) | Yes; notice in the PNG footer (R2) | Yes, share-alike |
| Cuentas Claras (CNE) | to verify in the M2 review | Yes, with attribution | Yes, with attribution | Yes |
| OFAC SDN | US public domain | Yes | Yes | Yes |
| GLEIF | CC0 | Yes | Yes | Yes |
| Función Pública PEPs via OpenSanctions | CC BY-NC 4.0 | Yes (nonprofit public instance), attributed | Non-commercial notice attached | **Excluded**; the operator fetches it under their own license (or scrapes the primary source) |
| ICIJ Offshore Leaks | ODbL (+ CC BY-SA for content) | Yes, attributed | ICIJ-derived data carries the ODbL share-alike notice | Yes, with the ODbL notice |

- **Code license: MIT** (chosen by the user during review). Code is maximally reusable; hosted forks are not required to publish modifications. Data licenses are separate and documented in `DATA_LICENSES.md`. Data files are never committed to the repo, so NC/ODbL terms never attach to the code.

## Open Questions

1. **Cuentas Claras ID coverage:** do donor records carry cédula/NIT consistently?
2. ~~SECOP group membership~~ Answered: SECOP II Grupos de Proveedores (`ceth-n4bn`) lists members with IDs; the hand trace confirms the join key.
3. ~~Naming~~ Answered: **Cabos Sueltos**, slug `cabosueltos` (R26). Claiming the domains, org and PyPI name is a P1 TODO.
4. **Hosting and abuse:** hosting budget; rate limits; a policy for handling pressure or takedown requests.
5. **Florida Sunbiz / Panama registry:** bulk availability, for a future adapter.
6. **Calibration:** the size of the labeled set of reviewer judgments needed before grades can become honest probabilities.
7. ~~datos.gov.co license~~ Answered: CC BY-SA 4.0 for SECOP II, RUES and Grupos de Proveedores. Compatibility with CC BY-NC and ODbL sources is on the M1 legal review (R2).

## Success Criteria

- **M1 typical-task test:** ≥ 70% of 15 tasks on randomly sampled contractors pass (R21).
- **M1 Sunday test:** a journalist given an unfamiliar contractor finds a documented shared legal representative and opens the supporting SECOP record **within 2 minutes**.
- The golden case is reproduced by the tool, with every edge citing its source record(s).
- The privacy test suite passes: no full natural-person ID (cédula or natural-person NIT, in any formatting) appears in any response, URL, entity ID, or image download.
- **M2 exit criteria** as listed above.
- **M3 drawer usability:** in 3 of 3 journalist sessions, a verify-or-reject decision is reached from the evidence drawer alone.
- **M3 discovery value:** on a reviewed sample of 30 strong leads, ≥ 50% are confirmed true, and at least one confirmed lead did not appear in a news-archive search (Google News + El Tiempo, El Espectador, La Silla Vacía, Cuestión Pública) at the time of review. **"Confirmed" means the promotion standard:** a cited identity-establishing document, checked by the moderator **and** one external journalist. If fewer than 30 strong leads exist, all of them are reviewed (minimum 10). With fewer than 10, the result is reported as inconclusive and the grade thresholds are revisited.

## Distribution Plan

- Public web service (single hosted instance): FastAPI serves the API and the web build from one origin (R27), on Supabase Postgres (R23). GitHub Actions deploys from `main` to staging. Production is promoted manually after the M1 launch gate. Ingest jobs run on a schedule.
- Public GitHub monorepo, MIT, with `DATA_LICENSES.md`.
- A self-host docker-compose (public-data stack only, OpenSanctions-derived data excluded) after M2. It builds the web image locally, fetching the fonts itself, because the font license forbids publishing them (R30).

## Next Steps

1. Do the Assignment (the hand trace). It defines the golden fixture and the first cohort, and confirms the Grupos de Proveedores join key.
2. Claim the name (`cabosueltos` domains, GitHub org, PyPI) and resolve the correction-flow TODOs before booking the M1 legal review (TODOS.md).
3. Build M1 from the Implementation Tasks of both eng reviews (this file's review section and `docs/reviews/eng-review-2026-09-25.md`): scaffold, ids, db, ingest, resolver, API, web, tests, infra.
4. Deploy to staging behind the Supabase Auth allowlist; measure the cohort, run the load test and the ops gate; widen to all of SECOP II + reachable RUES.
5. Run the typical-task test, the Sunday test and the legal review on the widened data, then launch publicly, then start the M2 eng review.

## The Assignment

Before writing any code: pick one already-reported Colombian contracting case and **trace it by hand** through the SECOP II and RUES APIs using only contractor and legal-rep ID numbers. Centros Poblados is a good candidate; if its unión temporal members can't be reached from open data, pick a case with a single-company contractor. Write down every hop (dataset, field, value → next hop, and the date of each record). That file becomes M1's golden test fixture and defines the first ingest cohort. Then show the hand-drawn graph to one Colombian investigative journalist and ask: "what's the next hop you'd want to click?"

## What I noticed about how you think

- You named the lens immediately: "As crypto researcher I am used to follow the wallets, I want to go beyond that." That framing produced the key insight of the session. Colombian IDs *are* the wallet addresses.
- When I pushed name-only matches into a safe corner, you pushed back: "that is where things can get interesting... that's where patterns emerge." The whole hypothesis-mode design came from that pushback.
- You drew the line between leads and verdicts yourself: "not legally binding but the idea here is to reduce the investigation time and uncover hidden facts that need to be verified."
- "The mental map and the UX are the product." You then chose the boldest visual direction, which fits that conviction.

<!-- gstack:office-hours:concerns:start -->
## Reviewer Concerns

Disposition: CONCERNS_RECORDED

Stop: MAX_ITERATIONS

### R3-1 — consistency

**Problem**

> Leads are created before any lead UI exists. Suspect-ID edges arise in M1 ('until then the edge is shown as a lead'), and OFAC/PEP exact-ID matches with name disagreement arise in M2 ('stay a strong lead'). But 'Mostrar pistas', the hypothesis chip and 'asumir misma persona' are deferred to M3 (Visual direction; Roles: 'use viewer hypotheses (M3)'), and the lead-display legal review is the M3 gate. The document does not say how an M1 or M2 viewer sees these edges, or whether they are hidden entirely until M3. A suspect legal-rep ID in the M1 core hop would silently drop out of the graph.

**Remedy**

> State the pre-M3 behavior. Either ID-based leads are hidden entirely until M3, with a neutral 'vínculo en revisión' marker on the affected node, or a minimal 'Mostrar pistas' for ID-based leads ships in M1/M2 and its wording is covered by the M1 legal review.

### R3-2 — feasibility

**Problem**

> ID validation rule 4 downgrades an ID to a suspect candidate when 'it equals a known agency or contracting-entity NIT'. In SECOP, public entities are routinely contractors of other public entities (convenios/contratos interadministrativos, such as universities or public companies), so legitimate contractor NITs would be flagged as suspect. This floods the single-moderator queue and hides real edges as leads.

**Remedy**

> Flag an agency NIT only when it appears where it cannot legitimately be: as the contractor on its own contract (contractor NIT equals the contracting entity), or in a natural-person field (legal rep, ordenador, supervisor). Treat public-entity contractors as normal exact edges.

### R3-3 — clarity

**Problem**

> Rule 4 defines name disagreement as 'similarity below the weak threshold'. The only weak threshold in the grade table is ≥ 0.95, which is stricter than the strong threshold of 0.92. Read literally, any ID whose attached names score below 0.95 (ordinary typos, abbreviations, or a reordered name across SECOP records) is flagged suspect. That differs sharply from a reading of 'below 0.92'.

**Remedy**

> Give an explicit numeric cutoff for ID name disagreement, separate from the lead grades (for example a lower bound such as < 0.80 on the surname component). Validate it on the M1 cohort.

### R3-4 — consistency

**Problem**

> The hub-threshold mechanism was replaced (prolific IDs now keep exact edges with a 'hub' flag, and only suspect IDs are reviewed). The Roles table still lists 'hub-threshold reviews' as a moderator duty, and the M2 exit still requires 'a reviewed hub report'. These no longer match rule 4.

**Remedy**

> Rename these to 'suspect-ID reviews' and 'a reviewed suspect-ID report' (plus a hub-flag report if that is wanted), so the duties and exit criteria match rule 4.

### R3-5 — clarity

**Problem**

> The criterion for the visible 'hub' flag is no longer stated. The earlier '> 50 entities' threshold was removed, and rule 4 says only 'many companies or contracts under one ID', so engineering cannot implement the flag and reviewers cannot predict which people carry a publicly visible label.

**Remedy**

> Define the hub-flag rule (for example, legal rep of ≥ N companies or party to ≥ M contracts, measured on the M1 cohort) and its UI wording, and have the M1 legal review check that the wording is neutral.

### R3-6 — feasibility

**Problem**

> ICIJ stores officer names as a single free-text string, often reordered ('SURNAME, GIVEN') or with one surname. The fallback 'last two tokens are surnames' misparses these: 'JUAN CARLOS PEREZ' yields surnames 'CARLOS PEREZ'. The rule 'a missing second surname is not penalized' cannot apply, because the parser cannot know a surname is missing. It also breaks the 'first surname must align ≥ 0.9' check.

**Remedy**

> When field structure is absent, score several parses (one and two surnames, given-first and surname-first, comma-delimited) and keep the best-scoring parse, recording which parse was used in the lead's reasons. Measure parse ambiguity in the pre-M3 sample run.

### R3-7 — feasibility

**Problem**

> Each record gets one blocking key, 'first-surname + second-surname-or-first-given-name', and that key needs exact normalized token equality. A record with two surnames and its one-surname counterpart get different keys, so they are never compared. Spelling variants of the first surname never share a block either, so the similarity function's tolerance (≥ 0.9 Jaro-Winkler on the first surname, no penalty for a missing second surname) never applies.

**Remedy**

> Emit several blocking keys per record: first surname + second surname, first surname + first given name, and a phonetic or prefix key on the first surname. Deduplicate the candidate pairs before scoring, and report the recall effect in the sample run.

### R3-8 — feasibility

**Problem**

> Blocking includes 'country', and the sample run is on 'ICIJ's Colombia-linked records'. Colombians in offshore leaks often appear only with a Panama, Miami, BVI or Spain address or jurisdiction. Requiring a Colombia country match drops exactly the cross-border cases that motivate M3.

**Remedy**

> Define which ICIJ country fields make a record eligible (for example any linked country, or address country, or the country of an intermediary or entity). Or drop country from the block key and use it only as a filter in the sample run to measure what is lost.

### R3-9 — clarity

**Problem**

> 'A co-linked entity' is a corroborating attribute, but the document does not define how two records are co-linked when ICIJ and the Colombian sources share no IDs. If this is itself a name match (an ICIJ company name resembling a RUES company), a name-only inference would be counted as independent corroboration.

**Remedy**

> Define co-linked entity precisely: which entity pairs count, and what match standard applies (for example an exact-ID link through GLEIF, or a legal-entity name match above a stated threshold). State whether an unverified name match can count as corroboration.

### R3-10 — completeness

**Problem**

> An unverified requester can still obtain outcome (a), 'annotate the entity with the requester's statement'. The public notice then says the information was annotated 'a solicitud del titular'. Anyone can therefore attach text to a person's or company's page and present it as the data subject's own statement. This is an impersonation and defamation vector, and it undercuts the verification rule added for R2-4.

**Remedy**

> Restrict (a) to verified requesters, or label unverified annotations as 'declaración de un tercero no verificado' with moderator review of the content. Use the 'a solicitud del titular' notice only for verified requests.

### R3-11 — consistency

**Problem**

> The correction flow states a single 'Response target: within 15 business days, per Ley 1581' on the Owner line. The next bullet sets 10 business days for consultas. The blanket 15-day statement contradicts the access deadline.

**Remedy**

> Change the Owner line to point to the per-type targets (consulta 10, reclamo 15 business days), or remove it.

### R3-12 — completeness

**Problem**

> Requester verification collects signed requests and copies of identity documents 'handled out of band and never stored in the main database'. The document does not say where these sensitive documents are kept, who can access them, or how long they are retained, even though the project is itself a Ley 1581 data controller for them.

**Remedy**

> Specify the storage location (for example an encrypted mailbox or vault), access (moderator only), a retention period tied to ticket closure plus the legal hold, and deletion. Include this in the M1 legal review.

### R3-13 — feasibility

**Problem**

> One maintainer is the moderator for legally binding habeas-data deadlines (10 and 15 business days), the suspect-ID queue with a 10-business-day SLA, promotions, and researcher vetting. The suspect-ID queue size is measured only on the M1 cohort, but public launch happens after widening to national SECOP II plus RUES. There is no backup owner, and the queue is not re-measured at national scale before the launch gate.

**Remedy**

> Re-measure the suspect-ID queue on the widened national data before the launch gate. Name a backup moderator, or define an escalation path for the legal deadlines. State that the suspect-ID SLA is best-effort (edges stay leads) while the Ley 1581 deadlines take priority.

### R3-14 — feasibility

**Problem**

> The M3 discovery metric counts a lead as 'confirmed' only under the promotion standard: an identity-establishing document such as a notarized document or court record, checked by the moderator and one external journalist. For ICIJ offshore officers such documents are rarely obtainable, so most true leads would stay unresolved. They would count against the '≥ 50% confirmed true' target. The metric would then measure how documentable a lead is rather than how precise the grades are, and it has no refuted or unresolved category.

**Remedy**

> Classify each reviewed lead as confirmed, refuted, or unresolved. Compute precision over resolved leads, with a minimum number resolved, and report the unresolved share separately. Alternatively, define a lower but still explicit evidence standard for the metric than for public promotion.

### R3-15 — completeness

**Problem**

> ICIJ data is ODbL. The ICIJ-derived leads, grades and reviewer judgments are a Derivative Database that the public site uses publicly, and ODbL then requires offering that derivative database (or the means to recreate it) under ODbL. v1 ships no data export (PNG only), so the design has no way to meet the share-alike obligation. The Licensing table covers only notices.

**Remedy**

> Decide how the ICIJ-derived database is made available under ODbL: for example a periodic dump of the lead and grade tables with pseudonymized reviewer judgments, or published matching code plus parameters as the alteration file. Confirm the choice in the M3 legal review.

### R3-16 — feasibility

**Problem**

> Prolific individuals now keep all their exact edges with a hub flag. Expanding the M1 path contractor → legal rep → other contractors → contracts can therefore return thousands of nodes for one legal rep or agency. The document sets no per-expansion node cap, pagination, or aggregation, so neither the Cytoscape rendering nor the overlay's 'p95 ≤ 800 ms' over 2 hops is bounded.

**Remedy**

> Define the expansion limits: a maximum number of nodes per expand (for example 50), aggregation into 'N contratos más' or grouping by agency or year with drill-down, and the ordering used to pick which neighbors are shown first.
<!-- gstack:office-hours:concerns:end -->

## Eng Review 2 (2026-09-25): re-review after the design review, DESIGN.md and the entity page reference

Generated by /plan-eng-review on 2026-09-25. Branch: master · Commit: 95c28f8 (working tree has uncommitted DESIGN.md, TODOS.md, docs/).
Target: this file, `docs/designs/follow-the-money.md` (the product design doc), re-reviewed against `docs/reviews/eng-review-2026-09-25.md` (R1-R24, ISSUES OPEN), `docs/reviews/design-review-2026-09-25.md` (23 decisions), `DESIGN.md` and `docs/designs/assets/m1-entity-graph/entity-page.html`.
Report file: this file (selected plan file). Decision IDs continue from the first eng review (R25 onward) so they never collide with R1-R24.

### Evidence gathered (bounded probes, 2026-09-25, read-only)

| Probe | Result |
|---|---|
| SECOP II `jbjy-vk9h` max `valor_del_contrato` | 881,804,150,601,557,114,880 COP (a professional-services contract at Institución Universitaria Pascual Bravo) |
| contracts with value > COP 100 billones (1e14) | 2,305 |
| contracts with value > COP 10 billones (1e13) | 3,185 |
| highest values below COP 30 billones | natural-person "Prestación de servicios" contracts at ~COP 29 billones each (e.g. 29,648,025,000,000, a salary-sized value with extra zeros) |
| contracts with value = 0 / ≤ 1 | 196,554 / 199,302 |
| slug `cabossueltos` (double s) | PyPI 404, GitHub 404, `.co` and `.com.co` unregistered |
| slug `cabosueltos` (single s) | PyPI 404, GitHub 404, `.co` and `.com.co` unregistered |
| Name availability check during /design-consultation | ran on `cabosueltos` (single s); decision log says `cabosueltos`; TODOS.md, DESIGN.md and the entity page say `cabossueltos` |
| ITF Free Font License ([fontshare.com/licenses/itf-ffl](https://www.fontshare.com/licenses/itf-ffl)) | self-hosting in your own site/app allowed; making the font files available to anyone else through a repository or publicly accessible server is forbidden |

### Scope Challenge

**A. Assessment.**
- Existing solutions: unchanged from the first eng review (FtM at the boundary, Socrata SODA, Postgres + pg_trgm, Supabase, Cytoscape.js). New since then: `DESIGN.md` (tokens, fonts, print theme) and `entity-page.html` (a single-file visual and interaction reference with hand-placed SVG, fictional data and Pretext). No repo code yet.
- Minimum change: the design-review work lands inside `apps/web` plus API response fields; no new service.
- Complexity: still 2 apps/projects + Postgres, well over 8 files. The gate is resolved by the exact prior answers D3 (M1 only) and D4 (`apps/web` + one Python `backend/`); the design review adds modules inside `apps/web`, not services.
- Search check: [Layer 1] Cytoscape.js styles (line-style, underlay, shapes) and `cy.png()`; [Layer 1] browser `Intl` and `document.fonts`; [Layer 1] Python `holidays` (CO calendar) for business-day deadlines. No [EUREKA].
- TODOS.md: P1 name claim blocks the scaffold's package name; P1 office-hours correction-flow concerns still open (R3-10..13).
- Distribution: web + API as one hosted service (design line 236). Public container images are a licensing question for the fonts (see R29).

**B. Scope record (post-answer).** feature answers: D3 (first eng review, M1 only); structure: D4 (first eng review, smaller arrangement); accepted scope: M1 as locked by R1-R24 plus the design review's 23 decisions; pending remedies: R25-R34.

**C. Findings.**
1. **F1 [P1] (confidence 9/10) lines 75, 80, 106, 166-170, 184, 219, 224** — "uses **SECOP's rep-at-signing**", "get a visible **\"hub\"** flag", "statements (partitioned by dataset) ... materialized entity/edge tables", "`apps/api`", variant B image, Open Q3 and Q7 open. None of R1-R24 (eng T11) or the design-review decisions (design T1) have been applied. This doc still describes the pre-review system.
2. **F2 [P2] (confidence 9/10) Open Q3 line 219; TODOS.md "Claim the Cabos Sueltos name"** — the slug was availability-checked as `cabosueltos`, but TODOS.md, DESIGN.md and the entity page use `cabossueltos`. Both are free today. The scaffold's package name waits on this.

## Decision ledger (Eng Review 2)

### R25: Bring this design doc up to date with every approved M1 decision
Finding: F1, P1, confidence 9/10, lines 75/80/106/166-170/184/219/224, reviewer: Claude (host).
Plan baseline: first eng review T11 and design review T1 both say "apply the amendments to both design doc copies"; neither was done. docs/README.md order of authority ranks the review reports above this doc for M1.
Runtime evidence: this file at commit 95c28f8 plus the uncommitted "Superseded for M1" line (line 186); grep finds "rep-at-signing", "hub", "partitioned by dataset", "apps/api".
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| R25 design doc content | pre-review text; amendments only in review files | rewrite the affected sections in place (R1-R24 table, design decisions 1-22 + carried R4/R6/R16, DESIGN.md pointer, name, and this review's approved rows); mark the gstack copy superseded by this file | unchanged; builders read three documents and reconcile them |

Question D1:
D1 — Rewrite this design doc now so it matches every approved M1 decision?
Project/branch/task: master, eng re-review of docs/designs/follow-the-money.md after the design review, DESIGN.md and the entity page.
ELI10: The design doc is what a builder reads first, but it still describes the system from before two reviews changed it. It says the legal-rep link is "at signing" (the review proved that is false for 67% of contracts), it promises a "hub" label that was dropped, and it lays out folders and tables that were replaced. The corrections only live in two long review files. This review writes into this file anyway, so it can fix the text in place.
Stakes if we pick wrong: someone builds from the doc and ships the "rep at signing" claim or the hub label, the two things the reviews removed for legal reasons.
Recommendation: A because the doc is the entry point and the edit is about 20 minutes of CC time with no product risk.
Completeness: A=10/10, B=4/10
Pros / cons:
A) Rewrite in place (recommended)
  ✅ One document states the current M1 plan; the reviews become history, not required reading
  ✅ Closes first-eng-review T11 and design-review T1 in the same pass, no separate chore left
  ❌ Large diff to the committed design doc; the office-hours wording is replaced, though git keeps it
B) Leave the doc as is
  ✅ No churn now; the review files already hold every amendment table
  ✅ Keeps the office-hours doc as an untouched historical record of the original thinking
  ❌ Builders must merge three documents by hand, and the doc keeps contradicting them
Net: 20 minutes of editing buys a single source of truth before any code exists.
Header: Doc rewrite
Options:
A) Rewrite in place (recommended)
Rewrite the affected sections of docs/designs/follow-the-money.md in place: R1-R24 amendments, design-review decisions 1-22 plus carried R4/R6/R16, a DESIGN.md pointer, the product name, and the rows this review approves. Mark the ~/.gstack copy as superseded by this file. (human: ~3h / CC: ~20min)
B) Leave the doc as is
Keep the doc's text unchanged and keep relying on docs/README.md's order of authority, with the review reports overriding this doc for M1. No edits. (human: 0 / CC: 0)

State: approved
Actual answer: A) Rewrite in place (recommended), D1 answer 2026-09-25
Accepted scope: Rewrite the affected sections of docs/designs/follow-the-money.md in place: R1-R24 amendments, design-review decisions 1-22 plus carried R4/R6/R16, a DESIGN.md pointer, the product name, and the rows this review approves. Mark the ~/.gstack copy as superseded by this file. Applied in Required outputs, after the remaining choices are answered, so the rewrite includes them.
History: none

### R26: Package, org and domain slug for Cabos Sueltos
Finding: F2, P2, confidence 9/10, Open Q3 line 219; TODOS.md "Claim the Cabos Sueltos name"; DESIGN.md Decisions Log "Product name: Cabos Sueltos (`cabossueltos`)"; decision log entry "package/org slug cabosueltos". Reviewer: Claude (host).
Plan baseline: design-consultation D5 approved the name "Cabos Sueltos"; the slug spelling was never asked. The availability check ran on `cabosueltos`.
Runtime evidence: probe 2026-09-25: both `cabossueltos` and `cabosueltos` are free on PyPI, GitHub, `.co` and `.com.co`.
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| R26 slug | two spellings in use: `cabossueltos` (TODOS.md, DESIGN.md, entity page) and `cabosueltos` (decision log, the spelling checked) | `cabossueltos` everywhere; decision log corrected | `cabosueltos` everywhere; TODOS.md, DESIGN.md, entity page and preview corrected |
| Claiming the other spelling as a typo guard | not planned | unchanged, not planned | unchanged, not planned |

Question D2:
D2 — Which spelling is the package, org and domain slug: `cabossueltos` or `cabosueltos`?
Project/branch/task: master, eng re-review of docs/designs/follow-the-money.md; the scaffold's Python package name waits on this.
ELI10: The product is "Cabos Sueltos". When we squash it into one word for the Python package, GitHub org and web domain, two spellings are floating around: `cabossueltos` (both words kept whole) and `cabosueltos` (the two s's merged). The availability check ran on the second one, but the design files use the first. I re-checked today and both are free everywhere. We need one, because the package name gets baked into every import and the domain into every shared link.
Stakes if we pick wrong: renaming a package or domain after launch breaks imports, shared links and press mentions.
Recommendation: A because `cabossueltos` reads as the two real words, and DESIGN.md, TODOS.md and the entity page already use it.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) cabossueltos (recommended)
  ✅ Keeps both words whole, so people who hear the name "Cabos Sueltos" spell it right
  ✅ Already used in DESIGN.md, TODOS.md and the entity page, so only the decision log changes
  ❌ The double "ss" is easy to mistype when someone types the domain from memory
B) cabosueltos
  ✅ One letter shorter, and it is the spelling the original availability check covered
  ✅ Avoids the double-s run, which some people find awkward to read in a URL
  ❌ Drops a letter from "cabos", so anyone spelling the name from hearing it gets it wrong
Net: pick the spelling people will type from hearing the name; both are free today.
Header: Name slug
Options:
A) cabossueltos (recommended)
Use `cabossueltos` for the Python package, GitHub org, PyPI name and the .co/.com.co domains. Correct the decision log entry. The other spelling is not claimed by this choice.
B) cabosueltos
Use `cabosueltos` for the Python package, GitHub org, PyPI name and the .co/.com.co domains. Correct TODOS.md, DESIGN.md, the entity page and the preview. The other spelling is not claimed by this choice.

State: approved
Actual answer: B) cabosueltos, D2 answer 2026-09-25
Accepted scope: Use `cabosueltos` for the Python package, GitHub org, PyPI name and the .co/.com.co domains. Correct TODOS.md, DESIGN.md, the entity page and the preview. The other spelling is not claimed by this choice. Applied 2026-09-25: TODOS.md, DESIGN.md (format check still passes), docs/designs/assets/design-system/preview.html, docs/designs/assets/m1-entity-graph/entity-page.html and their ~/.gstack sources now say `cabosueltos`; the decision log already did.
History: none

Scope Challenge dispositions: F1 → R25 approved (A, D1). F2 → R26 approved (B, D2). No pending rows.

### Section 1: Architecture review (Eng Review 2)

Findings:
1. **A1 [P1] (confidence 9/10) design-review line 125-127 and T7** — "suppressed person → identical to not found, same 404 (8A)"; T7 verify "suppressed person URL returns the same body and 404 as a nonexistent ULID". Design line 236: "Public web service (single hosted instance)". No component in the plan serves `apps/web` or decides an HTTP status per route. A Vite SPA on a static host returns 200 for every path, so the 404 half of 8A cannot be met, and merged-entity 301s, CSP and `X-Robots-Tag` have no home.
2. **A2 [P1] (confidence 8/10) design-review 6A, 19A; first eng review R8** — "one contract edge per agency with a count and value"; 19A "encodes ... selected node/edge"; R8 "shared-rep computed at query time"; API `/edge/:id/evidence`. Grouped agency edges and shared-rep edges have no row in the `edge` table, so they have no ID for selection, URL state or the evidence endpoint.
3. **A3 [P1] (confidence 9/10) first eng review R3 "ordered by contract value desc"; DESIGN.md "Millones COP"; design-review 6A "N contratos · COP X"** — probe: max value 8.8e20 COP; 3,185 contracts above COP 10 billones; salary-sized service contracts at ~COP 29 billones. Junk values rank first in every expand and dominate every sum.
4. **A4 [P2] (confidence 7/10) DESIGN.md line 225** — "A build step fetches the WOFF2 files from Fontshare into the build output". No pinning, checksum, CI cache or failure rule. Cytoscape draws labels on a canvas, so fonts must be loaded before the first draw and the PNG. The ITF license forbids making the files available through a repository or public server, which includes a public container image.
5. **A5 [P2] (confidence 8/10) docs/README.md line 8** — entity-page.html is "the build reference for the entity page (eng T9)"; it draws hand-placed SVG with Pretext, while design line 166 and the design review choose Cytoscape.js with fcose. Nothing says which parts of the reference are spec and which are throwaway code.

### R27: Who serves the web app and sets HTTP status per route
Finding: A1, P1, confidence 9/10, design-review line 125-127 + T7; design line 236. Reviewer: Claude (host).
Plan baseline: design-review 8A (approved): suppressed URL identical to not found, same body and same 404. Design line 236: single hosted web service. First eng review D4: `apps/web` + one Python `backend/` with an API server entrypoint. How `apps/web` is served: unspecified.
Runtime evidence: no code. Static SPA hosting serves `index.html` with 200 for unknown paths (standard SPA fallback); unverified for a specific provider because none is chosen.
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| R27 web serving and route status | unspecified (framework fixed: React + TypeScript + Vite, design line 166) | Vite build output served by FastAPI; `/entidad/:ulid` does one indexed lookup: found 200, merged 301 to the surviving ULID, split 200, not found or suppressed 404 with the identical body; CSP and `X-Robots-Tag` set there; same origin as the API; Vite dev server with /api proxy locally | Vite build output on a static host/CDN; every route 200; client renders not-found; 8A's 404 dropped (same body only); CORS for the API; headers configured on the CDN |
| Web framework | React + TypeScript + Vite (approved, design line 166) | unchanged | unchanged |
| 8A suppressed = not found | approved (same body + same 404) | met in full | reopened: body only |

Question D4:
D4 — Keep Vite + React, and have the FastAPI server hand out the built files so entity URLs can return real 404s?
Project/branch/task: master, eng re-review of docs/designs/follow-the-money.md, Section 1 architecture.
ELI10: Vite + React stays in both options: the app is written in React and Vite builds it into a folder of static files (HTML, JS, CSS). This question is only about which server sends that folder to the browser. The design review promised that a suppressed person's old link looks exactly like a link that never existed, including the "404 not found" code, so nobody can tell someone asked to be removed. A plain static host answers "200 OK" for every address. If our FastAPI server sends the built files instead, one database lookup picks 200, 301 (merged entity) or 404.
Stakes if we pick wrong: a suppressed person's page keeps answering 200, stays in search indexes, and can be told apart from a never-existing page.
Recommendation: A because it meets the approved 8A promise with one lookup and no extra service, and it gives CSP and redirects one home.
Completeness: A=9/10, B=6/10
Pros / cons:
A) Vite build, served by FastAPI (recommended)
  ✅ Real 404 for suppressed and unknown entities, and 301 for merged ones, as 8A requires
  ✅ Same origin for site and API: no CORS, one CSP (fonts from self only), one deploy
  ❌ Static assets come from the app server, so a CDN needs adding later if traffic grows
B) Vite build on a static host
  ✅ Cheap global caching of the JS bundle from day one, independent of the API server
  ✅ Web deploys don't touch the API process at all
  ❌ Every URL returns 200, so the 404 half of 8A is dropped and suppressed pages look alive to crawlers
Net: same React app either way; one indexed lookup per page load buys the privacy promise the design review made.
Header: Web serving
Options:
A) Vite build, served by FastAPI (recommended)
React + TypeScript + Vite as planned. Vite's build output is served by the FastAPI service. `/entidad/:ulid` does one indexed lookup: found → 200, merged → 301 to the surviving ULID, split → 200 (chooser in the client), not found or suppressed → 404 with the identical body. CSP (`font-src 'self'`, no third-party origins) and `X-Robots-Tag` are set there. Same origin as the API. Local dev keeps the Vite dev server, proxying /api. Test: the T7 suppressed-vs-nonexistent comparison checks status and body. (human: ~1 day / CC: ~30min)
B) Vite build on a static host
React + TypeScript + Vite as planned. Vite's build output is hosted on a static host or CDN. Every route returns 200 and the client renders not-found. This reopens 8A to "same body only" (no 404). The API needs CORS, and headers are configured on the CDN. (human: ~0.5 day / CC: ~20min)

State: approved
Actual answer: A) Vite build, served by FastAPI (recommended), D4 answer 2026-09-25
Accepted scope: React + TypeScript + Vite as planned. Vite's build output is served by the FastAPI service. `/entidad/:ulid` does one indexed lookup: found → 200, merged → 301 to the surviving ULID, split → 200 (chooser in the client), not found or suppressed → 404 with the identical body. CSP (`font-src 'self'`, no third-party origins) and `X-Robots-Tag` are set there. Same origin as the API. Local dev keeps the Vite dev server, proxying /api. Test: the T7 suppressed-vs-nonexistent comparison checks status and body.
History: D3 (2026-09-25) asked the same choice with labels "A) API serves the site (recommended)" / "B) Static host / CDN". User reply: "I thought we were going to use Vite + react" (no option chosen). Revised as D4 to state that both options keep React + TypeScript + Vite and differ only in who serves the build output.

### R28: Stable IDs for grouped and derived edges (selection, URL, evidence)
Finding: A2, P1, confidence 8/10, design-review 6A and 19A; first eng review R8 and T8. Reviewer: Claude (host).
Plan baseline: R8 (D12 A): plain direct `entity`/`edge` tables; shared-rep computed at query time. 6A (approved): one contract edge per agency with count and value; ordenador/supervisor on the agency side "N contratos con esta empresa". 19A (approved): URL encodes the selected node/edge and never an ID number. T8: `/edge/:id/evidence`.
Runtime evidence: no code. The `edge` table (R8) holds direct edges per source record; grouped contract edges, agency-side ordenador edges and "comparte representante" edges exist only in query results.
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| R28 edge identity for grouped/derived edges | undefined | deterministic keys from kind + ULIDs: `contrato:<empresa>:<entidad>`, `rep_secop:<persona>:<empresa>`, `rep_rues:<persona>:<empresa>`, `ordenador:<persona>:<entidad>:<empresa>`, `supervisor:<persona>:<entidad>:<empresa>`, `comparte_rep:<empresaA>:<empresaB>:<persona>` (company ULIDs sorted), `miembro:<grupo>:<miembro>`; `/edge/:key/evidence?cursor=` parses the key, resolves each ULID through the redirect table, returns 5 records per page | grouped and shared-rep edges materialized as rows with their own ULIDs, rebuilt on every contract upsert and moderator action |
| R8 storage shape | approved: direct edges only, shared-rep at query time | unchanged | reopened: derived rows stored |
| Suppressed or unknown ULID inside a key | n/a | 404, same body as an unknown key | 404 via missing row |

Question D5:
D5 — Give grouped and derived edges IDs built from their endpoints, instead of storing them as rows?
Project/branch/task: master, eng re-review of docs/designs/follow-the-money.md, Section 1 architecture.
ELI10: The design review draws some lines that are summaries, not single records: "12 contracts between this company and this agency", "the ordenador who signed those 12", and "these two companies share a legal rep". The database only stores the underlying one-record links. But a user must be able to click such a line, see its evidence, and share a URL that reopens with that line selected. So each summary line needs a stable name. The simplest name is built from what it connects, for example `contrato:<company>:<agency>`, using only our random entity IDs, never a cédula or NIT.
Stakes if we pick wrong: selected lines can't survive a shared link, or we store millions of summary rows that must be rebuilt on every data change.
Recommendation: A because it needs no new tables, keeps the approved R8 storage shape, and never puts ID numbers in URLs.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Keys from endpoints (recommended)
  ✅ No new tables or rebuild jobs; the key is computed from what the line connects
  ✅ Merges are handled by resolving each ULID in the key through the existing redirect table
  ❌ Key format becomes a public URL contract; changing it later needs a compatibility parser
B) Store derived edges as rows
  ✅ Every edge has one opaque ULID, the same shape as direct edges in the API
  ✅ Evidence lookup is a single primary-key read instead of parsing a key
  ❌ Reopens R8: millions of derived rows to rebuild on every ingest and moderator action
Net: a parsed key keeps storage simple at the cost of one small, versioned URL format.
Header: Edge IDs
Options:
A) Keys from endpoints (recommended)
Grouped and derived edges get deterministic keys from kind + ULIDs (`contrato:<empresa>:<entidad>`, `rep_secop:<persona>:<empresa>`, `rep_rues:<persona>:<empresa>`, `ordenador:`/`supervisor:<persona>:<entidad>:<empresa>`, `comparte_rep:<empresaA>:<empresaB>:<persona>` with company ULIDs sorted, `miembro:<grupo>:<miembro>`). `/edge/:key/evidence?cursor=` parses the key, resolves each ULID through the redirect table and returns 5 records per page. A suppressed or unknown ULID in a key returns the same 404 as an unknown key. Tests: format/parse round trip, redirect resolution, suppressed → 404, no digits of any ID number in a key. (human: ~1 day / CC: ~30min)
B) Store derived edges as rows
Materialize grouped contract edges, agency-side official edges and shared-rep edges as rows with their own ULIDs, rebuilt inside every ingest and moderator transaction. Reopens R8 (D12). (human: ~3 days / CC: ~1.5h)

State: approved
Actual answer: A) Keys from endpoints (recommended), D5 answer 2026-09-25
Accepted scope: Grouped and derived edges get deterministic keys from kind + ULIDs (`contrato:<empresa>:<entidad>`, `rep_secop:<persona>:<empresa>`, `rep_rues:<persona>:<empresa>`, `ordenador:`/`supervisor:<persona>:<entidad>:<empresa>`, `comparte_rep:<empresaA>:<empresaB>:<persona>` with company ULIDs sorted, `miembro:<grupo>:<miembro>`). `/edge/:key/evidence?cursor=` parses the key, resolves each ULID through the redirect table and returns 5 records per page. A suppressed or unknown ULID in a key returns the same 404 as an unknown key. Tests: format/parse round trip, redirect resolution, suppressed → 404, no digits of any ID number in a key.
History: none

### R29: Implausible contract values (ordering, sums, display)
Finding: A3, P1, confidence 9/10, first eng review R3 "ordered by contract value desc"; DESIGN.md "Millones COP" column; design-review 6A "N contratos · COP X". Reviewer: Claude (host), with a probe.
Plan baseline: R3 (D7 A): neighbors ordered by contract value desc, then signing date desc, then id. 6A: grouped contract edge shows count and summed value. Design-review 22: money shown as "COP 32.000 millones", exact value in drawer/tooltip. No value-quality rule anywhere.
Runtime evidence: probe 2026-09-25 on `jbjy-vk9h`: max `valor_del_contrato` 8.8e20 COP (a professional-services contract); 2,305 contracts above COP 100 billones, 3,185 above COP 10 billones; below COP 30 billones the top rows are still natural-person "Prestación de servicios" contracts (e.g. 29,648,025,000,000); 196,554 contracts at exactly 0.
Comparison grid:

| Choice | Current | A | B | C |
|---|---|---|---|---|
| R29 value quality | none; raw values ordered and summed | ingest flag `valor_atipico`: > COP 20 billones, or natural-person "Prestación de servicios" > COP 5.000 millones; thresholds checked on the 200 highest-value contracts per contract type before widening; flagged values excluded from sums and sorted after valued rows; drawer shows the raw value with a neutral note; value 0 shown as "sin valor reportado" | order by contract count, then date; sums shown raw | unchanged |
| R3 ordering | value desc, date desc, id | value desc over non-flagged values, flagged after, then date, id | contract count desc, date desc, id | unchanged |
| Neutral wording for flagged values | none | "Valor reportado en SECOP fuera del rango habitual; verifíquelo en el registro oficial", added to the M1 legal-review wording list | n/a | n/a |

Question D6:
D6 — Flag implausible contract values at ingest and keep them out of sorting and totals?
Project/branch/task: master, eng re-review of docs/designs/follow-the-money.md, Section 1 architecture.
ELI10: SECOP has typos in contract values. I checked: one freelance contract is recorded at about 881 quintillion pesos (8.8 × 10^20), nearly two million times Colombia's national budget. 3,185 contracts claim more than COP 10 billones, and many are ordinary personal-service contracts with extra zeros. The plan sorts every expansion by value and sums values per agency, so these typos land at the top of every list and blow up every total. A journalist would screenshot "COP 881.804.150.601.557 millones" and we'd look broken, or worse, like we're accusing someone.
Stakes if we pick wrong: the first rows and totals on many entity pages are nonsense numbers that look like findings.
Recommendation: A because it keeps value ordering meaningful, shows the raw record honestly, and uses the same calibrate-before-widening pattern as R11.
Completeness: A=9/10, B=5/10, C=2/10
Pros / cons:
A) Flag at ingest (recommended)
  ✅ Totals and value ordering reflect plausible data, and the raw value stays one click away
  ✅ Neutral note sends the reader to the official record instead of us judging the value
  ❌ Thresholds can misflag a real mega-contract until the 200-row check tunes them
B) Order by count instead
  ✅ No thresholds to calibrate; count of contracts can't be typo-inflated
  ✅ Simple to implement: one ORDER BY change and no new ingest field
  ❌ Summed values in the card, table and PNG still show absurd totals
C) Keep raw values
  ✅ Zero work, and the numbers exactly mirror what SECOP publishes
  ✅ No risk of wrongly hiding a genuinely enormous contract
  ❌ Typos top every list and dominate every total on launch day
Net: a small ingest flag protects every number the site shows, at the cost of one calibration pass.
Header: Bad values
Options:
A) Flag at ingest (recommended)
At ingest, set `valor_atipico` when a value is above COP 20 billones, or a natural-person "Prestación de servicios" contract is above COP 5.000 millones. Check the thresholds on the 200 highest-value contracts per contract type before widening. Flagged values are excluded from sums and sorted after valued rows. The drawer shows the raw value with "Valor reportado en SECOP fuera del rango habitual; verifíquelo en el registro oficial", and that wording joins the M1 legal-review list. Value 0 shows as "sin valor reportado". Tests for each rule, sums and ordering. (human: ~1 day / CC: ~40min)
B) Order by count instead
Change R3 ordering to contract count desc, then date desc, then id. Values and sums are shown raw, with no flag. (human: ~2h / CC: ~10min)
C) Keep raw values
Keep R3 value ordering and raw sums unchanged. (human: 0 / CC: 0)

State: approved
Actual answer: A) Flag at ingest (recommended), D6 answer 2026-09-25
Accepted scope: At ingest, set `valor_atipico` when a value is above COP 20 billones, or a natural-person "Prestación de servicios" contract is above COP 5.000 millones. Check the thresholds on the 200 highest-value contracts per contract type before widening. Flagged values are excluded from sums and sorted after valued rows. The drawer shows the raw value with "Valor reportado en SECOP fuera del rango habitual; verifíquelo en el registro oficial", and that wording joins the M1 legal-review list. Value 0 shows as "sin valor reportado". Tests for each rule, sums and ordering.
History: none

### R30: Font pipeline: what happens when the font fetch fails
Finding: A4, P2, confidence 7/10, DESIGN.md line 224-227. Reviewer: Claude (host), with a license lookup.
Plan baseline: DESIGN.md (decision logged 2026-09-25): "A build step fetches the WOFF2 files from Fontshare into the build output, which is gitignored"; never committed; never loaded from a third-party CDN at runtime; JetBrains Mono from `@fontsource/jetbrains-mono`. Fetch mechanics and failure behavior are unspecified.
Runtime evidence: ITF Free Font License: self-hosting in your own site/app allowed; making the files available to others through a repository or publicly accessible server forbidden. Cytoscape.js renders labels on a canvas, which uses whatever font is loaded at draw time.
Carried without a question (required implementation of the approved DESIGN.md font rules, same in every option): the app awaits `document.fonts.load()` for the three families before the first Cytoscape draw and before PNG export; images containing the font files go only to a private registry, and the post-M2 self-host compose builds the web image locally; an E2E check asserts no request leaves the site's origin.
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| R30 fetch integrity and failure rule | "a build step"; failure unspecified | pinned WOFF2 URLs into gitignored `apps/web/public/fonts/`, SHA-256 checked against a committed hash manifest, CI caches the folder; production build fails if fetch fails and cache is empty; local dev falls back to system fonts with a warning | pinned URLs, no hash check, no cache; every build falls back to system fonts with a warning on failure |
| Canvas wait, private images, no-third-party E2E | carried (DESIGN.md rules) | carried | carried |

Question D7:
D7 — When the Fontshare download fails at build time, fail the build or ship fallback fonts?
Project/branch/task: master, eng re-review of docs/designs/follow-the-money.md, Section 1 architecture.
ELI10: DESIGN.md says we download Cabinet Grotesk and General Sans while building the site, because their license forbids putting the files in our public repo. It doesn't say what happens if that download fails, or if Fontshare quietly changes a file. One choice is to check each file against a saved fingerprint, cache it in CI, and stop the build if neither works. The other is to build anyway with the computer's default fonts and print a warning nobody reads. Either way, the page waits for fonts before drawing the graph, and images with the fonts never go public: DESIGN.md's rules already require that.
Stakes if we pick wrong: production quietly ships the wrong typeface and PNGs bake it in, or a Fontshare outage with a cold cache blocks a release.
Recommendation: A because the typography is a user-approved decision and a CI cache makes a blocked release rare.
Completeness: A=9/10, B=5/10
Pros / cons:
A) Check files, fail the build (recommended)
  ✅ A Fontshare outage or a changed file stops the build instead of shipping the wrong look
  ✅ The CI cache means deploys only depend on Fontshare the first time or after a cache reset
  ❌ A cold cache during a Fontshare outage blocks a release until it comes back
B) Warn and use fallback fonts
  ✅ Deploys never block on a third-party font server
  ✅ Less code: no hash manifest and no cache configuration to maintain
  ❌ Production can quietly ship system fonts, and PNGs exported then carry them
Net: a hash file and a cache buy predictable typography; the cost is a rare blocked release.
Header: Font failures
Options:
A) Check files, fail the build (recommended)
A script downloads pinned WOFF2 URLs into gitignored `apps/web/public/fonts/` and checks SHA-256 against a committed hash manifest (hashes only). CI caches the folder. A production build fails if the fetch fails and the cache is empty; local dev falls back to system fonts with a warning. Unit test for the manifest check. Canvas wait, private images and the no-third-party E2E are carried either way. (human: ~4h / CC: ~20min)
B) Warn and use fallback fonts
The same download script with pinned URLs, no hash check and no cache. If the fetch fails, every build falls back to the system font stack with a warning. Canvas wait, private images and the no-third-party E2E are carried either way. (human: ~2h / CC: ~10min)

State: approved
Actual answer: A) Check files, fail the build (recommended), D7 answer 2026-09-25
Accepted scope: A script downloads pinned WOFF2 URLs into gitignored `apps/web/public/fonts/` and checks SHA-256 against a committed hash manifest (hashes only). CI caches the folder. A production build fails if the fetch fails and the cache is empty; local dev falls back to system fonts with a warning. Unit test for the manifest check. Carried: the app awaits `document.fonts.load()` before the first Cytoscape draw and PNG export; images with the font files go only to a private registry, and the post-M2 self-host compose builds locally; an E2E check asserts no request leaves the site's origin.
History: A draft of this record (not sent) bundled the carried items into option A; split before asking because they are required by approved DESIGN.md rules, not part of the choice.

### R31: What the entity page reference is: spec or code to port
Finding: A5, P2, confidence 8/10, docs/README.md line 8; design line 166; design-review "What already exists". Reviewer: Claude (host).
Plan baseline: design line 166 "React + TypeScript + Vite, Cytoscape.js" (approved at office hours); design review lists Cytoscape.js with fcose and fixed nodes. /design-html D1 (user) approved entity-page.html as the entity page reference; docs/README.md calls it "the build reference for the entity page (eng T9)".
Runtime evidence: entity-page.html draws the graph as hand-placed SVG coordinates for fictional data, and uses Pretext (@chenglou/pretext) for title fitting, bottom-sheet height and SVG label wrapping.
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| R31 role of entity-page.html | "build reference"; unclear whether code or spec | visual and interaction spec (layout, tokens, grammar, states, copy, keyboard, URL keys); production graph on Cytoscape.js + fcose with pinned nodes, 15A grammar as Cytoscape styles (line-style, arrows, shapes, underlay for the highlighter, text-outline for label halos); no Pretext (CSS and ResizeObserver cover title fit and sheet height); README wording updated | code to port: its SVG renderer becomes the graph, plus a layout engine (force or ELK) for real data; Pretext kept for label wrapping |
| Graph library (design line 166) | Cytoscape.js (approved) | unchanged | reopened: replaced by custom SVG |

Question D8:
D8 — Treat the approved entity page as a visual spec and build the real graph on Cytoscape.js?
Project/branch/task: master, eng re-review of docs/designs/follow-the-money.md, Section 1 architecture.
ELI10: The entity page we built and you approved is one HTML file with fake data. Its graph is hand-drawn SVG: every node sits at coordinates I typed in, and a small text-measuring library (Pretext) fits the labels. That works for one fictional company, not for real data where each page has different nodes. The plan already picked Cytoscape.js, a graph library that places nodes automatically and handles zoom, clicks and PNG export. The docs just call the HTML file "the build reference" without saying whether to copy its code or its look.
Stakes if we pick wrong: a builder ports hand-placed SVG, then has to invent automatic layout, hit-testing and export that Cytoscape already provides.
Recommendation: A because the look and behavior are what was approved, and Cytoscape covers every piece of the 15A graph grammar natively.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Spec, build on Cytoscape (recommended)
  ✅ Keeps the approved library: automatic layout, pinned nodes on expand, hit-testing and `cy.png()` come built in
  ✅ No extra text-measuring dependency; CSS and a ResizeObserver cover the two layout tricks
  ❌ Visual details (highlighter swipe, label halos) must be matched in Cytoscape styles and checked by screenshot
B) Port the SVG code
  ✅ Pixel-exact to the approved page, since it is the same drawing code
  ✅ Full control over every SVG detail without working within a library's style system
  ❌ Needs a new layout engine, hit-testing, zoom and export written by hand, reopening the Cytoscape choice
Net: copy the look, not the code; the approved library already does the hard graph parts.
Header: Reference use
Options:
A) Spec, build on Cytoscape (recommended)
entity-page.html is the visual and interaction spec (layout, tokens, graph grammar, states, copy, keyboard, URL keys). The production graph uses Cytoscape.js + fcose with pinned nodes, with the 15A grammar as Cytoscape styles (line-style, arrows, node shapes, underlay for the highlighter, text-outline for label halos). No Pretext dependency. docs/README.md says "visual and interaction reference" instead of "build reference". Verify with the grayscale screenshot test (design T4). (human: 0 extra / CC: ~5min for the doc wording)
B) Port the SVG code
Port the reference's SVG renderer into apps/web, add a layout engine (force-directed or ELK) for real data, and keep Pretext for label wrapping. Reopens the Cytoscape.js choice (design line 166). (human: ~4 days / CC: ~2h extra)

State: approved
Actual answer: A) Spec, build on Cytoscape (recommended), D8 answer 2026-09-25
Accepted scope: entity-page.html is the visual and interaction spec (layout, tokens, graph grammar, states, copy, keyboard, URL keys). The production graph uses Cytoscape.js + fcose with pinned nodes, with the 15A grammar as Cytoscape styles (line-style, arrows, node shapes, underlay for the highlighter, text-outline for label halos). No Pretext dependency. docs/README.md says "visual and interaction reference" instead of "build reference" (applied 2026-09-25). Verify with the grayscale screenshot test (design T4).
History: none

Section 1 dispositions: A1 → R27 approved (A, D4; D3 re-asked after a clarification). A2 → R28 approved (A, D5). A3 → R29 approved (A, D6). A4 → R30 approved (A, D7). A5 → R31 approved (A, D8). Security: CSP and same-origin serving come from R27; masking unchanged (MaskedId, no ID digits in URLs or edge keys, R28). No pending rows.

### Section 2: Code quality review (Eng Review 2)

Findings:
1. **Q1 [P2] (confidence 8/10) design-review 9A, T5, 19A, 4A** — "The table shows every R3 result with \"Mostrando 50 de 312\" and loads the next 50 via cursor"; T5 "grouped/sortable"; 19A encodes "table sort/filter"; 4A "click filters the table". The plan does not say where sorting and filtering run. Sorting 50 loaded rows of 312 in the browser gives a wrong "top" list while looking sorted.
2. **Q2 [P1] (confidence 8/10) design lines 116-127; design-review /correccion screen and state table; first eng review T8** — "a form plus a dedicated email address. Each request gets a ticket ID"; confirmation shows "ticket ID + deadline (consulta 10 / reclamo 15 business days)". The data flow has a `correction_ticket` table, but T8's endpoint list has no intake endpoint, and nothing covers abuse control, maintainer notification, or computing business-day deadlines (Colombian holidays move to Mondays under Ley 51 de 1983).
3. Shared code: design-review 22 already requires one es-CO formatter module shared by UI, PNG and citations (approved; carried). No other shared-code opportunity: no repo code exists.

### R32: Where the Conexiones table sorts and filters
Finding: Q1, P2, confidence 8/10, design-review 9A/T5/19A/4A. Reviewer: Claude (host).
Plan baseline: R3 (D7 A): `/entity/:id/expand?kind=&cursor=` returns 50 per kind in value/date/id order plus `total`. 9A, T5, 19A, 4A approved (table pages by cursor, sortable, filterable, sort/filter in URL). R29 (D6 A): flagged values sort after valued rows. Where sort/filter execute: unspecified.
Runtime evidence: no code.
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| R32 sort/filter location | unspecified | server: expand/table endpoint takes `orden=valor|contratos|fecha|nombre` and `filtro=` (edge kind, `comparte_rep`), sorts and filters over all neighbors of the kind with a keyset cursor per sort key; "Mostrando X de Y" counts the filtered total; URL `orden`/`filtro` map 1:1 to the params | client: sort/filter only the loaded rows; a visible note "Ordenando las 50 filas cargadas de 312"; server keeps R3 order only |
| R3 default order + R29 flag ordering | approved | unchanged (default `orden=valor`) | unchanged |
| Load check | R15 load test on expand | + sort by `contratos` and `nombre` on top-100 hubs | unchanged |

Question D9:
D9 — Sort and filter the Conexiones table on the server, across every row, not just the 50 loaded?
Project/branch/task: master, eng re-review of docs/designs/follow-the-money.md, Section 2 code quality.
ELI10: A busy company can have 312 connections of one kind, and the table loads them 50 at a time. If a journalist clicks "sort by number of contracts" and the browser sorts only the 50 it has, the top row isn't the real top: it's the top of whatever happened to load first. The same goes for "show only companies that share a legal rep". Sorting on the server, over all 312, gives the true answer, and the sort can live in the shared link.
Stakes if we pick wrong: a journalist reports "the biggest contractor" from a list that silently skipped most rows.
Recommendation: A because the table is the accessible equivalent of the graph and must not mislead, and it is one query parameter on an endpoint we are already building.
Completeness: A=9/10, B=5/10
Pros / cons:
A) Server-side sort and filter (recommended)
  ✅ Every sort and filter is true over all rows, and "Mostrando X de Y" stays honest
  ✅ The shared URL's sort and filter reproduce the same rows for the recipient
  ❌ Four keyset cursors to implement and test, and hub sorts join the R15 load test
B) Client-side with a notice
  ✅ No backend work beyond the approved R3 cursor
  ✅ Instant re-sorting of the rows already on screen, with no request
  ❌ A notice that readers skip, and a shared sorted link can show different rows
Net: one sort parameter keeps the table truthful; the saving from client-only sorting is small.
Header: Table sorting
Options:
A) Server-side sort and filter (recommended)
The expand/table endpoint takes `orden=valor|contratos|fecha|nombre` and `filtro=` (edge kind, `comparte_rep`). It sorts and filters over all neighbors of that kind with a keyset cursor per sort key; default `orden=valor` keeps R3 and R29 ordering. "Mostrando X de Y" counts the filtered total. The URL's `orden`/`filtro` map 1:1 to these params. Tests: each sort key pages without gaps or repeats; filter totals; R15 load test adds `contratos` and `nombre` sorts on the top-100 hubs. (human: ~1 day / CC: ~40min)
B) Client-side with a notice
Sort and filter only the loaded rows in the browser, with a visible note "Ordenando las 50 filas cargadas de 312". The server keeps the R3 order only. (human: ~2h / CC: ~10min)

State: approved
Actual answer: A) Server-side sort and filter (recommended), D9 answer 2026-09-25
Accepted scope: The expand/table endpoint takes `orden=valor|contratos|fecha|nombre` and `filtro=` (edge kind, `comparte_rep`). It sorts and filters over all neighbors of that kind with a keyset cursor per sort key; default `orden=valor` keeps R3 and R29 ordering. "Mostrando X de Y" counts the filtered total. The URL's `orden`/`filtro` map 1:1 to these params. Tests: each sort key pages without gaps or repeats; filter totals; R15 load test adds `contratos` and `nombre` sorts on the top-100 hubs.
History: none

### R33: Correction intake: endpoint, deadlines, abuse control, notification
Finding: Q2, P1, confidence 8/10, design lines 116-127; design-review /correccion screen and Pass 2 state table; first eng review T8. Reviewer: Claude (host).
Plan baseline: design line 118 (approved): "a form plus a dedicated email address. Each request gets a ticket ID"; line 120: consulta 10, reclamo 15 business days; line 121: ID copies handled out of band, never in the main database. Design review (approved): confirmation shows ticket ID + deadline; on error keep input + email fallback. First eng review data flow: `correction_ticket` table. Endpoint, deadline computation, abuse control, notification: unspecified.
Runtime evidence: no code. Colombian public holidays move to the following Monday (Ley 51 de 1983), so business-day deadlines need a holiday calendar; the Python `holidays` package ships a CO calendar.
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| R33 intake | "a form" + email; no endpoint | `POST /api/correcciones` (type, entity ULID, requester name, email, message; no file uploads) → ticket code `CS-<año>-<secuencia>` stored in `correction_ticket` | mailto link with a prefilled template; ticket IDs assigned by hand |
| Deadline | "10 / 15 business days" | computed at receipt in Colombian business days (`holidays` CO), stored and shown on the confirmation | tracked by hand |
| Abuse control | per-IP rate limit named for search only | per-IP limit (5 per hour) + honeypot field; no third-party captcha | none (email) |
| Maintainer notification | unspecified | transactional email (provider from env); on send failure the ticket is kept, the requester still sees success, and the send is retried; admin CLI `tickets --vencen` lists by deadline | the email itself |
| Proof | none | tests: deadline across a Monday holiday and year end; rate limit; honeypot; notification failure keeps the ticket; ticket code contains no personal data | none |

Question D10:
D10 — Build a real correction-request endpoint with automatic legal deadlines?
Project/branch/task: master, eng re-review of docs/designs/follow-the-money.md, Section 2 code quality.
ELI10: From launch day, anyone named on the site can ask what we hold about them or ask for a fix. Colombian law (Ley 1581) gives us 10 business days for questions and 15 for fixes. The design promises a form that returns a ticket number and a due date, but the API plan never included the endpoint behind it. It also needs a spam guard, an email to the maintainer, and correct due dates, which is tricky because Colombian holidays move to Mondays.
Stakes if we pick wrong: a missed legal deadline, a lost request, or a form flooded with spam on the day a story runs.
Recommendation: A because the form and ticket screen are already approved, and the deadlines carry legal exposure worth automating.
Completeness: A=9/10, B=4/10
Pros / cons:
A) Endpoint + auto deadlines (recommended)
  ✅ Every request gets a stored ticket and a correct legal due date, including Monday holidays
  ✅ Maintainer is emailed, and a CLI lists open tickets by due date so none slip
  ❌ Adds an email provider secret and a small spam guard to build, test and run
B) Email only
  ✅ Nothing to build: the maintainer's mailbox is the whole system
  ✅ No requester data stored in our database at all
  ❌ Contradicts the approved form and ticket screen; deadlines depend on memory
Net: a small endpoint turns legal deadlines from memory into data, matching the screens already approved.
Header: Corrections
Options:
A) Endpoint + auto deadlines (recommended)
`POST /api/correcciones` takes type (consulta/reclamo), entity ULID, requester name, email and message, with no file uploads (ID copies stay out of band). It returns a ticket code `CS-<año>-<secuencia>` stored in `correction_ticket`, with the deadline computed in Colombian business days (`holidays` CO) and shown on the confirmation. Per-IP limit of 5 per hour plus a honeypot field, and no third-party captcha. The maintainer gets a transactional email (provider from env); if sending fails the ticket is kept, the requester still sees success, and the send is retried. Admin CLI `tickets --vencen` lists open tickets by deadline. Tests: deadline across a Monday holiday and year end, rate limit, honeypot, notification failure, ticket code without personal data. (human: ~1.5 days / CC: ~45min)
B) Email only
Replace the form with a mailto link and a prefilled template. The maintainer assigns ticket IDs and tracks deadlines by hand. Reopens the design-review correction screen. (human: ~1h / CC: ~10min)

State: approved
Actual answer: A) Endpoint + auto deadlines (recommended), D10 answer 2026-09-25
Accepted scope: `POST /api/correcciones` takes type (consulta/reclamo), entity ULID, requester name, email and message, with no file uploads (ID copies stay out of band). It returns a ticket code `CS-<año>-<secuencia>` stored in `correction_ticket`, with the deadline computed in Colombian business days (`holidays` CO) and shown on the confirmation. Per-IP limit of 5 per hour plus a honeypot field, and no third-party captcha. The maintainer gets a transactional email (provider from env); if sending fails the ticket is kept, the requester still sees success, and the send is retried. Admin CLI `tickets --vencen` lists open tickets by deadline. Tests: deadline across a Monday holiday and year end, rate limit, honeypot, notification failure, ticket code without personal data.
History: none

Section 2 dispositions: Q1 → R32 approved (A, D9). Q2 → R33 approved (A, D10). Shared formatter module carried from design-review 22. No pending rows.

### Section 3: Test review (Eng Review 2)

Framework: unchanged from R13/R14 (pytest + real Postgres 17 + Hypothesis; Vitest; Playwright + axe). No new framework choice. Required proof for the design-review decisions and for R27-R33 is carried as accepted work (each approved behavior brings its tests); the coverage diagram is in the review body below. Regression rule: greenfield, no existing behavior at risk. No new choices in this section.

### Section 4: Performance review (Eng Review 2)

Findings:
1. **P1 [P2] (confidence 8/10) design-review 19A; first eng review R15** — "encodes focus ULID, ordered expansion trail (node ULIDs) ... merge → redirect keeping the trail; ... vanished trail node dropped"; R15 expand p95 ≤ 500 ms. Opening a shared link with a 6-step trail costs the focus load plus 6 expands, each also resolving redirects: several seconds if sequential, and the merged/vanished/suppressed handling is spread over many calls. A split node inside the trail (not the focus) has no stated behavior.
2. **P2 [P3] (confidence 6/10, medium: verify bundle sizes when built) design-review 16A** — "< 768px: ... tabs \"Conexiones\" (default, grouped list) | \"Grafo\"". Phones open on the table, but a single bundle still downloads and parses Cytoscape.js + fcose before first paint.

### R34: Restoring a shared view (trail) in one request
Finding: P1, P2, confidence 8/10, design-review 19A; R15. Reviewer: Claude (host).
Plan baseline: 19A (approved): URL holds focus, trail, selection, sort/filter, tab; merge → redirect keeping the trail; split → chooser; vanished trail node dropped with "Parte de esta vista ya no está disponible". 8A: suppressed = not found. R15: expand p95 ≤ 500 ms. R27/R28/R32 approved. Trail restore mechanism and trail length: unspecified.
Runtime evidence: no code.
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| R34 restore mechanism | unspecified (one expand per trail node implied) | `GET /api/vista?foco=&ruta=&sel=&orden=&filtro=` resolves every ULID through redirects in one query and returns focus + each trail expansion (first page per kind, R3/R32 order) + the resolved selection in one response | client calls `/expand` per trail node in parallel; each response carries redirect or 404; client reconciles |
| Trail cap | none | 12 steps; extra steps dropped with the same notice | 12 steps; extra steps dropped with the same notice |
| Vanished, suppressed or split node inside the trail | vanished: dropped + notice; split: chooser (focus only) | vanished, suppressed and split trail nodes all dropped with the one notice (focus keeps 19A: split → chooser, suppressed → 404) | same rule, applied in the client |
| Budget | R15 per-call p95 | `/vista` p95 ≤ 1.5 s at 12 steps, added to the R15 load test | per-expand p95 ≤ 500 ms (R15) |

Question D11:
D11 — Restore shared links with one server request that rebuilds the whole view?
Project/branch/task: master, eng re-review of docs/designs/follow-the-money.md, Section 4 performance.
ELI10: A shared link remembers every step the sender expanded, say six hops. Opening it naively means one request for the page plus one per step, and each step also has to check whether that entity was merged, removed or suppressed since the link was made. That's slow on a phone, and the privacy rule "a suppressed person looks exactly like a missing one" would be enforced in six places. One endpoint can take the whole link, fix up merged IDs, quietly drop anything gone, and send back the full view at once. Either way I'd cap a trail at 12 steps and treat a gone, suppressed or split middle step the same way: drop it and show one notice.
Stakes if we pick wrong: shared links, the main way journalists pass findings to editors, open slowly, or a suppressed person leaks through one of many code paths.
Recommendation: A because one round trip is faster on mobile and keeps the suppression rule in one tested place.
Completeness: A=9/10, B=7/10
Pros / cons:
A) One /vista request (recommended)
  ✅ One round trip opens any shared link, about 1.5 s worst case instead of several seconds
  ✅ Redirect, vanished and suppressed handling lives in one server function with one privacy test
  ❌ One more endpoint whose response size grows with the trail, needing its own load-test case
B) Parallel expands in the client
  ✅ No new endpoint; reuses the expand call the graph already makes
  ✅ Each step can render as soon as it arrives, so the graph fills in progressively
  ❌ Redirect and suppression handling spread across the client, tested per call instead of once
Net: a single resolve-and-return endpoint trades one extra API route for speed and one privacy choke point.
Header: Link restore
Options:
A) One /vista request (recommended)
`GET /api/vista?foco=&ruta=&sel=&orden=&filtro=` resolves every ULID through the redirect table in one query and returns the focus, each trail expansion (first page per kind, R3/R32 order) and the resolved selection in one response. The trail is capped at 12 steps. Vanished, suppressed and split nodes inside the trail are all dropped with the one notice "Parte de esta vista ya no está disponible"; the focus keeps 19A (split → chooser, suppressed → 404). p95 ≤ 1.5 s at 12 steps, added to the R15 load test. Tests: merged, vanished, suppressed and split trail nodes; cap; suppressed and vanished responses identical. (human: ~1 day / CC: ~40min)
B) Parallel expands in the client
The client calls `/expand` for each trail node in parallel; each response carries redirect info or a 404, and the client reconciles. Same 12-step cap and the same drop-with-notice rule for vanished, suppressed and split trail nodes, applied in the client. Per-expand budget stays R15's p95 ≤ 500 ms. Tests for the same cases in Vitest + Playwright. (human: ~1 day / CC: ~40min)

State: approved
Actual answer: A) One /vista request (recommended), D11 answer 2026-09-25
Accepted scope: `GET /api/vista?foco=&ruta=&sel=&orden=&filtro=` resolves every ULID through the redirect table in one query and returns the focus, each trail expansion (first page per kind, R3/R32 order) and the resolved selection in one response. The trail is capped at 12 steps. Vanished, suppressed and split nodes inside the trail are all dropped with the one notice "Parte de esta vista ya no está disponible"; the focus keeps 19A (split → chooser, suppressed → 404). p95 ≤ 1.5 s at 12 steps, added to the R15 load test. Tests: merged, vanished, suppressed and split trail nodes; cap; suppressed and vanished responses identical.
History: none

### R35: Load the graph code only when the graph is shown
Finding: P2, P3, confidence 6/10 (medium: verify bundle sizes once built), design-review 16A. Reviewer: Claude (host).
Plan baseline: 16A (approved): under 768px the page opens on the "Conexiones" tab; "Grafo" is a second tab. Bundle splitting: unspecified.
Runtime evidence: no code. Cytoscape.js and the fcose layout are the largest planned web dependencies; exact sizes unmeasured.
Comparison grid:

| Choice | Current | A | B |
|---|---|---|---|
| R35 graph code loading | unspecified (single bundle implied) | the graph module (Cytoscape.js + fcose + styles) is a dynamic `import()`: loaded on first render at ≥ 768px, and on first open of the "Grafo" tab (or a shared link with `vista=grafo`) under 768px; PNG export loads it too | single bundle, graph code always loaded |
| Proof | none | Playwright at 390px asserts no graph chunk is requested until "Grafo" opens; build prints chunk sizes | none |

Question D12:
D12 — On phones, download the graph library only when someone opens the Grafo tab?
Project/branch/task: master, eng re-review of docs/designs/follow-the-money.md, Section 4 performance.
ELI10: On phones the entity page opens on the Conexiones list, not the graph. But unless we split the code, every phone still downloads and runs the whole graph library before showing that list. Vite can split it off so it loads the first time someone taps "Grafo" (or when a shared link opens straight on the graph). Desktop shows the graph right away, so it loads immediately there. It's a small change; the saving depends on sizes we'll only know once built.
Stakes if we pick wrong: slower first screen on the phones journalists use in the field, or a small extra delay the first time the Grafo tab opens.
Recommendation: A because it is a few lines of Vite code splitting and speeds up the default phone screen.
Completeness: A=8/10, B=5/10
Pros / cons:
A) Load graph on demand (recommended)
  ✅ Phones render the Conexiones list without downloading or parsing the graph library
  ✅ Standard Vite dynamic import, no new dependency and no server change
  ❌ First tap on "Grafo" waits for the chunk; needs a loading state for that tab
B) One bundle
  ✅ Graph tab opens instantly on phones once the page has loaded
  ✅ Simplest build, with one fewer loading state to design and test
  ❌ Every phone pays for the graph library even when the reader never opens it
Net: a dynamic import trades a one-time wait on the Grafo tab for a faster default phone screen.
Header: Graph loading
Options:
A) Load graph on demand (recommended)
Make the graph module (Cytoscape.js + fcose + styles) a dynamic `import()`. It loads on first render at ≥ 768px, and under 768px on first open of the "Grafo" tab or a shared link with `vista=grafo`; PNG export also loads it. The Grafo tab shows the existing skeleton while it loads. Tests: Playwright at 390px asserts no graph chunk is requested until "Grafo" opens; the build prints chunk sizes. (human: ~2h / CC: ~15min)
B) One bundle
Ship the graph code in the main bundle, always loaded. (human: 0 / CC: 0)

State: approved
Actual answer: A) Load graph on demand (recommended), D12 answer 2026-09-25
Accepted scope: Make the graph module (Cytoscape.js + fcose + styles) a dynamic `import()`. It loads on first render at ≥ 768px, and under 768px on first open of the "Grafo" tab or a shared link with `vista=grafo`; PNG export also loads it. The Grafo tab shows the existing skeleton while it loads. Tests: Playwright at 390px asserts no graph chunk is requested until "Grafo" opens; the build prints chunk sizes.
History: none

Section 4 dispositions: P1 → R34 approved (A, D11). P2 → R35 approved (A, D12). No pending rows.

### Outside Voice (Eng Review 2)

Codex (`gpt-6-sol`, read-only, high effort) started but hit the five-minute limit (exit 124) before completing. Only the tail of its output was captured, so this counts as `outside_status: unavailable` (logged). The native fallback needs a background-task output tool that this session does not expose, so it was unavailable too. No outside coverage for this review. The captured tail is listed in the suppressed-findings appendix as unreviewed, with no agreement or clean credit.

## Final planning decisions: TODO proposals (Eng Review 2)

### R36: TODO: search-engine indexing of natural-person entity pages on the M1 legal-review agenda
Finding: TODO proposal, P2, confidence 6/10, design line 131 "Leads are never included in search results or server-rendered, indexable HTML (lead views are `noindex`)"; R27 (D4) sets `X-Robots-Tag` per route. Reviewer: Claude (host).
Plan baseline: lead views noindex; entity pages (including natural persons: legal reps, ordenadores, supervisors) have no stated indexing policy. M1 legal review covers masking, wording, correction flow, suppression.
Runtime evidence: no code. Google renders JavaScript pages, so client-rendered person pages can be indexed; after suppression they drop out only when recrawled (R27's 404 makes that happen).
Comparison grid:

| Choice | Current | A | B | C |
|---|---|---|---|---|
| R36 indexing policy for person pages | unstated | TODO: add "index or noindex natural-person entity pages" to the M1 legal-review agenda; R27's `X-Robots-Tag` implements the lawyer's answer | not tracked | decide now in this PR: `noindex` on all natural-person pages until the legal review says otherwise |

Question D13:
D13 — Add "should search engines index people's pages?" to the M1 legal review?
Project/branch/task: master, eng re-review of docs/designs/follow-the-money.md, TODO proposals.
ELI10: What: put one question on the lawyer's M1 agenda: may Google index the pages of natural persons (legal reps, officials who signed contracts)? Why: a person's name in Google results next to public contracts is a bigger privacy footprint than the same data inside our site, and Ley 1581 suppression is slower to undo once Google has it. Pros: the lawyer answers it with the other privacy questions, and the header that enforces it is already planned (R27). Cons: one more agenda item, and until answered the default is unclear. Context: company pages are clearly fine to index; the question is only people. Depends on: the M1 legal review booking.
Stakes if we pick wrong: people's names spread into search results we can't recall quickly, or journalists lose search discovery for no legal reason.
Recommendation: A because it is a legal judgment, it costs one line on an agenda that already exists, and the switch is already built.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Add to TODOS.md (recommended)
  ✅ The lawyer settles it alongside masking and suppression, the questions it belongs with
  ✅ Zero build cost: R27's `X-Robots-Tag` header applies whichever answer comes back
  ❌ Leaves the launch default open until the legal review answers
B) Skip
  ✅ Keeps the legal agenda short and focused on the items already listed
  ✅ Nothing to track, and nothing changes in the current plan
  ❌ People's pages get indexed by default without anyone deciding it
C) Decide now: noindex people
  ✅ The safest privacy default ships from day one, with no dependency on the legal review
  ✅ Trivial to implement in the R27 route handler, one header per person page
  ❌ Makes a legal call without the lawyer and may hide pages journalists want to find
Net: route a legal question to the lawyer, with the switch already built.
Header: Person indexing
Options:
A) Add to TODOS.md (recommended)
Add a P2 TODO under "Legal & privacy": put "index or noindex natural-person entity pages" on the M1 legal-review agenda; R27's `X-Robots-Tag` implements the answer. Depends on booking the M1 legal review.
B) Skip
Do not track it. Entity pages have no stated indexing policy.
C) Decide now: noindex people
In this plan: every natural-person entity page sends `X-Robots-Tag: noindex` until the legal review decides otherwise. Company and agency pages stay indexable.

State: approved
Actual answer: A) Add to TODOS.md (recommended), D13 answer 2026-09-25
Accepted scope: Add a P2 TODO under "Legal & privacy": put "index or noindex natural-person entity pages" on the M1 legal-review agenda; R27's `X-Robots-Tag` implements the answer. Depends on booking the M1 legal review. (Added to TODOS.md 2026-09-25.)
History: none

### R37: TODO: complete the outside voice on this review
Finding: TODO proposal, P2, confidence 9/10, Outside Voice (Eng Review 2): Codex timed out at 5 minutes, native fallback unavailable; no outside coverage for R25-R35. Reviewer: Claude (host).
Plan baseline: outside voice is a standard step of plan review; the first eng review's outside voice (Codex) found 8 issues, all applied.
Runtime evidence: Codex exit 124 after 300 s with a 32 KB prompt; tail of its output captured (appendix).
Comparison grid:

| Choice | Current | A | B | C |
|---|---|---|---|---|
| R37 outside coverage for R25-R35 | none (timed out) | TODO: run `/codex review` on the updated design doc before the T1 scaffold | none | now, in this review: rerun Codex (`gpt-6-sol`, read-only) with a 10-minute limit and a smaller prompt (this review's decisions + design-review decisions only); its findings go through the decision procedure before outputs |

Question D14:
D14 — Rerun the outside voice now with a longer limit, or track it as a TODO?
Project/branch/task: master, eng re-review of docs/designs/follow-the-money.md, TODO proposals.
ELI10: What: get the independent second opinion that timed out. Why: in the first eng review Codex found 8 real problems we had missed, and nobody outside has checked this review's 11 decisions. Pros: a second model catches blind spots before code exists, when fixes are cheapest. Cons: about 10 minutes now, plus a few more questions if it finds something. Context: it timed out at 5 minutes on a 32 KB prompt; a smaller prompt and a 10-minute limit should fit. Depends on: nothing.
Stakes if we pick wrong: we start building on 11 decisions only one model has checked, or spend 10 minutes on a pass that finds nothing new.
Recommendation: C because it is 10 minutes now versus a missed flaw found during the build, and the first run already showed signs of real findings.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Add to TODOS.md
  ✅ Finishes this review now; the second opinion runs before the scaffold starts
  ✅ Can run on the rewritten design doc, which is easier for Codex to read
  ❌ Easy to skip once building starts, and findings then reopen settled decisions
B) Skip
  ✅ No more time spent on review; go straight to outputs and the build
  ✅ Every decision already has an explicit user answer and a stated rationale
  ❌ Eleven decisions ship without any outside check, unlike the first review
C) Rerun now (recommended)
  ✅ Outside findings land in this review's ledger, resolved before any code exists
  ✅ Smaller prompt and a 10-minute limit fix the cause of the timeout
  ❌ About 10 more minutes, plus follow-up questions if it finds real issues
Net: ten minutes now buys the same second-model check the first review had.
Header: Outside voice
Options:
A) Add to TODOS.md
Add a P2 TODO under "Planning": run `/codex review` (gpt-6-sol) on the updated design doc before the T1 scaffold; resolve its findings in the doc.
B) Skip
No outside coverage for R25-R35; nothing tracked.
C) Rerun now (recommended)
Rerun Codex (`gpt-6-sol`, read-only, high effort) in this review with a 10-minute limit and a smaller prompt (this review's decisions + design-review decisions + the relevant design sections). Its findings go through the decision procedure before outputs.

State: approved
Actual answer: C) Rerun now (recommended), D14 answer 2026-09-25
Accepted scope: Rerun Codex (`gpt-6-sol`, read-only, high effort) in this review with a 10-minute limit and a smaller prompt (this review's decisions + design-review decisions + the relevant design sections). Its findings go through the decision procedure before outputs.
History: D14 was sent with the same labels and descriptions but in the order C, A, B (recommended first) instead of the saved A, B, C order.

### Outside Voice rerun (Eng Review 2, per R37)

Codex (`gpt-6-sol`, read-only, high effort, 10-minute limit, 15.7 KB prompt) completed and passed output validation. Full output:

```
CODEX SAYS (plan review — outside voice):
════════════════════════════════════════════════════════════
- **P1 — R33:** The public correction form collects names, email addresses, and unrestricted messages, but the plan defines no privacy notice, retention period, access controls, or process for removing personal data submitted in messages. Keeping ID uploads out of band does not keep IDs out of the message field.

- **P1 — R33:** `holidays` can calculate dates; it cannot establish which legal deadline applies to each request or when that clock starts. The plan needs an approved deadline rule for each intake type before displaying a due date to requesters.

- **P1 — R34:** A 12-step trail does not bound the response: each step returns pages for every edge kind, including shared-representative results computed at query time. Define a response-size and query budget before treating the 1.5-second target as feasible.

- **P1 — R27:** A permanent `301` caches a merge decision outside the app’s control, while moderator decisions and suppression can change. It also needs an explicit rule for preserving the shared-view query string; otherwise opening a merged entity can discard the trail and selection.

- **P2 — R32:** Keyset pagination needs a unique tie-breaker for equal values, counts, dates, and names. Nightly ingest can change those sort keys between requests, so the stated “no gaps or repeats” guarantee also needs a snapshot or versioned cursor.

- **P2 — R29:** The natural-person threshold depends on identity classification, yet the flag is assigned at contract ingest. The plan does not say how identity merges, corrections, or later RUES data recompute flags and totals. Displaying a reported zero as “sin valor reportado” also conflates zero with missing data.

- **P2 — R30:** Hashes and a gitignored font directory do not make builds reproducible. A cold build depends on the pinned external URLs remaining available; a CI cache is not an asset source. Specify a durable, accessible source for the verified font files.

- **P2 — R19/R34:** The URL stores expansion node ULIDs, but expansion has a kind filter. Restoring every kind’s first page cannot reproduce a filtered trail, despite the “full view state” promise. Encode each step’s filter and define what happens to deeper table pages.

**Recommendation: revise the plan before M1 implementation because correction intake, redirects, pagination, and shared-view restoration currently promise behavior their specified state and data lifecycle cannot guarantee.**
════════════════════════════════════════════════════════════
```

Cross-model tension (host assessment of each outside finding):
- X1 R33 requester data (P1): agreed. The Ley 1581 correction-flow TODO covers ID copies only; ticket rows and free-text messages have no notice, access or retention rule. → R38.
- X2 R33 deadline clock (P1): agreed. Ley 1581 runs from receipt of a complete request, and an incomplete reclamo pauses the clock, so a due date fixed at submission can be wrong. → R39.
- X3 R34 response bound (P1): agreed. 12 steps × every kind × a 50-row page is unbounded in practice; the graph only draws 8 per kind (9A). → R40.
- X4 R27 permanent redirect (P1): agreed. Browsers cache a 301 indefinitely, while merges can be split later; the query string must be carried explicitly. → R41.
- X5 R32 pagination (P2): agreed on both parts: each sort needs the entity ULID as a final tie-breaker, and a nightly ingest between pages breaks "no gaps or repeats". → R42.
- X6 R29 flag inputs (P2): partly agreed. **Factual correction, no question:** the natural-person test uses the contract row's own `tipodocproveedor` (CC vs NIT), which is present at ingest, so identity merges or RUES changes never recompute it. Agreed on the zero display: a reported 0 is not "sin valor reportado". → R43.
- X7 R30 font source (P2): agreed. A CI cache is not an asset source, and the license allows private self-hosted copies. → R44.
- X8 19A/R34 per-step filters (P2): agreed. Each expansion has a kind filter (18A) that the trail does not encode. → R45.

### R38: Requester data in correction tickets (notice, access, retention)
Finding: X1, P1, confidence 8/10, R33 (D10); design line 121; TODOS.md "Resolve the open office-hours correction-flow concerns" (R3-12 covers ID copies only). Reviewer: Codex (outside), confirmed by Claude.
Plan baseline: R33 approved: ticket stores type, entity ULID, requester name, email and message. No privacy notice, access rule or retention for these rows.
Runtime evidence: no code.
Comparison grid:

| Choice | Current | A | B | C | D |
|---|---|---|---|---|---|
| R38 ticket personal data | unspecified | form shows an "aviso de privacidad" and requires an authorization checkbox; help text asks not to include document numbers; tickets live in the private `ftm` schema, readable only through the admin CLI; ticket rows (name, email, message) are deleted a fixed period after closure, the period set by the M1 legal review and added to its agenda; test that the deletion job removes closed tickets past the period | unchanged | investigate: ask the M1 legal review for the notice text and retention period first; nothing built until answered | defer this proposal only; R33 unchanged |

Question D15:
D15 — Add a privacy notice, access limits and a deletion period for correction-request data?
Project/branch/task: master, eng re-review of docs/designs/follow-the-money.md, outside-voice finding X1.
ELI10: The correction form we just approved stores each requester's name, email and message. That makes us responsible for that data under the same law (Ley 1581) that the form exists to honor. The plan says nothing about telling people how we use it, who can read it, or when we delete it. People will also type their cédula into the message box even if we ask them not to. Codex caught this; I agree.
Stakes if we pick wrong: the habeas-data form itself breaks habeas-data law, which is the first thing a hostile lawyer would check.
Recommendation: A because a notice, CLI-only access and a deletion job are small, and the lawyer only needs to supply the notice wording and the period.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Apply this change (recommended)
  ✅ The form meets Ley 1581's own notice and authorization requirement from launch day
  ✅ Ticket data has one reader (the maintainer, via CLI) and a scheduled deletion
  ❌ Needs the lawyer's notice text and retention period before launch, one more agenda item
B) Keep this row's current value
  ✅ No extra work beyond the approved R33 endpoint
  ✅ Nothing new is added to the legal-review agenda
  ❌ Stores personal data with no notice, access rule or deletion period
C) Investigate before choosing
  ✅ The lawyer's answers arrive before any code, so nothing needs rework later
  ✅ Keeps this review from guessing at the legal wording
  ❌ The ticket-data design stays open until the legal review happens
D) Defer this proposed change only
  ✅ Finishes this review sooner, with R33 left exactly as approved
  ✅ Keeps the proposal on record for a later decision
  ❌ The gap stays open and could reach launch without anyone deciding it
Net: a small notice and deletion job keeps the correction flow from creating the problem it solves.
Header: Ticket privacy
Options:
A) Apply this change (recommended)
The form shows an "aviso de privacidad" and requires an authorization checkbox; help text asks people not to include document numbers. Tickets live in the private `ftm` schema, readable only through the admin CLI. Ticket rows (name, email, message) are deleted a fixed period after closure; the period and the notice text go on the M1 legal-review agenda. Test: the deletion job removes closed tickets past the period and keeps open ones.
B) Keep this row's current value
R33 stays as approved, with no notice, access rule or retention for ticket data.
C) Investigate before choosing
Ask the M1 legal review for the notice text and retention period first; build nothing for this until it answers. R33 otherwise unchanged.
D) Defer this proposed change only
Leave this proposal unresolved and R33 unchanged; other choices unaffected.

State: approved
Actual answer: A) Apply this change (recommended), D15 answer 2026-09-25
Accepted scope: The form shows an "aviso de privacidad" and requires an authorization checkbox; help text asks people not to include document numbers. Tickets live in the private `ftm` schema, readable only through the admin CLI. Ticket rows (name, email, message) are deleted a fixed period after closure; the period and the notice text go on the M1 legal-review agenda. Test: the deletion job removes closed tickets past the period and keeps open ones.
History: none

### R39: When the legal deadline clock starts for a correction request
Finding: X2, P1, confidence 7/10, R33 (D10) "deadline computed at receipt ... shown on the confirmation"; design lines 120-121 (consulta 10 / reclamo 15 business days; verification out of band). Reviewer: Codex (outside), confirmed by Claude.
Plan baseline: R33 approved: deadline computed at submission in Colombian business days and shown on the confirmation.
Runtime evidence: no code. Ley 1581 art. 14-15 count from receipt of the request, and an incomplete reclamo triggers a request to complete it, so the effective start can differ from the submission time (legal detail to confirm with the lawyer).
Comparison grid:

| Choice | Current | A | B | C | D |
|---|---|---|---|---|---|
| R39 deadline start | at submission, one fixed date shown | the confirmation shows the legal term ("10 / 15 días hábiles desde la recepción de la solicitud completa") and a date labeled "fecha estimada"; the ticket stores `completa_desde`, set by the maintainer via CLI once the request is complete (e.g. after out-of-band verification); the deadline is recomputed from it; an incomplete reclamo is marked `pendiente_de_completar` and its clock paused; the per-type start rule goes on the M1 legal-review agenda; tests: recompute on completion, paused state, CLI ordering by effective deadline | unchanged | investigate: ask the lawyer for the clock-start rule first; R33's date display on hold until answered | defer this proposal only; R33 unchanged |

Question D16:
D16 — Show an estimated due date and restart the legal clock when a request becomes complete?
Project/branch/task: master, eng re-review of docs/designs/follow-the-money.md, outside-voice finding X2.
ELI10: R33 prints a fixed due date the moment someone submits the form. But under Ley 1581 the clock runs from when we have a complete request: for example, after the person proves who they are, or fills in what was missing. If we print a firm date at submission, we either promise too early or quietly miss the real deadline. Labeling the date "estimated", letting the maintainer mark when a request is complete, and recomputing from then keeps the date honest. The lawyer confirms the exact rule.
Stakes if we pick wrong: the site shows a legal due date that is wrong, either misleading the requester or hiding a real missed deadline.
Recommendation: A because it keeps the approved ticket screen but ties the date to the event the law actually counts from.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Apply this change (recommended)
  ✅ The due date the maintainer works to follows the legal clock, including pauses for incomplete reclamos
  ✅ Requesters see the legal term plus an honest "estimated" date instead of a false promise
  ❌ The maintainer must mark each request complete in the CLI, one more manual step per ticket
B) Keep this row's current value
  ✅ Simplest: one date computed at submission, no extra ticket state
  ✅ No additional step for the maintainer when handling each request
  ❌ Can display a legally wrong deadline whenever verification or completion takes time
C) Investigate before choosing
  ✅ The lawyer's exact rule is known before anything is built around it
  ✅ Avoids encoding a legal interpretation that might be wrong
  ❌ The due-date display stays undecided until the legal review, blocking the confirmation screen
D) Defer this proposed change only
  ✅ Finishes this review sooner, with R33 left exactly as approved
  ✅ Keeps the proposal on record for a later decision
  ❌ The fixed-date behavior stays in the plan unless someone reopens it
Net: an "estimated" label and a completion timestamp make the legal date match the law's clock.
Header: Deadline clock
Options:
A) Apply this change (recommended)
The confirmation shows the legal term ("10 / 15 días hábiles desde la recepción de la solicitud completa") and a date labeled "fecha estimada". The ticket stores `completa_desde`, which the maintainer sets via the CLI once the request is complete (e.g. after out-of-band verification), and the deadline is recomputed from it. An incomplete reclamo is marked `pendiente_de_completar` with its clock paused. The per-type start rule goes on the M1 legal-review agenda. Tests: recompute on completion, paused state, CLI ordering by effective deadline.
B) Keep this row's current value
R33 stays as approved: one deadline computed at submission and shown as the due date.
C) Investigate before choosing
Ask the M1 legal review for the clock-start rule first; the confirmation's date display is on hold until it answers. R33 otherwise unchanged.
D) Defer this proposed change only
Leave this proposal unresolved and R33 unchanged; other choices unaffected.

State: approved
Actual answer: A) Apply this change (recommended), D16 answer 2026-09-25
Accepted scope: The confirmation shows the legal term ("10 / 15 días hábiles desde la recepción de la solicitud completa") and a date labeled "fecha estimada". The ticket stores `completa_desde`, which the maintainer sets via the CLI once the request is complete (e.g. after out-of-band verification), and the deadline is recomputed from it. An incomplete reclamo is marked `pendiente_de_completar` with its clock paused. The per-type start rule goes on the M1 legal-review agenda. Tests: recompute on completion, paused state, CLI ordering by effective deadline.
History: none

### R40: Bound the size of the `/vista` response
Finding: X3, P1, confidence 8/10, R34 (D11) "returns the focus, each trail expansion (first page per kind ...)"; design-review 9A (graph draws 8 per kind). Reviewer: Codex (outside), confirmed by Claude.
Plan baseline: R34 approved: `/vista` returns every trail step's first page per kind (50 rows) plus focus and selection; 12-step cap; p95 ≤ 1.5 s.
Runtime evidence: no code. Worst case: 12 steps × up to 7 edge kinds × 50 rows = 4,200 rows plus query-time shared-rep computation per step.
Comparison grid:

| Choice | Current | A | B | C | D |
|---|---|---|---|---|---|
| R40 `/vista` payload per step | first table page (50) per kind | graph data only: top 8 per kind (9A) + per-kind totals; the 50-row table page (R32 sort/filter) only for the one step the table shows; worst case 12 × 7 × 8 = 672 neighbors; shared-rep query per step limited to the same 8 + total; load test on 12-step trails through top-100 hubs with response size recorded | unchanged | investigate: measure response size and time on staging with real hubs before choosing | defer this proposal only; R34 unchanged |

Question D17:
D17 — Make `/vista` return only what the graph draws (8 per kind), plus one table page?
Project/branch/task: master, eng re-review of docs/designs/follow-the-money.md, outside-voice finding X3.
ELI10: The shared-link endpoint we just approved returns, for each of up to 12 steps, a full 50-row page for every kind of connection. That can reach thousands of rows, and the graph only ever draws 8 per kind anyway (the rest become a "+N · ver en tabla" pill). The table shows one list at a time. So each step only needs the 8 drawn neighbors and the totals, plus a single 50-row page for whichever list the table is showing. That caps the worst case at about 670 neighbors.
Stakes if we pick wrong: shared links to busy entities load megabytes and miss the 1.5-second target on phones.
Recommendation: A because it sends exactly what the screen shows and makes the 1.5-second budget achievable.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Apply this change (recommended)
  ✅ Worst case drops from about 4,200 rows to about 670, matching what the graph draws
  ✅ The table still restores its sorted and filtered first page for the step it shows
  ❌ Opening another step's table afterwards costs one extra request
B) Keep this row's current value
  ✅ Every step's table page is already loaded, so switching lists is instant
  ✅ One response shape shared with the regular expand endpoint
  ❌ Unbounded in practice; busy trails blow the 1.5 s budget and phone data
C) Investigate before choosing
  ✅ Real hub data decides the payload rule instead of estimates
  ✅ Might show the current shape is fine for most real links
  ❌ The endpoint's contract stays open until staging has national data
D) Defer this proposed change only
  ✅ Finishes this review sooner, with R34 left exactly as approved
  ✅ Keeps the proposal on record for a later decision
  ❌ The unbounded payload stays in the plan unless someone reopens it
Net: send what is drawn, not everything that could be listed.
Header: Vista size
Options:
A) Apply this change (recommended)
For each trail step `/vista` returns graph data only: the top 8 per kind (9A) plus per-kind totals. The 50-row table page (R32 sort/filter) is returned only for the one step the table shows. Worst case 12 × 7 × 8 = 672 neighbors; the shared-rep query per step is limited to the same 8 + total. The R15 load test adds 12-step trails through the top-100 hubs and records response size.
B) Keep this row's current value
R34 stays as approved: every trail step returns its first 50-row page per kind.
C) Investigate before choosing
Measure `/vista` response size and time on staging with real hubs before choosing a payload rule; R34 otherwise unchanged.
D) Defer this proposed change only
Leave this proposal unresolved and R34 unchanged; other choices unaffected.

State: approved
Actual answer: A) Apply this change (recommended), D17 answer 2026-09-25
Accepted scope: For each trail step `/vista` returns graph data only: the top 8 per kind (9A) plus per-kind totals. The 50-row table page (R32 sort/filter) is returned only for the one step the table shows. Worst case 12 × 7 × 8 = 672 neighbors; the shared-rep query per step is limited to the same 8 + total. The R15 load test adds 12-step trails through the top-100 hubs and records response size.
History: none

### R41: Redirect type for merged entities, and keeping the shared view across it
Finding: X4, P1, confidence 9/10, R27 (D4) "merged → 301 to the surviving ULID"; design line 84 (splits after merges possible); 19A "merge → redirect keeping the trail". Reviewer: Codex (outside), confirmed by Claude.
Plan baseline: R27 approved: merged → 301. 19A approved: the redirect keeps the trail.
Runtime evidence: no code. Browsers cache 301 responses without an expiry unless told otherwise; a fragment (`#...`) survives a redirect when `Location` has none, but a query string survives only if the server copies it.
Comparison grid:

| Choice | Current | A | B | C | D |
|---|---|---|---|---|---|
| R41 merge redirect | 301 (cached by browsers) | 302 with `Cache-Control: no-store`; `Location` copies the request's query string; the fragment passes through unchanged; test: merged URL with query and fragment lands on the survivor with both intact, and a later split changes the redirect target on the next request | unchanged (301) | investigate caching behavior across target browsers first | defer this proposal only; R27 unchanged |

Question D18:
D18 — Use a temporary (302) redirect for merged entities and carry the shared view across it?
Project/branch/task: master, eng re-review of docs/designs/follow-the-money.md, outside-voice finding X4.
ELI10: When two entities are merged, the old address redirects to the one that remains. I proposed a "301 permanent" redirect, but browsers remember a 301 forever. If a moderator later splits that merge, anyone who already visited keeps getting sent to the wrong entity, and we can't fix it from our side. A "302 temporary" redirect is re-checked every time. The redirect must also copy the part of the address that holds the shared view, or a colleague's link loses its trail.
Stakes if we pick wrong: an undone merge keeps sending people to the wrong entity from their own browser cache, or shared links lose their view on redirect.
Recommendation: A because merges are reversible, so the redirect must be too, and carrying the view is required by 19A anyway.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Apply this change (recommended)
  ✅ A reversed merge takes effect on the next visit, never stuck in anyone's browser
  ✅ Shared links keep their trail and selection across the redirect, as 19A requires
  ❌ Every visit to a merged URL costs one extra round trip that a cached 301 would skip
B) Keep this row's current value
  ✅ Browsers skip the redirect request after the first visit to a merged URL
  ✅ Search engines consolidate the old URL into the survivor faster
  ❌ An undone merge can't be corrected in visitors' browsers, and the query string may be dropped
C) Investigate before choosing
  ✅ Browser caching evidence in hand before choosing the status code
  ✅ Might find a cached-but-revalidated middle ground worth using
  ❌ R27's redirect stays unresolved while a well-known behavior gets retested
D) Defer this proposed change only
  ✅ Finishes this review sooner, with R27 left exactly as approved
  ✅ Keeps the proposal on record for a later decision
  ❌ The permanent redirect stays in the plan unless someone reopens it
Net: reversible merges need a redirect we can take back.
Header: Merge redirect
Options:
A) Apply this change (recommended)
Merged entity URLs return 302 with `Cache-Control: no-store`. `Location` copies the request's query string, and the fragment passes through unchanged. Test: a merged URL with a query and fragment lands on the survivor with both intact, and after a split the next request redirects to the new target.
B) Keep this row's current value
R27 stays as approved: merged → 301.
C) Investigate before choosing
Test redirect caching across target browsers before choosing; R27 otherwise unchanged.
D) Defer this proposed change only
Leave this proposal unresolved and R27 unchanged; other choices unaffected.

State: approved
Actual answer: A) Apply this change (recommended), D18 answer 2026-09-25
Accepted scope: Merged entity URLs return 302 with `Cache-Control: no-store`. `Location` copies the request's query string, and the fragment passes through unchanged. Test: a merged URL with a query and fragment lands on the survivor with both intact, and after a split the next request redirects to the new target. (Amends R27's "merged → 301".)
History: none

### R42: Table pages when the data changes between requests
Finding: X5, P2, confidence 8/10, R32 (D9) "each sort key pages without gaps or repeats"; first eng review: nightly ingest (R7) and moderator actions (R17) change rows. Reviewer: Codex (outside), confirmed by Claude.
Plan baseline: R32 approved: keyset cursor per sort key, "no gaps or repeats".
Carried without a question (required implementation of R32's approved contract): every sort key ends with the neighbor's entity ULID as the final tie-breaker, so equal values, counts, dates or names page deterministically.
Runtime evidence: no code. Nightly ingest and moderator actions can change sort keys between two page requests.
Comparison grid:

| Choice | Current | A | B | C | D |
|---|---|---|---|---|---|
| R42 cursor across data changes | undefined; R32 promises no gaps or repeats | the cursor carries a data revision (incremented by each ingest run and moderator action that touches the entity); if it changed, the API says so and the table restarts at page 1 with "Los datos se actualizaron; la lista se reinició"; test: revision bump between pages | unchanged (guarantee holds only while data is still) | investigate how often revisions change mid-session on staging first | defer this proposal only; R32 unchanged |
| ULID tie-breaker | missing | carried | carried | carried | carried |

Question D19:
D19 — If the data changes while someone pages the table, restart the list with a notice?
Project/branch/task: master, eng re-review of docs/designs/follow-the-money.md, outside-voice finding X5.
ELI10: The table loads 50 rows at a time and promises no skipped or repeated rows. But the data refreshes every night, and a moderator can merge or hide something at any moment. If that happens between page 1 and page 2, rows shift and the promise breaks quietly. The fix: each page remembers which version of the data it came from; if the version changed, the table starts over with a short notice. (Separately, and required anyway: ties in the sort get broken by entity ID so the order is always fixed.)
Stakes if we pick wrong: a journalist paging through a long list silently misses or double-counts rows after a refresh.
Recommendation: A because a version number on the cursor turns a silent error into a visible, rare restart.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Apply this change (recommended)
  ✅ Paging never silently skips or repeats a row, even across a nightly refresh
  ✅ The restart is rare and explained to the reader in one line
  ❌ Needs a revision counter maintained by ingest and every moderator action
B) Keep this row's current value
  ✅ No revision counter to maintain across ingest and moderator code
  ✅ Refreshes run at night, so most sessions never hit the problem
  ❌ When it does happen, rows are skipped or repeated with no sign to the reader
C) Investigate before choosing
  ✅ Staging data shows how often this actually happens before building for it
  ✅ Might show a simpler fix is enough, such as only nightly changes
  ❌ The paging guarantee stays unproven until staging runs with national data
D) Defer this proposed change only
  ✅ Finishes this review sooner, with R32 left exactly as approved
  ✅ Keeps the proposal on record for a later decision
  ❌ The guarantee stays overstated unless someone reopens it
Net: a small version check keeps R32's promise true instead of usually true.
Header: Paging drift
Options:
A) Apply this change (recommended)
The table cursor carries a data revision, incremented by each ingest run and each moderator action that touches the entity. If the revision changed since page 1, the API says so and the table restarts at page 1 with "Los datos se actualizaron; la lista se reinició". Test: a revision bump between pages triggers the restart. The ULID tie-breaker is carried either way.
B) Keep this row's current value
R32 stays as approved; the no-gaps guarantee holds only while data is unchanged. The ULID tie-breaker is carried either way.
C) Investigate before choosing
Measure on staging how often revisions change mid-session before choosing; R32 otherwise unchanged. The ULID tie-breaker is carried either way.
D) Defer this proposed change only
Leave this proposal unresolved and R32 unchanged; other choices unaffected. The ULID tie-breaker is carried either way.

State: approved
Actual answer: A) Apply this change (recommended), D19 answer 2026-09-25
Accepted scope: The table cursor carries a data revision, incremented by each ingest run and each moderator action that touches the entity. If the revision changed since page 1, the API says so and the table restarts at page 1 with "Los datos se actualizaron; la lista se reinició". Test: a revision bump between pages triggers the restart. Carried: every sort key ends with the neighbor's entity ULID as the final tie-breaker.
History: none

### R43: How a reported zero value is shown
Finding: X6, P2, confidence 8/10, R29 (D6) "Value 0 shows as \"sin valor reportado\"". Reviewer: Codex (outside), confirmed by Claude. Factual correction recorded without a question: R29's natural-person test reads the contract row's own `tipodocproveedor` (CC vs NIT), available at ingest, so identity merges and RUES changes never recompute the flag.
Plan baseline: R29 approved: a value of 0 shows as "sin valor reportado".
Runtime evidence: probe: 196,554 contracts report exactly 0 (e.g. convenios with no amount). SECOP can also leave the value empty.
Comparison grid:

| Choice | Current | A | B | C | D |
|---|---|---|---|---|---|
| R43 zero vs missing value display | 0 → "sin valor reportado" | a reported 0 shows as "COP 0" (as SECOP reports it); an empty or null value shows as "sin valor reportado"; both add nothing to sums and sort after positive values; test for each case | unchanged | investigate which contract types report 0 before choosing wording | defer this proposal only; R29 unchanged |

Question D20:
D20 — Show a reported zero as "COP 0" and keep "sin valor reportado" for truly empty values?
Project/branch/task: master, eng re-review of docs/designs/follow-the-money.md, outside-voice finding X6.
ELI10: In R29 I proposed showing a contract value of 0 as "sin valor reportado" (no value reported). Codex pointed out that's not the same thing: SECOP did report a value, and it was zero, which happens with agreements that carry no amount. An empty field is the case where nothing was reported. Saying "no value reported" for a reported zero misstates the source, which is the one thing this site must never do.
Stakes if we pick wrong: 196,554 contracts get described as missing data when the official record says zero.
Recommendation: A because every label should say exactly what the source says.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Apply this change (recommended)
  ✅ Each label matches the official record: a reported zero reads as zero, a blank reads as not reported
  ✅ Sums and sorting are unaffected, since both add nothing and sort last
  ❌ "COP 0" on a real contract may look like an error to readers who don't know convenios
B) Keep this row's current value
  ✅ One label for both cases, simpler for readers skimming the table
  ✅ Avoids a column full of "COP 0" rows that look like errors
  ❌ Describes a reported zero as missing data, which misstates the source
C) Investigate before choosing
  ✅ Knowing which contract types report 0 could suggest better wording
  ✅ Might reveal zeros that are really placeholders for "unknown"
  ❌ The display rule stays open for a small wording question
D) Defer this proposed change only
  ✅ Finishes this review sooner, with R29 left exactly as approved
  ✅ Keeps the proposal on record for a later decision
  ❌ The misleading label stays in the plan unless someone reopens it
Net: say what the source says; a zero is not a blank.
Header: Zero values
Options:
A) Apply this change (recommended)
A reported 0 shows as "COP 0", as SECOP reports it; an empty or null value shows as "sin valor reportado". Both add nothing to sums and sort after positive values. Tests for each case. Amends R29's zero rule only.
B) Keep this row's current value
R29 stays as approved: 0 shows as "sin valor reportado".
C) Investigate before choosing
Profile which contract types report 0 before choosing the wording; R29 otherwise unchanged.
D) Defer this proposed change only
Leave this proposal unresolved and R29 unchanged; other choices unaffected.

State: approved
Actual answer: A) Apply this change (recommended), D20 answer 2026-09-25
Accepted scope: A reported 0 shows as "COP 0", as SECOP reports it; an empty or null value shows as "sin valor reportado". Both add nothing to sums and sort after positive values. Tests for each case. Amends R29's zero rule only.
History: none

### R44: A durable private source for the verified font files
Finding: X7, P2, confidence 7/10, R30 (D7): pinned Fontshare URLs + hash manifest + CI cache; production build fails if fetch fails and cache is empty. Reviewer: Codex (outside), confirmed by Claude.
Plan baseline: R30 approved as above.
Runtime evidence: ITF Free Font License allows keeping copies for your own self-hosting; it forbids public repositories and public servers. A CI cache can be evicted at any time.
Comparison grid:

| Choice | Current | A | B | C | D |
|---|---|---|---|---|---|
| R44 font source order | CI cache → Fontshare | CI cache → private store (a private GitHub repo or private bucket owned by the project, read token in CI secrets) → Fontshare; the hash manifest checks every source; the maintainer uploads the verified files once | unchanged | investigate Fontshare URL stability over a few weeks first | defer this proposal only; R30 unchanged |

Question D21:
D21 — Keep a private copy of the verified font files so builds don't depend on Fontshare?
Project/branch/task: master, eng re-review of docs/designs/follow-the-money.md, outside-voice finding X7.
ELI10: We just decided the build stops if it can't get the real fonts. It gets them from the CI cache or from Fontshare's download links. But caches get wiped, and Fontshare can move or change files, so one day a release could be blocked with no way around it. The font license does allow us to keep our own private copy for self-hosting, just not a public one. A private repo or storage bucket holding the checked files gives every build a source we control.
Stakes if we pick wrong: a production release blocked by a third party's changed URL, or an accidental public copy that breaks the license.
Recommendation: A because a private copy is allowed, costs one upload, and removes the only external dependency in the build.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Apply this change (recommended)
  ✅ Builds never block on Fontshare: the private copy is always there after the cache
  ✅ Same hash check on every source, so a tampered or changed file still fails loudly
  ❌ One more private store and CI secret to maintain, and it must never be made public
B) Keep this row's current value
  ✅ No extra storage or secrets; Fontshare stays the only origin of the files
  ✅ Nothing that could accidentally be published
  ❌ A wiped cache plus a moved Fontshare URL blocks releases until someone intervenes
C) Investigate before choosing
  ✅ Evidence on how stable Fontshare's URLs are before adding storage
  ✅ Might show the risk is small enough to leave as is
  ❌ The build's failure mode stays open while waiting weeks for evidence
D) Defer this proposed change only
  ✅ Finishes this review sooner, with R30 left exactly as approved
  ✅ Keeps the proposal on record for a later decision
  ❌ The single external dependency stays until someone reopens it
Net: a private, license-compliant copy makes font-strict builds dependable.
Header: Font source
Options:
A) Apply this change (recommended)
Build order for fonts: CI cache → a private store owned by the project (private GitHub repo or private bucket, read token in CI secrets) → Fontshare. The hash manifest checks every source. The maintainer uploads the verified files once. The store must stay private (ITF license).
B) Keep this row's current value
R30 stays as approved: CI cache, then Fontshare.
C) Investigate before choosing
Watch Fontshare URL stability for a few weeks before choosing; R30 otherwise unchanged.
D) Defer this proposed change only
Leave this proposal unresolved and R30 unchanged; other choices unaffected.

State: approved
Actual answer: A) Apply this change (recommended), D21 answer 2026-09-25
Accepted scope: Build order for fonts: CI cache → a private store owned by the project (private GitHub repo or private bucket, read token in CI secrets) → Fontshare. The hash manifest checks every source. The maintainer uploads the verified files once. The store must stay private (ITF license).
History: none

### R45: Encode each expansion step's kind filter in the shared URL
Finding: X8, P2, confidence 8/10, design-review 18A ("\"+\" handle ... expand it, with a kind filter") and 19A ("full view state in URL"); R34/R40 `/vista`. Reviewer: Codex (outside), confirmed by Claude.
Plan baseline: 19A approved: URL encodes focus, ordered trail of node ULIDs, selection, table sort/filter, tab. 18A approved: expansions can be filtered by kind. R40 approved: `/vista` returns the top 8 per kind for each step.
Runtime evidence: no code. A trail of bare ULIDs cannot say which kinds each step expanded.
Comparison grid:

| Choice | Current | A | B | C | D |
|---|---|---|---|---|---|
| R45 trail step encoding | node ULID only | node ULID plus its expanded kinds (e.g. `ruta=<ulid>~rep,contrato.<ulid>`; kind names only, never ID numbers); `/vista` applies each step's kinds; table pages beyond the first are not restored (the table reopens at page 1 of its sort/filter), stated in /metodologia; test: a filtered trail restores exactly the same nodes | unchanged (every kind restored per step) | investigate URL length with real trails first | defer this proposal only; 19A/R34 unchanged |

Question D22:
D22 — Save which kinds each step expanded, so a shared link reopens exactly the same graph?
Project/branch/task: master, eng re-review of docs/designs/follow-the-money.md, outside-voice finding X8.
ELI10: When you expand a node you can pick which kinds of links to show, say only legal reps. The shared link records which nodes you expanded, but not those choices. So your editor opening the link sees every kind of link on each step, a busier graph than the one you sent. Adding a few kind names per step to the link (never any ID numbers) fixes that. One limit stays: if you had scrolled the table to page 3, the link reopens it at page 1.
Stakes if we pick wrong: the "full view state in URL" promise fails, and the person receiving a link sees a different graph than the sender.
Recommendation: A because it keeps the 19A promise with a few characters per step, and it states the one thing a link does not restore.
Note: options differ in kind, not coverage — no completeness score.
Pros / cons:
A) Apply this change (recommended)
  ✅ The recipient sees exactly the graph the sender saw, filters included
  ✅ Only kind names are added, so no ID numbers ever enter the URL
  ❌ Longer URLs and one more parser case; deep table pages still reset to page 1
B) Keep this row's current value
  ✅ Shorter, simpler URLs with one ULID per step
  ✅ No change to the approved `/vista` parameters
  ❌ Shared links reopen with every kind expanded, not the view that was sent
C) Investigate before choosing
  ✅ Real trails show whether URL length is a problem before choosing a format
  ✅ Might show filtered expansions are rare enough to ignore
  ❌ The URL format stays open while the web build needs it
D) Defer this proposed change only
  ✅ Finishes this review sooner, with 19A and R34 left exactly as approved
  ✅ Keeps the proposal on record for a later decision
  ❌ Shared views stay inexact until someone reopens it
Net: a few characters per step make shared links reopen the view that was actually sent.
Header: Trail filters
Options:
A) Apply this change (recommended)
Each trail step encodes its node ULID plus its expanded kinds (e.g. `ruta=<ulid>~rep,contrato.<ulid>`; kind names only, never ID numbers). `/vista` applies each step's kinds. Table pages beyond the first are not restored: the table reopens at page 1 of its sort/filter, and /metodologia says so. Test: a filtered trail restores exactly the same nodes.
B) Keep this row's current value
19A and R34 stay as approved: bare ULIDs per step, every kind restored.
C) Investigate before choosing
Measure URL length with real trails before choosing a format; 19A and R34 otherwise unchanged.
D) Defer this proposed change only
Leave this proposal unresolved and 19A/R34 unchanged; other choices unaffected.

State: approved
Actual answer: A) Apply this change (recommended), D22 answer 2026-09-25
Accepted scope: Each trail step encodes its node ULID plus its expanded kinds (e.g. `ruta=<ulid>~rep,contrato.<ulid>`; kind names only, never ID numbers). `/vista` applies each step's kinds. Table pages beyond the first are not restored: the table reopens at page 1 of its sort/filter, and /metodologia says so. Test: a filtered trail restores exactly the same nodes.
History: none

Outside-voice dispositions: X1 → R38 (A, D15). X2 → R39 (A, D16). X3 → R40 (A, D17). X4 → R41 (A, D18). X5 → R42 (A, D19; ULID tie-breaker carried). X6 → factual correction + R43 (A, D20). X7 → R44 (A, D21). X8 → R45 (A, D22). No remaining disagreement.

Approval readiness: PASS. Checked R25 (D1), R26 (D2), R27 (D4; D3 unanswered, re-asked), R28 (D5), R29 (D6), R30 (D7), R31 (D8), R32 (D9), R33 (D10), R34 (D11), R35 (D12), R36 (D13), R37 (D14), R38 (D15), R39 (D16), R40 (D17), R41 (D18), R42 (D19), R43 (D20), R44 (D21), R45 (D22). Complexity gate: prior D3/D4. Carried without new questions: R30 canvas wait, private images, no-third-party E2E (DESIGN.md font rules); R42 ULID tie-breaker (R32 contract); X6 factual correction; design-review test requirements. Regression contracts: none needed (greenfield). No pending or deferred rows.

## Review body (Eng Review 2)

### Working plan: amendments applied to this file

The sections above now carry R1-R24, the design-review decisions, DESIGN.md, the name and R26-R45 (R25). Summary of what this review added:

| Area | Decision | Source |
|---|---|---|
| Name | slug `cabosueltos` for package, org, PyPI, domains | R26 (D2) |
| Serving | FastAPI serves the Vite build; per-route 200 / 302 no-store / 404 identical; CSP, `X-Robots-Tag` | R27 (D4), R41 (D18) |
| Edge IDs | derived-edge keys from kind + ULIDs; `/edge/:key/evidence?cursor=` | R28 (D5) |
| Values | `valor_atipico` flag, excluded from sums and value order; "COP 0" vs "sin valor reportado" | R29 (D6), R43 (D20) |
| Fonts | pinned, SHA-256 manifest, CI cache → private store → Fontshare, fail-closed prod build | R30 (D7), R44 (D21) |
| Reference | entity-page.html is a spec; Cytoscape.js + fcose; no Pretext | R31 (D8) |
| Table | server-side sort/filter, keyset cursors, ULID tie-breaker, revision restart | R32 (D9), R42 (D19) |
| Corrections | `POST /api/correcciones`, CO business days, `completa_desde`, rate limit + honeypot, email, CLI, privacy notice, retention | R33 (D10), R38 (D15), R39 (D16) |
| Shared links | `/api/vista`, 12-step cap, top 8 per kind + one table page, per-step kinds in URL | R34 (D11), R40 (D17), R45 (D22) |
| Mobile | graph module lazy-loaded until "Grafo" | R35 (D12) |
| TODOs | person-page indexing on the legal agenda; outside voice rerun (done) | R36 (D13), R37 (D14) |

### M1 request flow (as of this review)

```
 browser ──GET /entidad/:ulid?...#view──► FastAPI (one origin, CSP font-src 'self')
                                            │ lookup ulid (redirect / suppression / split)
                        ┌───────────────────┼──────────────────┬──────────────────┐
                        ▼                   ▼                  ▼                  ▼
                   200 shell        302 no-store →       200 shell +        404 shell
                   (found)          survivor, query      chooser (split)    (unknown OR
                                    copied, #frag kept                       suppressed,
                                                                             identical body)
 shell loads ─► fonts.load() ─► GET /api/vista?foco&ruta=<ulid>~kinds...&sel&orden&filtro
                                   │ resolve every ULID once (redirects); drop vanished /
                                   │ suppressed / split trail nodes → one notice; cap 12
                                   ▼
                     per step: top 8 per kind + totals; one 50-row table page
                                   │
     graph (Cytoscape, lazy on phones) ◄──┴──► Conexiones table
        │ select edge key                          │ orden/filtro, cursor{key, ulid, rev}
        ▼                                          ▼ rev changed → restart page 1
 GET /api/edge/:key/evidence?cursor   GET /api/entity/:id/expand?kind&orden&filtro&cursor
        │ 5 records/page, raw value + note if valor_atipico
        ▼
 drawer → Copiar cita / Ver registro oficial      "Solicitar corrección" → POST /api/correcciones
                                                   → ticket CS-<año>-<n>, fecha estimada,
                                                     email (retry), CLI tickets --vencen
```

Files that need an inline diagram comment when built: `backend/cabosueltos/api/vista.py` (resolve → drop rule → per-step payload), `backend/cabosueltos/api/edges.py` (key grammar and parse), `backend/cabosueltos/resolver/tickets.py` (ticket states: recibida → completa / pendiente_de_completar → cerrada → borrada).

### Test review: coverage diagram (new paths from this review; all planned, greenfield)

```
CODE PATHS                                             USER FLOWS
[+] api/web.py (R27, R41)                              [+] Open a shared link
  ├── [GAP] found → 200 shell                            ├── [GAP] [→E2E] trail + kinds + sort restored (R45)
  ├── [GAP] merged → 302 no-store, query + #frag kept    ├── [GAP] [→E2E] merged node in trail → survivor
  ├── [GAP] split → chooser                              └── [GAP] [→E2E] suppressed node = vanished, one notice
  └── [GAP] unknown vs suppressed → identical 404      [+] Read a busy entity
[+] api/edges.py + evidence (R28)                        ├── [GAP] [→E2E] sort by Contratos = true top (R32)
  ├── [GAP] format/parse round trip, sorted pair         ├── [GAP] page 2 after ingest → restart notice (R42)
  ├── [GAP] redirect inside key                          └── [GAP] 8.8e20 value not in totals (R29)
  └── [GAP] suppressed ULID in key → 404               [+] Request a correction
[+] ingest values (R29, R43)                             ├── [GAP] [→E2E] submit → ticket + fecha estimada
  ├── [GAP] > 20 billones flagged; CC services > 5.000M  ├── [GAP] rate limit / honeypot
  ├── [GAP] 0 → "COP 0"; null → "sin valor reportado"     └── [GAP] email down → ticket kept, retried
  └── [GAP] sums and order skip flagged                [+] Phone (390px)
[+] api/expand (R32, R42)                                └── [GAP] [→E2E] no graph chunk until "Grafo" (R35)
  ├── [GAP] 4 sort keys page without gaps (ULID ties)  [+] Fonts
  └── [GAP] revision change → restart signal             ├── [GAP] tampered file → build fails (R30)
[+] api/vista (R34, R40, R45)                            ├── [GAP] cache + store + Fontshare down → prod fails
  ├── [GAP] 12-step cap; payload ≤ 672 neighbors         └── [GAP] [→E2E] no request leaves the origin
  └── [GAP] load: p95 ≤ 1.5 s on top-100 hub trails
[+] resolver/tickets (R33, R38, R39)
  ├── [GAP] CO holidays (Monday festivos, year end)
  ├── [GAP] completa_desde recompute; paused reclamo
  └── [GAP] retention job deletes closed past period
COVERAGE (planned, greenfield): 0/38 paths tested | GAPS: 38 (9 E2E, 0 eval)
```

Test plan artifact: `~/.gstack/projects/follow-the-money/tru-master-eng-review-test-plan-20260925-083534.md` (copy: `docs/reviews/eng-review-2-test-plan-2026-09-25.md`). Design-review test requirements (grayscale screenshot test, axe in E2E, three viewports, literal-string lint, formatter unit tests, PNG checks) are carried as required proof.

### NOT in scope

- Link-preview metadata (Open Graph) for shared entity URLs: not raised by any decision; revisit with the R36 indexing answer.
- A CDN in front of FastAPI: add when traffic warrants (R27 con).
- Restoring table pages beyond the first from a shared link (R45 states the limit).
- Everything the first eng review and the design review put out of scope (M2/M3, exports, timeline, moderator web UI).

### What already exists

- Reused as-is: Cytoscape.js + fcose (graph, `cy.png()`), browser `Intl` and `document.fonts`, Vite dynamic `import()`, the Python `holidays` package (CO calendar), Supabase Postgres + Auth, Socrata SODA.
- Reused from this project: the first eng review's redirect table, MaskedId serializer, R3 ordering, R15 load test (extended), privacy suite (extended to edge keys and `/vista`).
- Not reused: `entity-page.html` code and Pretext (spec only, R31).

### Failure modes

| New path | Realistic production failure | Test / handling | User sees |
|---|---|---|---|
| route status | static fallback returns 200 for a suppressed URL | R27 identical-404 test | not found |
| merge redirect | merge later split, browser cached old target | 302 no-store (R41), test | correct entity |
| edge keys | key names a suppressed person | 404 like unknown (R28), test | "no disponible" |
| values | 8.8e20 typo tops the list | flag + exclusion (R29), test | plausible totals, raw value in drawer |
| fonts | Fontshare moves a file, cache wiped | private store + hash (R30, R44), build fails otherwise | correct fonts or no deploy |
| table paging | nightly ingest between pages | revision restart (R42), test | restart notice |
| corrections | email provider down | ticket kept, retry (R33), test | ticket + fecha estimada |
| corrections | deadline off by a Monday holiday | CO calendar tests (R33, R39) | correct estimated date |
| shared links | 12-step trail through hubs | payload cap + load test (R40) | view in ≤ 1.5 s |
| phones | slow 3G first load | lazy graph chunk (R35) | list first |

Critical gaps (no test, no handling, silent): **0**.

### Worktree parallelization strategy

| Step | Modules touched | Depends on |
|---|---|---|
| T17 web serving + status + redirects | `backend/cabosueltos/api` (web routes), `apps/web` build config | first-review S1, S6 |
| T18 edge keys + evidence | `backend/cabosueltos/api` | first-review S3, S6 |
| T19 value flag | `backend/cabosueltos/ingest`, `backend/cabosueltos/ids` | first-review S2, S4 |
| T20 font pipeline | `apps/web/scripts`, `.github/` | first-review S1 |
| T21 table sort/filter + revision | `backend/cabosueltos/api`, `backend/cabosueltos/db`, `apps/web` table | T18 |
| T22 corrections | `backend/cabosueltos/api`, `backend/cabosueltos/resolver`, `apps/web` routes | first-review S5 |
| T23 `/vista` + URL format | `backend/cabosueltos/api`, `apps/web/src/state` | T18, T21 |
| T24 lazy graph | `apps/web/src/graph` | first-review S7 |
| T25 legal agenda doc | `docs/legal` | — |

Lanes: A: T19 (ingest). B: T18 → T21 → T23 (shared `api` expand/evidence code). C: T17, then T22 (api routes). D: T20 → T24 (web build and graph). E: T25 (docs).
Execution order: launch A + B + C + D + E after the first review's S1-S6 foundations. Merge B's T18 before C's T22 touches the router.
Conflict flags: B and C both register routes in `backend/cabosueltos/api`; agree on the router module layout before both lanes start. T21 adds a revision counter that T19's ingest must bump; coordinate A and B on that column.

## Implementation Tasks
Synthesized from this review's findings. Each task derives from a specific
finding above. Run with Claude Code or Codex; checkbox as you ship.
Numbering continues the first eng review's T1-T16. Effort assumption: features ~30x, tests ~50x, docs ~10x human/CC.

- [ ] **T17 (P1, human: ~1 day / CC: ~30min)** — api — Serve the Vite build from FastAPI with per-route status (200 / 302 no-store / identical 404), CSP and `X-Robots-Tag`
  - Surfaced by: Section 1 A1 (R27); Outside Voice X4 (R41)
  - Files: `backend/cabosueltos/api/web.py`, `apps/web/vite.config.ts`
  - Verify: tests for each status; unknown vs suppressed bodies byte-identical; merged URL keeps query and fragment
- [ ] **T18 (P1, human: ~1 day / CC: ~30min)** — api — Derived-edge keys and paginated `/edge/:key/evidence` with redirect resolution
  - Surfaced by: Section 1 A2 (R28)
  - Files: `backend/cabosueltos/api/edges.py`, `backend/cabosueltos/api/evidence.py`
  - Verify: round trip, redirect, suppressed → 404, no ID digits in any key
- [ ] **T19 (P1, human: ~1 day / CC: ~40min)** — ingest — `valor_atipico` flag, 200-row calibration, exclusion from sums and value order, "COP 0" vs "sin valor reportado"
  - Surfaced by: Section 1 A3 (R29); Outside Voice X6 (R43)
  - Files: `backend/cabosueltos/ingest/secop.py`, `backend/cabosueltos/ids/values.py`
  - Verify: unit tests per rule; calibration numbers recorded in this doc
- [ ] **T20 (P1, human: ~1 day / CC: ~30min)** — web — Font pipeline: pinned fetch, SHA-256 manifest, CI cache → private store → Fontshare, fail-closed prod build, `fonts.load()` before draw, no-third-party E2E
  - Surfaced by: Section 1 A4 (R30); Outside Voice X7 (R44)
  - Files: `apps/web/scripts/fetch-fonts.ts`, `apps/web/fonts.manifest.json`, `.github/workflows/ci.yml`
  - Verify: tampered file fails; all sources down fails prod build; E2E sees no third-party request
- [ ] **T21 (P1, human: ~1 day / CC: ~45min)** — api — Server-side table sort/filter with keyset cursors, ULID tie-breaker, revision restart
  - Surfaced by: Section 2 Q1 (R32); Outside Voice X5 (R42)
  - Files: `backend/cabosueltos/api/expand.py`, `apps/web/src/components/ConnectionsTable.tsx`
  - Verify: each sort pages without gaps or repeats; revision bump restarts; load test sorts on top-100 hubs
- [ ] **T22 (P1, human: ~2 days / CC: ~1h)** — api — Correction intake, deadlines, abuse control, notification, privacy notice, retention
  - Surfaced by: Section 2 Q2 (R33); Outside Voice X1 (R38), X2 (R39)
  - Files: `backend/cabosueltos/api/correcciones.py`, `backend/cabosueltos/resolver/tickets.py`, `apps/web/src/routes/correccion.tsx`
  - Verify: holiday deadlines, `completa_desde` recompute, paused reclamo, rate limit, honeypot, email failure, retention job
- [ ] **T23 (P1, human: ~1.5 days / CC: ~50min)** — api — `/api/vista` one-request restore with payload cap and per-step kinds in the URL
  - Surfaced by: Section 4 P1 (R34); Outside Voice X3 (R40), X8 (R45)
  - Files: `backend/cabosueltos/api/vista.py`, `apps/web/src/state/url.ts`
  - Verify: merged/vanished/suppressed/split trail cases; cap; ≤ 672 neighbors; p95 ≤ 1.5 s
- [ ] **T24 (P3, human: ~2h / CC: ~15min)** — web — Lazy-load the graph module on phones until "Grafo" opens
  - Surfaced by: Section 4 P2 (R35)
  - Files: `apps/web/src/graph/index.ts`
  - Verify: Playwright at 390px sees no graph chunk before "Grafo"
- [ ] **T25 (P2, human: ~1h / CC: ~10min)** — docs — Compile the M1 legal-review agenda (R2, R4, R29 wording, R36 indexing, R38 notice and retention, R39 deadline clock, plus the TODOS.md correction-flow items)
  - Surfaced by: TODO R36; Outside Voice X1, X2
  - Files: `docs/legal/m1-legal-review-agenda.md`
  - Verify: every legal-review item in this file and TODOS.md appears once

JSONL: `~/.gstack/projects/follow-the-money/tasks-eng-review-20260925-083554.jsonl` (copy: `docs/reviews/tasks-eng-review-2-2026-09-25.jsonl`).

### Unresolved decisions

None. Every choice in this review has an actual answer (D3 was not answered and was re-asked as D4).

### Completion summary
- Step 0: Scope Challenge — scope accepted as-is (M1 per first-review D3/D4); 2 findings, both resolved (doc rewrite, slug)
- Architecture Review: 5 issues found
- Code Quality Review: 2 issues found
- Test Review: diagram produced, 38 planned gaps identified (greenfield)
- Performance Review: 2 issues found
- NOT in scope: written
- What already exists: written
- TODOS.md updates: 2 items proposed to user (1 added to TODOS.md, 1 built now: the outside-voice rerun)
- Failure modes: 0 critical gaps flagged
- Unresolved decisions: 0 in this review
- Outside voice: codex, first run unavailable (5-minute timeout), rerun completed (8 findings, all resolved: 8 applied)
- Parallelization: 5 lanes, 4 parallel / 1 sequential (B: T18 → T21 → T23)
- Lake Score: 8/8 (picked the most complete option on every coverage choice: D1, D4, D6, D7, D9, D10, D11, D12)

### Suppressed findings (appendix)

- (unreviewed, partial) First Codex run, timed out; only the tail was captured: "Correction intake can overwhelm its own deadline system" (triage and escalation for abusive or incomplete requests) and "The pagination guarantee fails during refresh". The second is covered by R42; the first is partly covered by R33/R39 (rate limit, `pendiente_de_completar`). It also mentioned "its success gate can approve a site with no discoveries" and "relationship, split ... contracts" without detail. No coverage credit.
- (confidence 4/10) Open Graph link previews could put a natural person's name into chat-app previews. Suppressed: no preview metadata is planned; revisit with R36.

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 0 | — | — |
| Outside Review | codex CLI via `/plan-eng-review` outside voice | Independent 2nd opinion | 3 | completed (issues_found) | this run: 8 findings, all resolved (8 applied); an earlier attempt this run timed out (unavailable) |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 3 | ISSUES OPEN (PLAN, this run) | 9 issues, 0 critical gaps |
| Design Review | `/plan-design-review` | UI/UX gaps | 1 | ISSUES OPEN (FULL) | score: 0/10 → 5/10, 23 decisions |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | — |

- **OUTSIDE COVERAGE:** codex, phase plan-review, 2026-09-25: first attempt unavailable (5-minute timeout, native fallback unavailable); rerun completed with 8 findings, all resolved through D15-D22 (one partly a factual correction). Prior: codex plan-review (first eng review) completed; codex and Claude subagent design phases completed.
- **CROSS-MODEL:** Claude (host) and codex (outside, gpt-6-sol) agreed on all 8 outside findings after checking them against the plan; codex's R29 recompute concern was narrowed by a factual correction (the flag reads the row's own document type). Codex alone raised the correction-data privacy, deadline clock, 301 caching, payload bound and per-step filter issues. No unresolved disagreement.
- **VERDICT:** No review CLEAR yet. Eng Review found 9 issues (plus 2 scope findings and 8 outside findings), all mapped to approved decisions R25-R45 and Implementation Tasks T17-T25 (issues_open = mapped work, not open questions); eng review required.

**UNRESOLVED DECISIONS:**
- + 2 unresolved from prior reviews (plan-design-review: design tokens and product name; both since settled by /design-consultation and R26 but not re-logged by a design review)
